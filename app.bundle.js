/* AuditSphere product visualization. Dependency-free and local-only.
 * This is NOT a secure accounting system. All roles, reviews and releases are
 * demonstrative, all records are synthetic, and no provider APIs are called.
 */
'use strict';
const PRD = JSON.parse(document.getElementById('prd-data').textContent);
const STORAGE_KEY = 'ste-auditsphere-role-portals-v2';
const SCHEMA = 2;
const DEMO_DATE = '2026-09-21';
const ROLES = JSON.parse(document.getElementById('role-data').textContent);
const paths = {
  grid:'M3 3h7v7H3z M14 3h7v7h-7z M3 14h7v7H3z M14 14h7v7h-7z',
  users:'M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2 M16 3a4 4 0 0 1 0 8 M22 21v-2a4 4 0 0 0-3-3.87 M13 7a4 4 0 1 1-8 0 4 4 0 0 1 8 0',
  user:'M20 21v-2a7 7 0 0 0-14 0v2 M17 7a5 5 0 1 1-10 0 5 5 0 0 1 10 0',
  brief:'M3 7h18v14H3z M8 7V3h8v4 M3 12h18 M10 12v3h4v-3',
  target:'M21 12a9 9 0 1 1-9-9 M17 12a5 5 0 1 1-5-5 M12 12l9-9 M17 3h4v4',
  folder:'M3 5h6l2 3h10v12H3z',
  file:'M14 2H5v20h14V7z M14 2v5h5 M8 12h8 M8 16h8',
  chart:'M3 3v18h18 M7 15v-4 M12 15V7 M17 15V4',
  calculator:'M5 2h14v20H5z M8 5h8v4H8z M8 13h1 M15 13h1 M8 17h1 M15 17h1',
  check:'M5 12l4 4L19 6',
  checkcircle:'M22 11.1V12a10 10 0 1 1-5.9-9.1 M22 4 12 14l-3-3',
  shield:'M12 2 3 6v6c0 6 9 10 9 10s9-4 9-10V6z M8 12l3 3 5-6',
  arrow:'M5 12h14 M14 7l5 5-5 5',
  down:'M6 9l6 6 6-6',
  left:'M19 12H5 M10 7l-5 5 5 5',
  search:'M21 21l-5-5 M18 10a8 8 0 1 1-16 0 8 8 0 0 1 16 0',
  clock:'M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0 M12 6v6l4 2',
  calendar:'M3 5h18v16H3z M16 3v4 M8 3v4 M3 11h18',
  plus:'M12 5v14 M5 12h14',
  more:'M4 12h.01 M12 12h.01 M20 12h.01',
  upload:'M12 16V3 M7 8l5-5 5 5 M3 16v5h18v-5',
  download:'M12 3v13 M7 11l5 5 5-5 M3 16v5h18v-5',
  bell:'M18 8a6 6 0 0 0-12 0c0 7-3 7-3 9h18c0-2-3-2-3-9 M10 21h4',
  refresh:'M21 3v6h-6 M3 21v-6h6 M3 9a9 9 0 0 1 15-6l3 6 M21 15a9 9 0 0 1-15 6l-3-6',
  settings:'M12 8a4 4 0 1 1 0 8 4 4 0 0 1 0-8 M9 2h6l1 4 4 1 2 5-2 5-4 1-1 4H9l-1-4-4-1-2-5 2-5 4-1z',
  book:'M12 5C8 2 4 3 2 4v16c3-2 6-2 10 0 4-2 7-2 10 0V4c-2-1-6-2-10 1z M12 5v15',
  logout:'M9 21H3V3h6 M10 12h11 M16 7l5 5-5 5',
  close:'M6 6l12 12 M6 18 18 6',
  play:'M8 4l12 8-12 8z',
  lock:'M5 10h14v12H5z M8 10V6a4 4 0 0 1 8 0v4 M12 15v3',
  eye:'M1 12s4-7 11-7 11 7 11 7-4 7-11 7-11-7-11-7 M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0',
  message:'M21 3H3v14h4v5l6-5h8z M7 7h10 M7 11h7',
  layers:'M12 2 2 7l10 5 10-5z M2 12l10 5 10-5 M2 17l10 5 10-5',
  cloud:'M6 18H5a4 4 0 0 1-1-8 7 7 0 0 1 13-3 5 5 0 0 1 2 11h-1 M9 14l3-3 3 3 M12 11v11',
  menu:'M3 6h18 M3 12h18 M3 18h18',
  info:'M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0 M12 8h.01 M12 11v6',
  alert:'M12 3 1 21h22z M12 9v5 M12 17h.01',
  link:'M10 13a5 5 0 0 0 7 0l4-4a5 5 0 0 0-7-7l-2 2 M14 11a5 5 0 0 0-7 0l-4 4a5 5 0 0 0 7 7l2-2',
  archive:'M3 3h18v5H3z M5 8v13h14V8 M10 12h4',
  globe:'M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0 M2 12h20 M12 2c-6 6-6 14 0 20 6-6 6-14 0-20',
  trend:'M3 17l6-6 4 4 8-11 M16 4h5v5',
  send:'M22 2 9 15 M22 2l-7 20-6-7-7-6z',
  flow:'M12 3v5 M4 12V8h16v4 M1 12h6v7H1z M9 12h6v7H9z M17 12h6v7h-6z',
  arrowup:'M12 20V4 M5 11l7-7 7 7',
  spark:'M12 2l3 7 7 3-7 3-3 7-3-7-7-3 7-3z',
  receipt:'M4 3l2 2 3-2 3 2 3-2 3 2 2-2v19l-2-2-3 2-3-2-3 2-3-2-2 2z M8 9h8 M8 13h8 M8 17h4',
  checkboard:'M9 3h6v4H9z M9 5H4v17h16V5h-5 M8 13l3 3 5-6'
};
function icon(name,cls=''){return `<svg class="icon ${cls}" aria-hidden="true" viewBox="0 0 24 24"><path d="${paths[name]||paths.file}"/></svg>`;}
function esc(v){return String(v??'').replace(/[&<>"']/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[s]));}
const clone=v=>JSON.parse(JSON.stringify(v));
const fmt=v=>new Intl.NumberFormat('en-US',{minimumFractionDigits:2,maximumFractionDigits:2}).format(v/100);
const cash=v=>'QAR '+fmt(v);
const moneyView=v=>v<0?'('+fmt(-v)+')':fmt(v);
const shortDate=s=>new Date(s+'T12:00:00').toLocaleDateString('en-GB',{day:'2-digit',month:'short'});
function cents(s){if(!/^-?\d+(\.\d{1,2})?$/.test(String(s).trim()))throw Error('Use decimal amounts with at most two places.'); const n=Number(s);if(!Number.isSafeInteger(Math.round(n*100))||Math.abs(n)>1e9)throw Error('Amount is outside the prototype limit.');return Math.round(n*100);}
function badge(text,color=''){const key=String(text).toLowerCase();color=color||(/approved|cleared|accepted|active|balanced|delivered|archived|received|current|continued|applied|complete|verified/.test(key)?'green':/review|responded|ready/.test(key)?'purple':/overdue|blocked|stale|rejected|changes|declined/.test(key)?'red':/pending|awaiting|requested|draft|planning/.test(key)?'amber':'gray');return `<span class="tag ${color}"><span class="tagdot"></span>${esc(text)}</span>`;}
function btn(text,action,cls='',extra='',ic=''){return `<button class="btn ${cls}" data-action="${action}" ${extra}>${ic?icon(ic):''}${text}</button>`;}
function linkBtn(text,route,cls=''){return `<button class="btn ghost ${cls}" data-route="${route}">${text}${icon('arrow')}</button>`;}
function avatar(initials,color='',size=''){return `<span class="avatar ${color} ${size}">${esc(initials)}</span>`;}
function panel(title,sub,body,action=''){return `<section class="panel"><div class="panel-head"><div><h2>${title}</h2>${sub?`<div class="sub">${sub}</div>`:''}</div>${action}</div>${body}</section>`;}
function metric(label,value,caption,ic,color=''){return `<div class="metric"><div class="metric-top"><span>${label}</span><span class="metric-icon ${color}">${icon(ic)}</span></div><div class="metric-value">${value}</div><div class="metric-caption">${caption}</div></div>`;}
function note(text,color='',extra=''){return `<div class="banner ${color}">${icon(color==='red'?'alert':'info')}<p>${text}</p>${extra}</div>`;}
function footer(ref='STE-PRD-001'){return `<footer class="pagefooter"><span>Illustrative records · Browser-only simulation · No live financial or provider actions</span><button data-action="source" data-section="1">${ref} · Requirements reference ${icon('arrow')}</button></footer>`;}
function head(title,sub,actions='',eyebrow=''){return `<div class="pagehead"><div>${eyebrow?`<div class="eyebrow mb16">${eyebrow}</div>`:''}<h1>${title}</h1><p>${sub}</p></div><div class="head-actions">${actions}</div></div>`;}
function tabs(items,key){return `<div class="tabs">${items.map(([id,label])=>`<button class="tab ${ui[key]===id?'active':''}" data-action="tab" data-key="${key}" data-id="${id}">${label}</button>`).join('')}</div>`;}
function initialRows(multiplier=1){return [
 ['1000','Cash','asset',1000000],['1100','Trade receivables','asset',500000],['1500','Equipment cost','asset',800000],
 ['5000','Operating expenses','expense',300000],['2000','Trade payables','liability',-300000],['2200','Loan','liability',-700000],
 ['3000','Opening equity','equity',-1000000],['4000','Revenue','revenue',-600000]
].map(([code,name,type,balance])=>({code,name,type,balance:balance*multiplier}));}
function newEng(id,client,service='External audit',stage='Review',multiplier=1){return {
 id,client,service,stage,year:2026,mode:'External books',period:'01 Jan – 31 Dec 2026',due:'2026-09-28',manager:'Layla Rahman',partner:'Daniel James',acceptance:true,terms:true,planning:true,sourceAccepted:true,mappingApproved:true,generation:3,packageRevision:3,builtGeneration:3,sourceVersion:1,
 rows:initialRows(multiplier),adjustment:50000*multiplier,journalState:'Applied',sourceReflection:false,supplements:true,
 reconciliations:[{name:'Cash & bank',ref:'REC-01',status:'Cleared',evidence:'Bank statement and book balance'},{name:'Trade receivables',ref:'REC-02',status:'Cleared',evidence:'Ageing schedule tied to TB'},{name:'Property and equipment',ref:'REC-03',status:'Cleared',evidence:'Asset register and depreciation'},{name:'Equity',ref:'REC-04',status:'Cleared',evidence:'Opening equity and current profit'}],
 workpapers:[{id:'WP-A1',title:'Cash & bank',objective:'Agree cash balances to the ledger and supporting statement.',assertion:'Existence · Completeness',risk:'Unreconciled cash balances',version:2,status:'Cleared',preparer:'Adam Khan',reviewer:'Sara Malik',conclusion:'Illustrative reconciliation ties to the selected reporting balance.'},
 {id:'WP-B1',title:'Trade receivables',objective:'Evaluate year-end receivables and supporting ageing.',assertion:'Existence · Valuation',risk:'Overstated or unrecoverable receivables',version:2,status:'Cleared',preparer:'Adam Khan',reviewer:'Sara Malik',conclusion:'Synthetic ageing agrees to the selected trial balance.'},
 {id:'WP-C1',title:'Fixed assets',objective:'Review equipment cost and the supported depreciation adjustment.',assertion:'Valuation · Rights',risk:'Unsupported depreciation assumptions',version:3,status:'Changes required',preparer:'Adam Khan',reviewer:'Sara Malik',conclusion:'Depreciation proposal is linked to the illustrative asset schedule; reviewer response is pending.'},
 {id:'WP-F1',title:'Completion & disclosures',objective:'Tie final statements and complete the disclosure review.',assertion:'Presentation · Completeness',risk:'Missing financial statement disclosures',version:1,status:'Changes required',preparer:'Adam Khan',reviewer:'Sara Malik',conclusion:'Final disclosures require a management confirmation and independent clearance.'}],
 reviews:[{id:'RN-001',wp:'WP-C1',title:'Support the depreciation assumptions',body:'Document the useful-life rationale supporting the QAR 500 depreciation adjustment. Link the updated asset schedule and explain the accounting treatment.',severity:'High',status:'Open',raisedBy:'Sara Malik',assigned:'Adam Khan',due:'2026-09-23',response:'',version:3,history:[]},{id:'RN-002',wp:'WP-F1',title:'Confirm related-party disclosures',body:'Obtain the management confirmation and explain the applicability of the related-party note. A blank disclosure is not an approved not-applicable decision.',severity:'Medium',status:'Open',raisedBy:'Sara Malik',assigned:'Adam Khan',due:'2026-09-24',response:'',version:1,history:[]}],
 approvals:{manager:null,client:null,partner:null,eqr:null},eqrRequired:true,opinion:'Unmodified — illustrative only',candidate:null,releases:[],archive:null,
 pbc:[{id:'PBC-01',title:'Year-end trial balance',category:'Financial records',status:'Accepted',due:'2026-09-18',owner:'Omar Nasser',file:'FY2026_Trial_Balance.csv',version:1},{id:'PBC-02',title:'Bank statement and reconciliation',category:'Cash & bank',status:'Accepted',due:'2026-09-20',owner:'Omar Nasser',file:'Bank_Statement_December.pdf',version:1},{id:'PBC-03',title:'Fixed-asset register and useful lives',category:'Fixed assets',status:'Requested',due:'2026-09-23',owner:'Omar Nasser',file:'',version:0},{id:'PBC-04',title:'Related-party confirmation',category:'Disclosures',status:'Requested',due:'2026-09-24',owner:'Omar Nasser',file:'',version:0}],annual:{confirmed:[],decision:null,nextId:null},questionnaire:{answers:{},status:'Not started'},events:[]};}
function seed(){const clients=[{id:'CL-001',name:'Example Trading Entity',initials:'ET',color:'',industry:'Trading & distribution',contact:'Omar Nasser',risk:'Moderate',status:'Active',country:'Demo jurisdiction',revenue:600000}, {id:'CL-002',name:'Northstar Services',initials:'NS',color:'purple',industry:'Professional services',contact:'Aisha Saleh',risk:'Low',status:'Active',country:'Demo jurisdiction',revenue:1800000}, {id:'CL-003',name:'Cedar Manufacturing',initials:'CM',color:'blue',industry:'Manufacturing',contact:'Noah Williams',risk:'Moderate',status:'Active',country:'Demo jurisdiction',revenue:2400000}, {id:'CL-004',name:'Harbor Logistics',initials:'HL',color:'amber',industry:'Transport & logistics',contact:'Riya Ahmed',risk:'Low',status:'Active',country:'Demo jurisdiction',revenue:1200000}];
 const engs=[newEng('ENG-26001','CL-001'),newEng('ENG-26002','CL-002','Annual accounts','Accounting',3),newEng('ENG-26003','CL-003','External audit','Fieldwork',4),newEng('ENG-26004','CL-004','Internal audit','Planning',2)];
 engs[1].eqrRequired=false;engs[1].reviews=engs[1].reviews.slice(0,1);engs[1].workpapers=engs[1].workpapers.slice(0,3);engs[2].reviews=engs[2].reviews.slice(0,1);engs[3].reviews=[];engs[3].eqrRequired=false;engs[3].workpapers=engs[3].workpapers.map(w=>({...w,status:'Planned'}));
 return {schema:SCHEMA,selected:'ENG-26001',role:'manager',clients,engagements:engs,leads:[{id:'LD-001',name:'Aspen Retail',contact:'Mira Thomas',service:'Annual accounts',value:850000,stage:'Inquiry',accepted:false,terms:false},{id:'LD-002',name:'Summit Engineering',contact:'Zayd Hasan',service:'External audit',value:1400000,stage:'Discovery',accepted:false,terms:false},{id:'LD-003',name:'Orchid Hospitality',contact:'Lena James',service:'External audit',value:1800000,stage:'Evaluation',accepted:false,terms:false},{id:'LD-004',name:'Atlas Technology',contact:'Hana Ali',service:'Internal audit',value:1200000,stage:'Proposal',accepted:true,terms:false}],invoices:[{id:'INV-26001',client:'CL-001',eng:'ENG-26001',description:'Annual audit · first milestone',amount:600000,paid:0,status:'Issued',due:'2026-09-28'},{id:'INV-26002',client:'CL-002',eng:'ENG-26002',description:'Annual accounts · final milestone',amount:850000,paid:850000,status:'Paid',due:'2026-09-18'},{id:'INV-26003',client:'CL-003',eng:'ENG-26003',description:'Financial audit · retainer',amount:900000,paid:0,status:'Draft',due:'2026-10-01'}],times:[{name:'Adam Khan',task:'Fixed-asset workpaper',hours:3,eng:'ENG-26001',date:DEMO_DATE}],events:[{text:'Financial package v3 prepared for review',ref:'ENG-26001',time:'Sample · 09:40',type:'file'},{text:'Sara raised a depreciation review point',ref:'RN-001',time:'Sample · 09:20',type:'message'},{text:'Trial balance accepted and mapped',ref:'ENG-26001',time:'Sample · 08:45',type:'checkcircle'}]};}
let state, persist=true;
try{const raw=localStorage.getItem(STORAGE_KEY);state=raw?JSON.parse(raw):seed();if(state.schema!==SCHEMA||!Array.isArray(state.engagements)||!Array.isArray(state.clients))state=seed();}catch{state=seed();persist=false;}
const knownRoutes=['overview','clients','acquisition','engagements','onboarding','documents','accounting','audit','reviews','delivery','portal','billing','continuance','services','requirements'];
let ui={route:knownRoutes.includes(location.hash.slice(1))?location.hash.slice(1):'overview',accountTab:'tb',auditTab:'workpapers',reviewTab:'notes',requirementTab:'lifecycle',selectedReview:'RN-001',onboardTab:'assessment',search:'',clientFilter:'',docFilter:'All',serviceFilter:'',referenceSearch:'',modal:null};
let lastFocus=null, uploadTarget=null;
const E=()=>state.engagements.find(x=>x.id===state.selected)||state.engagements[0];
const C=id=>state.clients.find(x=>x.id===(id||E().client));
const actor=()=>ROLES[state.role];
function save(){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(state));}catch{persist=false;}}
function toast(text,type=''){const t=document.createElement('div');t.className='toast '+type;t.textContent=text;document.getElementById('toasts').appendChild(t);setTimeout(()=>t.remove(),5600);}
function log(text,ref=E().id,type='checkcircle'){const t={text,ref,time:'Demo · '+new Date().toLocaleTimeString('en-GB',{hour:'2-digit',minute:'2-digit'}),type};state.events.unshift(t);state.events=state.events.slice(0,80);E().events.unshift(t);save();}
function roleAllowed(role){if(![role].flat().includes(state.role)){toast('Switch the demo role to '+[role].flat().map(r=>ROLES[r].label).join(' or ')+'.','warn');return false;}return true;}
function changed(reason){const e=E();e.generation++;e.candidate=null;log(reason+' · dependent approvals are now stale');save();}
function isCurrent(key,e=E()){return e.approvals[key]?.generation===e.generation;}
function allNotesClear(e=E()){return e.reviews.every(r=>r.status==='Cleared');}
function allWorkClear(e=E()){return e.workpapers.every(w=>w.status==='Cleared');}
function isAudit(e=E()){return e.service==='External audit';}
function totals(e=E()){
 const rows=clone(e.rows);if(e.journalState==='Applied'&&!e.sourceReflection){const a=rows.find(r=>r.code==='1590'),b=rows.find(r=>r.code==='5200');if(a)a.balance-=e.adjustment;else rows.push({code:'1590',name:'Accumulated depreciation',type:'asset',balance:-e.adjustment});if(b)b.balance+=e.adjustment;else rows.push({code:'5200',name:'Depreciation expense',type:'expense',balance:e.adjustment});}
 const sum=t=>rows.filter(r=>r.type===t).reduce((s,r)=>s+r.balance,0);
 return {rows,assets:sum('asset'),liabilities:-sum('liability'),openingEquity:-sum('equity'),revenue:-sum('revenue'),expenses:sum('expense'),profit:-sum('revenue')-sum('expense'),balance:rows.reduce((s,r)=>s+r.balance,0)};
}
function releaseBlockers(e=E()){const out=[];if(!e.acceptance)out.push('Partner acceptance is pending');if(!e.terms)out.push('Effective terms are not recorded');if(!e.planning)out.push('Planning approval is pending');if(!e.sourceAccepted)out.push('Source trial balance is unaccepted');if(!e.mappingApproved)out.push('Mapping is unapproved');if(totals(e).balance!==0)out.push('Trial balance is unbalanced');if(e.builtGeneration!==e.generation)out.push('Rebuild the package for the current input generation');if(!e.supplements)out.push('Supporting cash-flow/disclosure schedules are missing');if(!allNotesClear(e))out.push('Review points need independent clearance');if(!allWorkClear(e))out.push('Working papers are not all cleared');if(e.reconciliations.some(r=>r.status!=='Cleared'))out.push('Reconciliations are incomplete');if(!isCurrent('manager',e))out.push('Current manager review is missing');if(!isCurrent('client',e))out.push('Current management approval is missing');if(!isCurrent('partner',e))out.push('Current partner authorization is missing');if(e.eqrRequired&&!isCurrent('eqr',e))out.push('Required EQR is incomplete');return out;}
function stage(e){const last=e.releases.at(-1);if(e.archive&&last?.generation===e.generation)return 'Archived';if(last?.generation===e.generation)return last.delivered?'Delivered':'Demo issued';if(e.builtGeneration!==e.generation)return 'Changes required';if(!e.acceptance||!e.terms)return 'Onboarding';if(!e.planning)return 'Planning';if(releaseBlockers(e).length===0)return 'Ready to issue';return e.stage;}
function percent(e){const v=[e.acceptance,e.terms,e.planning,e.sourceAccepted,e.mappingApproved,e.builtGeneration===e.generation,allNotesClear(e),allWorkClear(e),isCurrent('manager',e),isCurrent('client',e),isCurrent('partner',e),!e.eqrRequired||isCurrent('eqr',e),e.releases.length>0,!!e.archive];return Math.round(v.filter(Boolean).length/v.length*100);}
function context(){const e=E(),c=C();return `<div class="contextbar"><div class="context-item"><label>Client / engagement</label><select id="eng-select" aria-label="Selected engagement">${state.engagements.map(x=>`<option value="${esc(x.id)}" ${x.id===e.id?'selected':''}>${esc(C(x.client)?.name||'Client')} · ${x.year}</option>`).join('')}</select></div><div class="context-item"><label>Service</label>${esc(e.service)}</div><div class="context-item"><label>Period</label>FY ${e.year}</div><div class="context-item"><label>Mode / currency</label>${esc(e.mode)} · QAR</div><div class="context-item"><label>Package</label><span class="mono">v${e.packageRevision}</span> ${badge(e.builtGeneration===e.generation?'Current':'Stale',e.builtGeneration===e.generation?'green':'red')}</div></div>`;}
const nav=[['WORKSPACE',[['overview','Practice overview','grid'],['clients','Client portfolio','users'],['acquisition','Acquisition','target'],['engagements','Engagements','brief'],['onboarding','Acceptance & KYC','shield']]],['ENGAGEMENT DELIVERY',[['documents','Documents & PBC','folder'],['accounting','Accounting','calculator'],['audit','Audit workpapers','checkboard'],['reviews','Reviews & sign-off','message'],['delivery','Delivery & archive','archive']]],['PRACTICE',[['portal','Client portal preview','globe'],['billing','Billing & time','receipt'],['continuance','Annual continuance','refresh'],['services','Service catalogue','layers'],['requirements','Requirements & flow','flow']]]];
function render(){const title=nav.flatMap(g=>g[1]).find(x=>x[0]===ui.route)?.[1]||'Overview';const open=E().reviews.filter(r=>r.status!=='Cleared').length;
 document.getElementById('app').innerHTML=`<div class="mobile-overlay" data-action="menu" id="mobile-overlay"></div><aside class="sidebar" id="sidebar"><div class="brand"><div class="brandmark">${icon('layers')}</div><div class="brandname">Audit<span>Sphere</span></div></div><div class="workspaceselect"><span class="firmavatar">STE</span><span>STE Audit & Accounting<div style="font-size:9px;color:#789598;margin-top:1px">Practice workspace</div></span></div><nav class="side-scroll" aria-label="Main navigation">${nav.map(([group,items])=>`<div class="nav-label">${group}</div>${items.map(([route,label,ic])=>`<button class="navitem ${route===ui.route?'active':''}" data-route="${route}" ${route===ui.route?'aria-current="page"':''}>${icon(ic)}<span>${label}</span>${route==='reviews'&&open?`<span class="count">${open}</span>`:''}</button>`).join('')}`).join('')}</nav><div class="side-footer"><div class="side-demo"><strong><span class="demo-dot" style="display:inline-block;margin-right:6px"></span>Interactive product prototype</strong>Synthetic records. No live integrations.<br>${persist?'Changes saved in this browser.':'Browser storage unavailable; session only.'}</div><button class="navitem" data-action="reset">${icon('refresh')}Reset demo workspace</button></div></aside><div class="shell"><header class="topbar"><div class="topbar-left"><button class="icon-btn mobile-menu" data-action="menu" aria-label="Open navigation">${icon('menu')}</button><div class="crumb">Workspace &nbsp;/&nbsp; <b>${esc(title)}</b></div><button class="search-trigger" data-action="search">${icon('search')}<span>Search clients, work, services…</span><kbd>/</kbd></button></div><div class="topbar-right"><span class="demo-pill"><span class="demo-dot"></span>LOCAL DEMO</span><button class="btn sm tour-header" data-action="tour">${icon('play')}Explore the demo</button><button class="icon-btn" data-action="activity" aria-label="View activity">${icon('bell')}</button><div class="persona">${avatar(actor().initials)}<div><label for="role-select">SIMULATED ROLE</label><select id="role-select">${Object.entries(ROLES).map(([id,r])=>`<option value="${id}" ${id===state.role?'selected':''}>${r.label}</option>`).join('')}</select></div></div></div></header><main class="main" id="main" tabindex="-1">${pages[ui.route]()}</main></div>`;
 document.title=`${title} · AuditSphere Prototype`;
}
function go(route){if(!knownRoutes.includes(route))return;ui.route=route;ui.search='';location.hash=route;render();window.scrollTo(0,0);}
function engagementRows(list){return list.map(e=>{const c=C(e.client);return `<tr><td><div class="row">${avatar(c.initials,c.color,'square')}<div><button class="tablelink" data-action="eng-open" data-id="${e.id}"><b>${esc(c.name)}</b></button><div class="cell-sub">${e.id} · FY ${e.year}</div></div></div></td><td><span class="small">${esc(e.service)}</span><div class="cell-sub">${esc(e.mode)}</div></td><td>${badge(stage(e))}</td><td><div class="progress-row"><div class="progress ${stage(e)==='Review'?'purple':''}"><i style="width:${percent(e)}%"></i></div><span>${percent(e)}%</span></div></td><td><span class="small">${shortDate(e.due)}</span></td><td><div class="avatar-group">${avatar('AK','','sm')}${avatar('SM','purple','sm')}</div></td><td><button class="icon-btn" data-action="eng-open" data-id="${e.id}" aria-label="Open ${esc(c.name)}">${icon('arrow')}</button></td></tr>`;}).join('');}
function overview(){const engs=state.engagements,open=engs.reduce((s,e)=>s+e.reviews.filter(r=>r.status!=='Cleared').length,0),requests=engs.reduce((s,e)=>s+e.pbc.filter(p=>p.status!=='Accepted').length,0);
return head('A clear view of every engagement.','Your practice, in sync. Client work, reviews and deadlines in one place.',`<span class="btn sm">${icon('calendar')}21 Sep 2026 · sample day</span>${btn('New engagement','new-eng','primary','','plus')}`)+
`<div class="metric-grid">${metric('Active engagements',engs.filter(e=>!e.archive).length,'Across '+state.clients.length+' illustrative clients','brief')}${metric('Awaiting review',open,'Open review points across the practice','message','purple')}${metric('Client requests',requests,'Information still awaiting acceptance','folder','blue')}${metric('Ready to release',engs.filter(e=>releaseBlockers(e).length===0).length,'Only current, cleared packages','shield','amber')}</div>
<div class="grid-main"><div class="stack">${panel('Engagement portfolio','Every client. One connected workflow.',`<div class="tablewrap"><table><thead><tr><th>Client & engagement</th><th>Service</th><th>Status</th><th>Demo gates</th><th>Target date</th><th>Team</th><th></th></tr></thead><tbody>${engagementRows(engs.slice(0,5))}</tbody></table></div><div class="panel-foot"><span>Gate completion is calculated from sample state, not real project progress.</span>${linkBtn('All engagements','engagements')}</div>`,badge('FY 2026','gray'))}
${panel('Your attention, where it matters','Prioritized next actions for the selected engagement.',`<div class="taskrow"><div class="taskcheck purple">${icon('message')}</div><div><h3>${E().reviews.filter(r=>r.status!=='Cleared').length} review points need a response</h3><p>${esc(C().name)} · Fixed assets and disclosures</p></div>${btn('Open review desk','open-reviews','sm','','arrow')}</div><div class="taskrow"><div class="taskcheck">${icon('folder')}</div><div><h3>Complete the client evidence request</h3><p>Asset register and related-party confirmation</p></div>${linkBtn('View requests','documents')}</div><div class="taskrow"><div class="taskcheck green">${icon('checkcircle')}</div><div><h3>Keep the final package aligned</h3><p>Exact-version approvals protect the release path</p></div>${linkBtn('View gates','delivery')}</div>`)}
${panel('Recent activity','Demonstration events stored in this browser.',`<div class="panel-pad" style="padding-top:0">${state.events.slice(0,3).map(ev=>`<div class="activity"><div class="activity-dot">${icon(ev.type)}</div><div><p>${esc(ev.text)}</p><small>${esc(ev.ref)} · ${esc(ev.time)}</small></div></div>`).join('')}</div>`,btn('Full history','activity','ghost sm'))}</div><div class="stack"><div class="focus-card"><div class="focus-icon">${icon('spark','lg')}</div><div class="focus-label">Today’s focus</div><div class="focus-title">Move the review forward.<br>Keep the evidence intact.</div><p>Explore a full correction loop, from an open review note to an approved package.</p>${btn('Continue review','open-reviews','primary sm','','arrow')}</div>
${panel('Where work stands','Illustrative engagement distribution',`<div class="panel-pad" style="padding-top:4px"><div class="stage-bars">${[['Planning',engs.filter(e=>stage(e)==='Planning').length],['Review',engs.filter(e=>/Review|Changes/.test(stage(e))).length],['Accounting',engs.filter(e=>stage(e)==='Accounting').length],['Other',engs.filter(e=>!['Planning','Review','Changes required','Accounting'].includes(stage(e))).length]].map(([s,n])=>`<div class="stage-col"><b>${n}</b><i style="height:${20+n*23}px"></i><span>${s}</span></div>`).join('')}</div></div>`)}
${panel('Upcoming in this demo','Dates are sample targets, not live reminders.',`<div class="panel-pad" style="padding-top:0"><div class="activity"><div class="datebox"><small>SEP</small><b>23</b></div><div><p>Asset register due</p><small>Example Trading Entity</small></div></div><div class="activity"><div class="datebox"><small>SEP</small><b>24</b></div><div><p>Management confirmation</p><small>Example Trading Entity</small></div></div><div class="activity"><div class="datebox"><small>SEP</small><b>28</b></div><div><p>Engagement review target</p><small>Current draft package</small></div></div></div>`)}
<div class="borderbox" style="background:#eef2f2;font-size:11px;color:#798e91">${icon('book')} &nbsp; Based on <b>STE-PRD-001</b><p class="mt8">Explore the 26 lifecycle stages and original requirements alongside the product screens.</p>${linkBtn('Open requirements','requirements')}</div></div></div>`+footer();}
function clients(){const list=state.clients.filter(c=>(c.name+' '+c.industry).toLowerCase().includes(ui.clientFilter.toLowerCase()));return head('Client portfolio','Separate relationships, legal entities and engagement responsibilities.',btn('Add client via acquisition','new-lead','primary','','plus'))+`<div class="toolbar"><div class="input-search">${icon('search')}<input class="input" id="client-filter" value="${esc(ui.clientFilter)}" placeholder="Find a client or industry" aria-label="Filter clients"></div><span class="tag gray">${list.length} sample clients</span></div><div class="grid3">${list.map(c=>{const es=state.engagements.filter(e=>e.client===c.id);return `<section class="client-card"><div class="between">${avatar(c.initials,c.color,'square')}${badge(c.status)}</div><h3>${esc(c.name)}</h3><p class="sub">${esc(c.industry)}</p><div class="row mt12"><span class="caption">${c.id}</span>${badge(c.risk+' risk',c.risk==='Low'?'green':'amber')}</div><div class="clientstats"><div><small>Engagements</small><b>${es.length}</b></div><div><small>Relationship contact</small><span class="small">${esc(c.contact)}</span></div></div><div class="between mt20"><button class="textlink" data-action="client-detail" data-id="${c.id}">Client 360</button>${btn('Open workspace','eng-open','sm',`data-id="${es[0]?.id||''}"`,'arrow')}</div></section>`;}).join('')}</div>${note('These are synthetic legal entities. A relationship may contain several entities; this demo keeps them separate and links work by explicit engagement ID.','', '').replace('class="banner ','class="banner mt24 ')}${footer('DAT-01–04 · LC-07')}`;}
function acquisition(){const cols=['Inquiry','Discovery','Evaluation','Proposal'];return head('From first conversation to trusted client.','Qualify opportunities without confusing commercial interest with professional acceptance.',btn('New inquiry','new-lead','primary','','plus'))+`<div class="metric-grid">${metric('Open opportunities',state.leads.filter(l=>l.stage!=='Converted').length,'Synthetic commercial pipeline','target')}${metric('Illustrative fee pipeline',cash(state.leads.filter(l=>l.stage!=='Converted').reduce((s,l)=>s+l.value,0)),'Proposed fees, not posted revenue','chart','purple')}${metric('Acceptance required',state.leads.filter(l=>!l.accepted).length,'A proposal does not authorize work','shield','amber')}${metric('Converted',state.leads.filter(l=>l.stage==='Converted').length,'Terms and demo acceptance recorded','checkcircle','blue')}</div><div class="kanban">${cols.map((col,i)=>`<section class="kanban-col"><div class="kanban-title"><span>${col}</span><span class="tag gray">${state.leads.filter(l=>l.stage===col).length}</span></div>${state.leads.filter(l=>l.stage===col).map(l=>`<button class="lead-card" data-action="lead-detail" data-id="${l.id}">${badge(l.service,i%2?'purple':'blue')}<h3>${esc(l.name)}</h3><p>${esc(l.contact)} · ${l.id}</p><div class="value">${cash(l.value)}</div><div class="between mt12"><span class="caption">${l.accepted?'Acceptance recorded':'Acceptance pending'}</span>${avatar('LR','purple','sm')}</div></button>`).join('')}<button class="kanban-add" data-action="new-lead">+ Add opportunity</button></section>`).join('')}</div>${state.leads.some(l=>l.stage==='Converted')?`<div class="panel panel-pad mt24"><h3>Converted relationships</h3><p class="sub">${state.leads.filter(l=>l.stage==='Converted').map(l=>esc(l.name)).join(' · ')}</p></div>`:''}${note('Client conversion creates a draft engagement only. Acceptance, effective terms, scope and access must remain distinct before professional work begins.','').replace('class="banner ','class="banner mt24 ')}${footer('LC-01–07')}`;}
function engagements(){const e=E();return head('Engagements','One legal scope, one reporting period, one accountable team.',btn('New engagement','new-eng','primary','','plus'))+context()+`<div class="panel panel-pad mb16"><div class="between"><div><span class="eyebrow">SELECTED ENGAGEMENT · ${e.id}</span><h2 class="mt8">${esc(C().name)}</h2><p class="sub">${esc(e.service)} · ${esc(e.period)} · ${esc(e.mode)}</p></div>${badge(stage(e))}</div>${lifeBar(e)}<div class="info-grid"><div><label>Engagement manager</label><span>${esc(e.manager)}</span></div><div><label>Professional signatory</label><span>${esc(e.partner)}</span></div><div><label>Management signatory</label><span>${esc(C().contact)}</span></div><div><label>Effective terms</label>${badge(e.terms?'Recorded':'Pending')}</div><div><label>Acceptance</label>${badge(e.acceptance?'Accepted':'Pending')}</div><div><label>Quality review</label><span>${e.eqrRequired?'Required for this demo profile':'Not required by this demo profile'}</span></div></div><div class="row mt24 wrap">${linkBtn('Accounting workspace','accounting')}${linkBtn('Audit workpapers','audit')}${linkBtn('Release gates','delivery')}${btn('View service scope','service-for-eng','sm')}</div></div>${panel('All engagements','Gate percentages describe this prototype state, not project implementation.',`<div class="tablewrap"><table><thead><tr><th>Client</th><th>Service</th><th>Stage</th><th>Demo gates</th><th>Target</th><th>Team</th><th></th></tr></thead><tbody>${engagementRows(state.engagements)}</tbody></table></div>`)}${footer('LC-09 · G02–03')}`;}
function lifeBar(e){const steps=['Acceptance','Planning','Production','Review','Release','Archive'];const ix=e.archive?5:e.releases.length?4:!allNotesClear(e)?3:!e.sourceAccepted?2:!e.planning?1:3;return `<div class="lifecyclebar">${steps.map((s,i)=>`<div class="life-step ${i<ix?'done':i===ix?'current':''}"><em>${i<ix?icon('check'):i+1}</em>${s}</div>`).join('')}</div>`;}
const assessmentItems=[['Identity & authority','Legal name, registration, addresses and authorized contacts','Q01–Q10'],['Financial & operational profile','Financial metrics, volumes, currencies and significant balances','Q11–Q19'],['Books, systems & evidence','Accounting mode, available records, framework and data permissions','Q20–Q29'],['Compliance & engagement suitability','Obligations, predecessor, management responsibility and firm capability','Q30–Q40']];
function onboarding(){const e=E(),q=e.questionnaire;return head('Acceptance is a decision, not a checkbox.','A guided preview of intake, supporting evidence and professional clearance.',btn('Read the exact questionnaire','source','', 'data-section="7"','book'))+context()+tabs([['assessment','Assessment'],['questions','Question bank'],['clearances','Clearances & terms']],'onboardTab')+note('This screen demonstrates the intake route. Completing demo declarations is not a KYC verification, screening result or professional acceptance.')+(ui.onboardTab==='assessment'?`<div class="grid-main"><div class="stack">${assessmentItems.map(([title,sub,ids],i)=>panel(title,sub,`<div class="panel-pad" style="padding-top:0"><div class="between"><span class="caption">${ids} · source requirements</span>${badge(q.answers[i]?'Demo information confirmed':'Awaiting information',q.answers[i]?'green':'amber')}</div><label class="checkbox mt16"><input type="checkbox" data-change="assessment-item" data-id="${i}" ${q.answers[i]?'checked':''}>I have reviewed the synthetic information and recorded the required demo evidence.</label><div class="mt12">${btn('Read source questions','source','ghost sm','data-section="7"')}</div></div>`)).join('')}</div><div class="stack"><div class="panel panel-pad"><span class="eyebrow">ASSESSMENT SUMMARY</span><h2 class="mt12">${Object.values(q.answers).filter(Boolean).length} of 4 sections</h2><div class="progress mt12" style="width:100%"><i style="width:${Object.values(q.answers).filter(Boolean).length*25}%"></i></div><p class="sub mt16">Scores and forms may route work. Only an authorized partner can record acceptance.</p><div class="divider"></div>${badge(q.status)}<div class="stack mt16">${btn('Submit demo assessment','assessment-submit','primary')}${btn('Record partner acceptance','assessment-accept','')}</div><p class="role-note">Use the Engagement partner role for the final decision. This is a simulation, not identity verification.</p></div><div class="panel panel-pad"><h3>Always distinguish</h3><p class="sub mt12">Information received → verified evidence → specialist clearance → partner decision → engagement authorization.</p></div></div></div>`:ui.onboardTab==='questions'?`<div class="panel panel-pad"><h2>Source intake and annual-validation questions</h2><p class="sub">Read-only source text. Q01–Q40 and Y01–Y30 are preserved in the document reference.</p>${sourceBlocks(PRD.sections.find(s=>s.id===7).blocks)}</div>`:`<div class="grid2">${panel('Required clearances','Each professional decision remains separate.',`<div class="panel-pad" style="padding-top:0"><div class="controlbox"><span class="statusicon">${icon('check')}</span><div><h4>Independence / service permissibility</h4><p>Synthetic scenario cleared; no real assessment performed.</p></div></div><div class="controlbox mt12"><span class="statusicon">${icon('check')}</span><div><h4>Capacity & competence</h4><p>Illustrative team assignment recorded.</p></div></div><div class="controlbox mt12"><span class="statusicon warn">${icon('info')}</span><div><h4>External screening</h4><p>Not connected. No sanctions or identity check is performed.</p></div></div></div>`)}${panel('Engagement terms','Commercial acceptance is not activation.',`<div class="panel-pad" style="padding-top:0">${badge(e.terms?'Effective terms recorded':'Terms pending')}<p class="sub mt16">Signed scope, entity, period, responsibilities, fees, data terms and signatory authority are required in production.</p><div class="mt20">${btn('Record demo terms','record-terms','soft')}</div></div>`)}</div>`)+footer('LC-04–07 · Q01–40');}
function documents(){const e=E(),items=e.pbc.filter(p=>ui.docFilter==='All'||p.status===ui.docFilter);return head('Evidence, with a clear chain of custody.','Every request has an owner, a period and a separate adequacy decision.',btn('New PBC request','new-pbc','primary','','plus'))+context()+`<div class="metric-grid">${metric('Requested',e.pbc.length,'Client evidence requests','folder')}${metric('Awaiting client',e.pbc.filter(p=>['Requested','Clarification required'].includes(p.status)).length,'Files or clarification outstanding','clock','amber')}${metric('Ready for review',e.pbc.filter(p=>p.status==='Received').length,'Received does not mean accepted','eye','purple')}${metric('Accepted',e.pbc.filter(p=>p.status==='Accepted').length,'Adequate for the stated request','checkcircle','blue')}</div><div class="toolbar"><select class="select" id="doc-filter" aria-label="Filter PBC requests">${['All','Requested','Received','Accepted','Clarification required'].map(s=>`<option ${ui.docFilter===s?'selected':''}>${s}</option>`).join('')}</select><span class="caption">Provider status: SharePoint is not connected</span></div>${panel('PBC request register','Sample files are visual references. Real uploads stay local and retain metadata only.',`<div class="tablewrap"><table><thead><tr><th>Request / document</th><th>Client owner</th><th>Due date</th><th>Status</th><th>Version</th><th class="right">Actions</th></tr></thead><tbody>${items.map(p=>`<tr><td><div class="row"><div class="document-icon ${p.file?.endsWith('.pdf')?'pdf':''}">${p.file?.endsWith('.pdf')?'PDF':'DOC'}</div><div><button class="tablelink" data-action="pbc-detail" data-id="${p.id}"><b>${esc(p.title)}</b></button><div class="cell-sub">${p.id} · ${esc(p.file||'No file received')}</div></div></div></td><td><span class="small">${esc(p.owner)}</span></td><td><span class="small">${shortDate(p.due)}</span></td><td>${badge(p.status)}</td><td class="mono">${p.version?'v'+p.version:'—'}</td><td class="right">${btn(p.file?'View request':'Upload','pbc-detail','sm',`data-id="${p.id}"`,p.file?'eye':'upload')}</td></tr>`).join('')}</tbody></table></div>`)}<div class="grid2 mt24"><div class="panel panel-pad"><h3>Receipt → review → accepted evidence</h3><div class="service-track mt16"><span>Requested</span>${icon('arrow')}<span>Received</span>${icon('arrow')}<span>Technical review</span>${icon('arrow')}<span>Accepted</span></div><p class="sub mt16">Replacement files retain their prior metadata and return to review. Internal review notes never become client-visible just because a query is shared.</p></div><div class="panel panel-pad"><div class="between"><h3>Document integration</h3>${badge('Not connected','gray')}</div><p class="sub mt12">The production architecture can resolve a logical document to an approved provider. This prototype does not upload to SharePoint, launch Office, or apply retention policies.</p>${btn('Read document requirements','source','ghost sm mt12','data-section="9"')}</div></div>${footer('LC-11–12 · API-07')}`;}
function accounting(){const e=E();return head('Accounting, from source to statement.','Preserve the original. Explain the adjustments. Review the exact reporting version.',`${btn('Export TB CSV','export-tb','','','download')}${btn('Import demo CSV','import-tb','primary','','upload')}`)+context()+tabs([['tb','Trial balance'],['mapping','Account mapping'],['adjustments','Adjustments'],['reconciliations','Reconciliations'],['statements','Financial statements']],'accountTab')+(e.builtGeneration!==e.generation?note(`<b>Package v${e.packageRevision} is stale.</b> Inputs changed to generation ${e.generation}. Rebuild, then obtain fresh approvals. Historical sign-offs remain attached to their original version.`,'amber',`<span class="banner-action">${btn('Rebuild package','build-package','sm soft')}</span>`):'')+({tb:tbView,mapping:mappingView,adjustments:adjustmentsView,reconciliations:reconciliationView,statements:statementsView}[ui.accountTab]||tbView)()+footer('LC-13–19 · GOV-01–03');}
function tbView(){const e=E(),t=totals(),raw=e.rows.reduce((s,r)=>s+r.balance,0),debits=e.rows.filter(r=>r.balance>0).reduce((s,r)=>s+r.balance,0);const rs=t.rows.map(r=>{const rawrow=e.rows.find(x=>x.code===r.code);const rawBal=rawrow?.balance||0;return `<tr><td class="mono">${esc(r.code)}</td><td><b>${esc(r.name)}</b><div class="cell-sub">${esc(r.type)} · ${e.id}</div></td><td class="right num">${moneyView(rawBal)}</td><td class="right num ${r.balance-rawBal?'teal':''}">${r.balance-rawBal?moneyView(r.balance-rawBal):'—'}</td><td class="right num bold">${moneyView(r.balance)}</td><td>${badge(e.mappingApproved?'Mapped':'Review required',e.mappingApproved?'green':'amber')}</td></tr>`;}).join('');return `<div class="grid3 mb16">${metric('Source debits',cash(debits),`Source version ${e.sourceVersion} · debit-positive convention`,'file')}${metric('Trial balance residual',cash(raw),raw===0?'Debits and credits agree':'Source requires correction','checkcircle',raw===0?'':'amber')}${metric('Adjusted profit',cash(t.profit),'Reporting adjustments applied once','trend','purple')}</div>${panel('Source and adjusted trial balance','QAR · Debit balances positive / credit balances in parentheses',`<div class="tablewrap"><table><thead><tr><th>Account</th><th>Description</th><th class="right">Source</th><th class="right">Adjustments</th><th class="right">Adjusted</th><th>Mapping</th></tr></thead><tbody>${rs}<tr class="table-summary"><td colspan="2">Signed-balance control</td><td class="right">${moneyView(raw)}</td><td class="right">${moneyView(t.balance-raw)}</td><td class="right">${moneyView(t.balance)}</td><td>${badge(t.balance===0?'Balanced':'Unbalanced',t.balance===0?'green':'red')}</td></tr></tbody></table></div><div class="panel-foot"><span>Received source → approved mapping → adjusted snapshot → report</span><span class="mono">TB-v${e.sourceVersion} / generation ${e.generation}</span></div>`,btn('Source fixture','source','ghost sm','data-section="25"'))}<div class="grid2 mt20"><div class="panel panel-pad"><h3>Try a controlled source replacement</h3><p class="sub mt8">Create a replacement TB already containing the depreciation entry. The reporting layer must not apply it a second time.</p><div class="mt16">${btn('Replace TB with posted adjustment','replace-tb','soft','','refresh')}</div></div><div class="panel panel-pad"><h3>Demonstrate approval invalidation</h3><p class="sub mt8">Add a synthetic QAR 1,000 credit sale: receivables and revenue change. Existing package approvals become stale.</p><div class="mt16">${btn('Simulate source change','source-change','','','layers')}</div></div></div>${note('The initial 8-account fixture and QAR 500 adjustment come from PRD §25. Other clients and supplemental schedules are illustrative. This is not a production ledger.','').replace('class="banner ','class="banner mt20 ')}`;}
function mappingView(){const e=E();return note('Mappings classify source accounts. Changing an account classification creates a new input generation and requires a fresh reviewer decision.')+panel('Account-to-statement mapping','Select the destination classification, then switch to Senior reviewer to approve.',`<div class="tablewrap"><table><thead><tr><th>Source code</th><th>Source account</th><th>Statement classification</th><th>Review</th></tr></thead><tbody>${e.rows.map(r=>`<tr><td class="mono">${esc(r.code)}</td><td>${esc(r.name)}</td><td><select class="select" data-change="mapping" data-code="${esc(r.code)}" aria-label="Classification for ${esc(r.name)}">${['asset','liability','equity','revenue','expense'].map(t=>`<option value="${t}" ${r.type===t?'selected':''}>${{asset:'Assets → Financial position',liability:'Liabilities → Financial position',equity:'Equity → Equity statement',revenue:'Revenue → Profit or loss',expense:'Expenses → Profit or loss'}[t]}</option>`).join('')}</select></td><td>${badge(e.mappingApproved?'Approved':'Review required')}</td></tr>`).join('')}</tbody></table></div><div class="panel-foot"><span>Prototype supports one classification per account; split allocations are a production requirement.</span>${btn('Approve mapping','approve-mapping','primary sm')}</div>`) + `<div class="panel panel-pad mt20"><h3>Reporting traceability</h3><div class="lineage mt16"><span>Source account</span>${icon('arrow')}<span>Entity account</span>${icon('arrow')}<span>Approved mapping</span>${icon('arrow')}<span>Statement line</span>${icon('arrow')}<span>Supporting note</span></div></div>`;}
function adjustmentsView(){const e=E();return `<div class="grid-main"><div class="stack">${panel('AJ-001 · Depreciation adjustment','Based on the worked example in STE-PRD-001 §25.3.',`<div class="panel-pad" style="padding-top:0"><div class="between">${badge(e.journalState)}${badge(e.sourceReflection?'Reflected in source':'Reporting layer',e.sourceReflection?'blue':'purple')}</div><div class="tablewrap mt20"><table><thead><tr><th>Account</th><th class="right">Debit</th><th class="right">Credit</th></tr></thead><tbody><tr><td>Depreciation expense</td><td class="right">${fmt(e.adjustment)}</td><td class="right">—</td></tr><tr><td>Accumulated depreciation</td><td class="right">—</td><td class="right">${fmt(e.adjustment)}</td></tr></tbody></table></div><div class="info-grid mt20"><div><label>Prepared by</label><span>Adam Khan</span></div><div><label>Technical review</label><span>Sara Malik · sample record</span></div><div><label>Management</label><span>${e.journalState==='Applied'?'Demo authorization recorded':'Authorization pending'}</span></div></div><div class="divider"></div><p class="small muted">${e.sourceReflection?'The replacement trial balance already includes this entry. Additional reporting-layer effect is QAR 0.00.':'The approved reporting adjustment is included once. The client’s external ledger is not changed by this application.'}</p><div class="row mt16 wrap">${btn('Revise proposed amount','revise-adjustment','','','file')}${e.journalState!=='Applied'?btn('Management authorize & apply demo','apply-adjustment','primary'):''}</div></div>`)}${panel('Correction lineage','Every change keeps a source and decision trail.',`<div class="panel-pad" style="padding-top:0"><div class="lineage"><span>WP-C1</span>${icon('arrow')}<span>AJ-001</span>${icon('arrow')}<span>Management decision</span>${icon('arrow')}<span>TB-v${e.sourceVersion}</span>${icon('arrow')}<span>Package-v${e.packageRevision}</span></div></div>`)}</div><div class="stack"><div class="panel panel-pad"><h3>Four different outcomes</h3><div class="status-line mt16">${icon('check')}Technical treatment reviewed</div><div class="status-line">${icon('check')}Management authorization</div><div class="status-line">${icon('layers')}Included in reporting</div><div class="status-line">${icon('globe')}Externally posted: separate evidence</div><p class="sub mt16">A downloaded journal file is not evidence of external posting. A rejected entry remains in the differences register.</p></div>${note('All journal actions are local simulations. No client or firm ledger receives a posting.','amber')}</div></div>`;}
function reconciliationView(){const e=E();return panel('Source-bound reconciliations','Opening balances, supporting schedules and explained differences.',`<div class="tablewrap"><table><thead><tr><th>Area</th><th>Reference</th><th>Supporting basis</th><th>Status</th><th></th></tr></thead><tbody>${e.reconciliations.map((r,i)=>`<tr><td><b>${r.name}</b></td><td class="mono">${r.ref}</td><td class="small muted">${r.evidence}</td><td>${badge(r.status)}</td><td>${btn('Open schedule','reconciliation','sm',`data-id="${i}"`)}</td></tr>`).join('')}</tbody></table></div>`)+`<div class="panel panel-pad mt20"><h3>Unexplained differences cannot be hidden</h3><p class="sub mt8">The production workflow classifies timing, error, omission, estimates and disputed items. This prototype displays sample reconciliation outcomes; it does not perform bank matching.</p><div class="mt12">${btn('Read detailed reconciliation requirements','source','ghost sm','data-section="10"')}</div></div>`;}
function statementPaper(e=E(),release=null){const t=release?.totals||totals(e),rev=release?.revision||e.packageRevision,c=C(e.client);return `<article class="paper"><div class="watermark">${release?'DEMO ISSUED ARTIFACT — NOT AN AUDITOR’S REPORT':'ILLUSTRATIVE DRAFT — NOT FOR PROFESSIONAL USE'}</div><div class="letterhead"><div><h2>${esc(c.name)}</h2><p>Illustrative financial statements · FY ${e.year}</p></div><div class="right"><strong>QAR</strong><div class="caption">Package v${rev}</div><div class="caption">STE-PRD-001 demonstration</div></div></div><h3>Statement of financial position</h3><table><thead><tr><th>Line item</th><th class="right">Amount</th></tr></thead><tbody>${t.rows.filter(r=>r.type==='asset').map(r=>`<tr><td>${esc(r.name)}</td><td class="right num">${moneyView(r.balance)}</td></tr>`).join('')}<tr class="statement-total"><td>Total assets</td><td class="right">${fmt(t.assets)}</td></tr>${t.rows.filter(r=>r.type==='liability').map(r=>`<tr><td>${esc(r.name)}</td><td class="right">${moneyView(-r.balance)}</td></tr>`).join('')}<tr><td>Total liabilities</td><td class="right">${fmt(t.liabilities)}</td></tr><tr><td>Opening equity</td><td class="right">${fmt(t.openingEquity)}</td></tr><tr><td>Current-period profit / (loss)</td><td class="right">${moneyView(t.profit)}</td></tr><tr class="statement-total"><td>Total liabilities and equity</td><td class="right">${fmt(t.liabilities+t.openingEquity+t.profit)}</td></tr></tbody></table><h3>Statement of profit or loss</h3><table><tbody>${t.rows.filter(r=>['revenue','expense'].includes(r.type)).map(r=>`<tr><td>${esc(r.name)}</td><td class="right">${moneyView(r.type==='revenue'?-r.balance:r.balance)}</td></tr>`).join('')}<tr class="statement-total"><td>Profit / (loss) for the period</td><td class="right">${moneyView(t.profit)}</td></tr></tbody></table><h3>Equity movement</h3><p>Opening equity ${cash(t.openingEquity)} + current result ${cash(t.profit)} = closing equity ${cash(t.openingEquity+t.profit)}. This simplified illustration assumes no distributions or other comprehensive income.</p><h3>Cash-flow and disclosure support</h3>${e.supplements?`<p><b>Illustrative supporting schedule, not derived from closing TB alone.</b> The original example assumes opening cash QAR 8,000 and a QAR 1,000 increase in receivables: operating cash QAR 2,000, no investing or financing movements, closing cash QAR 10,000. These demo assumptions apply only to the original Example Trading fixture; source changes require new supporting schedules.</p><p>Disclosure checklist: accounting basis, asset policy, related parties, going concern and subsequent events require explicit applicability and review. This preview is not a complete framework-compliant statement set.</p>`:`<p><strong>Supporting schedules require review.</strong> Opening balances, cash movements, noncash changes and disclosure information must be supplied. A closing trial balance is insufficient.</p>`}<div class="divider"></div><div class="caption">Source: ${e.id} · TB-v${e.sourceVersion} · input generation ${release?.generation||e.generation}<br>No real signature, audit opinion, management attestation or filing is created by this prototype.</div></article>`;}
function statementsView(){const e=E(),t=totals();return `<div class="toolbar">${btn('Rebuild current package','build-package','primary','','refresh')}${btn('Export preview HTML','export-report','','','download')}${btn('Print preview','print-report','','','file')}${linkBtn('Approval desk','reviews')}</div><div class="grid3 mb16">${metric('Total assets',cash(t.assets),'Statement of financial position','chart')}${metric('Closing equity',cash(t.openingEquity+t.profit),'Opening equity + current-period result','layers','purple')}${metric('Statement equation',t.assets===t.liabilities+t.openingEquity+t.profit?'Balanced':'Review required','Assets = liabilities + equity','checkcircle','blue')}</div>${!e.supplements?note('After a source change, the illustrative cash-flow/disclosure support must be reconfirmed. This demo confirmation does not create real evidence.','amber',`<span class="banner-action">${btn('Review demo supplements','supplements','sm')}</span>`):''}${statementPaper()}<div class="row mt20 wrap">${btn('Review demo supplements','supplements','soft')}${btn('Read statement requirements','source','ghost','data-section="10"')}</div>`;}
function audit(){const e=E();return head('Evidence that supports a conclusion.','Plan, test and review without losing the source version.',btn('Planning & materiality','planning','primary','','target'))+context()+tabs([['workpapers','Working papers'],['risks','Risks & assertions'],['sampling','Population & sampling'],['memoranda','Memoranda']],'auditTab')+(ui.auditTab==='workpapers'?`<div class="metric-grid">${metric('Working papers',e.workpapers.length,'In this illustrative engagement','checkboard')}${metric('Cleared',e.workpapers.filter(w=>w.status==='Cleared').length,'Independent sample review recorded','checkcircle','blue')}${metric('Changes required',e.workpapers.filter(w=>w.status==='Changes required').length,'Responses and evidence outstanding','message','amber')}${metric('Input generation',e.generation,'Source changes invalidate dependent work','layers','purple')}</div>${panel('Workpaper index','A completed task is not an evidence-based sign-off.',`<div class="tablewrap"><table><thead><tr><th>Reference / area</th><th>Assertion focus</th><th>Version</th><th>Status</th><th>Reviewer</th><th></th></tr></thead><tbody>${e.workpapers.map(w=>`<tr><td><b>${esc(w.title)}</b><div class="cell-sub">${w.id}</div></td><td class="small">${esc(w.assertion)}</td><td class="mono">v${w.version}</td><td>${badge(w.status)}</td><td><div class="row gap8">${avatar('SM','purple','sm')}<span class="small">Sara Malik</span></div></td><td>${btn('Open','workpaper','sm',`data-id="${w.id}"`,'arrow')}</td></tr>`).join('')}</tbody></table></div>`)}<div class="panel panel-pad mt20"><h3>Evidence chain</h3><div class="lineage mt16">${['Risk','Assertion','Procedure','Population','Evidence','Exception','Conclusion','Review'].map((t,i)=>`<span>${t}</span>${i<7?icon('arrow'):''}`).join('')}</div></div>`:ui.auditTab==='risks'?`<div class="grid2">${e.workpapers.map(w=>`<section class="panel panel-pad"><div class="between"><span class="eyebrow">${w.id}</span>${badge('Engagement-specific','purple')}</div><h3 class="mt12">${esc(w.risk)}</h3><p class="sub mt8">${esc(w.assertion)}</p><p class="small mt16">${esc(w.objective)}</p><div class="mt16">${btn('View linked procedure','workpaper','soft sm',`data-id="${w.id}"`,'link')}</div></section>`).join('')}</div>`:ui.auditTab==='sampling'?samplingView():memorandaView())+footer('AUD-01–05 · LC-10');}
function samplingView(){return note('This is a sampling-workflow illustration, not a statistical sampling engine. No confidence level or assurance is implied by selecting demonstration rows.','amber')+`<div class="grid-main"><div class="panel panel-pad"><h2>Receivables population</h2><p class="sub">A miniature synthetic population for exploring the evidence workflow.</p><div class="tablewrap mt20"><table><thead><tr><th>Source ID</th><th>Counterparty</th><th class="right">QAR</th><th>Selection</th></tr></thead><tbody>${[['AR-01','Demo Customer A',250000],['AR-02','Demo Customer B',175000],['AR-03','Demo Customer C',75000]].map(([id,name,value],i)=>`<tr><td class="mono">${id}</td><td>${name}</td><td class="right">${fmt(value)}</td><td>${badge(i===0?'Specific item':'Unselected',i===0?'purple':'gray')}</td></tr>`).join('')}</tbody></table></div><p class="sub mt20">This example population totals QAR 5,000 for the original Example Trading fixture. Reconcile the actual source and define the testing objective before real selections.</p></div><div class="panel panel-pad"><h3>Selection plan</h3><div class="info-grid mt20" style="grid-template-columns:1fr"><div><label>Method</label><span>Specific-item selection · demonstration</span></div><div><label>Objective</label><span>Inspect support for one high-value receivable</span></div><div><label>Evaluation</label><span>No projection to the untested remainder</span></div></div>${btn('Record illustrative test','sample-result','primary mt24')}</div></div>`;}
function memorandaView(){return `<div class="grid2">${[['Strategy memorandum','Scope, entity understanding, significant areas, materiality, procedures, team and timing.','planning'],['Summary memorandum','Work performed, findings, estimates, differences, going concern, subsequent events and reporting conclusion.','summary-memo'],['Audit announcement','Approved scope, intended dates, support required and contact responsibilities.','announcement'],['Report / service conclusion','Version-bound report, supporting conclusion and the authorized final package.','report-memo']].map(([title,sub,action])=>`<div class="panel panel-pad"><span class="metric-icon">${icon('file')}</span><h3 class="mt16">${title}</h3><p class="sub mt8">${sub}</p><div class="mt16">${btn('Open illustrative document',action,'soft sm')}</div></div>`).join('')}</div>`;}
function reviews(){const e=E();const r=e.reviews.find(r=>r.id===ui.selectedReview)||e.reviews[0];return head('Review the work. Preserve the decision.','Responses, independent clearance and exact-version sign-off remain separate.',btn('Demonstrate a source change','source-change','','','refresh'))+context()+tabs([['notes','Review points'],['approvals','Package approvals'],['history','Decision history']],'reviewTab')+(ui.reviewTab==='notes'?(r?`<div class="review-split"><div class="panel"><div class="panel-head"><h2>Review queue</h2><span class="tag gray">${e.reviews.filter(n=>n.status!=='Cleared').length} open</span></div>${e.reviews.map(n=>`<button class="review-selector ${n.id===r.id?'active':''}" data-action="select-review" data-id="${n.id}"><div class="between"><span class="mono">${n.id}</span>${badge(n.severity,n.severity==='High'?'red':'amber')}</div><h3>${esc(n.title)}</h3><p>${n.wp} · due ${shortDate(n.due)}</p><div class="mt12">${badge(n.status)}</div></button>`).join('')}<div class="panel-pad">${btn('Add review point','add-note','soft sm','','plus')}</div></div><div class="panel"><div class="panel-head"><div><span class="eyebrow">${r.id} · ${r.wp}</span><h2 class="mt8">${esc(r.title)}</h2></div>${badge(r.status)}</div><div class="panel-pad" style="padding-top:0"><div class="row"><span class="caption">Raised by ${esc(r.raisedBy)}</span><span class="caption">Assigned to ${esc(r.assigned)}</span></div><div class="comment"><div class="row gap8">${avatar('SM','purple','sm')}<b class="small">Reviewer’s request</b></div><p>${esc(r.body)}</p></div><div class="lineage"><span>${r.wp}-v${r.version}</span>${icon('link')}<span>TB-v${e.sourceVersion}</span>${icon('link')}<span>Package-v${e.packageRevision}</span></div>${r.response?`<div class="comment" style="background:#f1f8f4;border-color:#dbe9e0"><div class="row gap8">${avatar('AK','','sm')}<b class="small">Response from ${esc(r.respondedBy||'Adam Khan')}</b></div><p>${esc(r.response)}</p><p class="caption">A response is not clearance. The reviewer must assess it.</p></div>`:''}<div class="field mt24"><label for="review-response">Response and supporting evidence explanation</label><textarea class="input" id="review-response" placeholder="Describe the correction, evidence supplied, and revised conclusion…">${esc(r.status==='Open'?'':r.response)}</textarea></div><div class="row mt16 wrap">${btn('Submit response & revision','respond-note','primary',`data-id="${r.id}"`,'send')}${btn('Clear as reviewer','clear-note','',`data-id="${r.id}"`,'check')}${r.status==='Cleared'?btn('Reopen','reopen-note','danger',`data-id="${r.id}"`):''}</div><div class="role-note">Simulated role: <b>${actor().label}</b>. Use Preparer to respond and Senior reviewer to clear. Browser role switching is not real authentication.</div>${r.status==='Cleared'?note('This review point is cleared for the recorded workpaper revision. A later relevant change reopens review and invalidates dependent package approvals.'):''}</div></div></div>`:note('There are no review points in this selected engagement. Workpapers and package gates still require their own review.')):ui.reviewTab==='approvals'?approvalView():historyView())+footer('LC-20–23 · GOV-01–04');}
function approvalView(){const e=E();return note(`Approval decisions bind to package <b>v${e.packageRevision}</b> and input generation <b>${e.generation}</b>. This prototype simulates the decisions; it creates no real signatures.`)+(e.builtGeneration!==e.generation?note('Rebuild the package before signing off.','amber',`<span class="banner-action">${btn('Rebuild','build-package','sm')}</span>`):'')+`<div class="panel panel-pad mb16"><div class="between"><div><h3>Readiness before package approval</h3><p class="sub">Review notes and working papers must be cleared; supporting schedules must be available.</p></div>${btn('Preview exact draft','preview-report','soft sm','','eye')}</div><div class="row wrap mt16">${badge(allNotesClear()?'Review notes cleared':'Review notes open')}${badge(allWorkClear()?'Workpapers cleared':'Workpapers require review')}${badge(e.supplements?'Supplements available':'Supplements pending')}</div></div><div class="approvals">${[['manager','Manager review','Internal consistency and significant judgments.'],['client','Management approval','The client approves its accounts, not the audit opinion.'],['partner','Partner authorization','Professional conclusion and exact package release.'],['eqr','Quality review','Separate eligibility and completion where required.']].map(([key,title,desc])=>{const a=e.approvals[key],current=isCurrent(key);return `<section class="approval-card ${current?'approved':''}"><span class="statusicon ${current?'':'pending'}">${icon(current?'check':'user')}</span><h4>${title}</h4>${badge(key==='eqr'&&!e.eqrRequired?'Not required':current?'Current approval':a?'Stale approval':'Pending',key==='eqr'&&!e.eqrRequired?'gray':current?'green':a?'red':'amber')}<p>${desc}</p>${a?`<p>${esc(a.actor)}<br>v${a.packageRevision} · generation ${a.generation}</p>`:''}${btn('Record demo approval','package-approve','sm '+(current?'':'primary'),`data-key="${key}" ${key==='eqr'&&!e.eqrRequired?'disabled':''}`)}</section>`;}).join('')}</div><div class="panel panel-pad mt20"><div class="between"><div><h3>Next: prepare the release candidate</h3><p class="sub">Issue only the selected, current, fully approved package.</p></div>${linkBtn('Delivery & archive','delivery')}</div></div>`;}
function historyView(){const e=E();return panel('Decision and change history','Local demo events; not a tamper-proof or production audit log.',`<div class="panel-pad" style="padding-top:0">${e.events.length?e.events.map(ev=>`<div class="activity"><div class="activity-dot">${icon(ev.type)}</div><div><p>${esc(ev.text)}</p><small>${esc(ev.time)} · ${esc(ev.ref)}</small></div></div>`).join(''):'<div class="empty">No new demo decisions yet. Respond to a review point or record an approval.</div>'}</div>`);}
function delivery(){const e=E(),blockers=releaseBlockers();const gates=[['G01','Acceptance',e.acceptance],['G02','Activation / terms',e.terms],['G03','Planning',e.planning],['G04','Financial intake',e.sourceAccepted&&e.mappingApproved&&totals().balance===0],['G05','Production complete',allWorkClear()&&e.supplements&&e.builtGeneration===e.generation],['G06','Senior clearance',allNotesClear()],['G07','Manager completion',isCurrent('manager')],['G08','Management approval',isCurrent('client')],['G09','Professional release',isCurrent('partner')&&(!e.eqrRequired||isCurrent('eqr'))],['G10','Delivery recorded',e.releases.some(r=>r.delivered)],['G11','Archive assembled',!!e.archive],['G12','Renewal / offboarding',!!e.annual.nextId]];return head('Only the right version leaves the practice.','A frozen package, explicit authority and a traceable delivery record.',btn('Preview current package','preview-report','','','eye'))+context()+note('All issue, delivery, signing and archive actions below are simulations. No report is sent, legally signed, filed or protected in Microsoft Purview.','amber')+`<div class="grid-main"><div class="stack">${panel('Release readiness','Gates are evaluated from the current demo state.',`<div class="panel-pad" style="padding-top:0"><div class="gate-grid">${gates.map(([id,title,ok])=>`<div class="controlbox"><span class="statusicon ${ok?'':'pending'}">${icon(ok?'check':'clock')}</span><div><span class="eyebrow">${id}</span><h4>${title}</h4></div></div>`).join('')}</div></div>`)}${panel('Issued sample packages','Issued snapshots remain unchanged if you edit the working source.',`<div class="panel-pad" style="padding-top:0">${e.releases.length?e.releases.map((r,i)=>`<div class="borderbox ${i?'mt12':''}"><div class="between"><div><h3>${r.id} · package v${r.revision}</h3><p class="sub">${esc(r.issuedAt)} · ${esc(r.author)}</p></div>${badge(r.delivered?'Demo delivered':'Demo issued')}</div><div class="hash mt12">SHA-256: ${r.hash}</div><div class="row mt12 wrap">${btn('View preserved preview','released-preview','sm',`data-id="${r.id}"`,'eye')}${btn('Export manifest JSON','release-export','sm',`data-id="${r.id}"`,'download')}${!r.delivered?btn('Record demo delivery','demo-deliver','soft sm',`data-id="${r.id}"`):''}</div></div>`).join(''):'<div class="empty">No issued packages. Complete the required gates first.</div>'}</div>`)}</div><div class="stack"><div class="focus-card"><div class="focus-label">CURRENT CANDIDATE</div><h2 class="mt12">${blockers.length?blockers.length+' release blockers':'Ready for a demo release'}</h2><p>${blockers.length?'Required reviews and source dependencies must be current.':'The selected version has satisfied the simulated prerequisites.'}</p>${blockers.length?`<div class="stack gap8 small" style="color:#617869">${blockers.map(b=>`<div class="row gap8">${icon('clock')}<span>${esc(b)}</span></div>`).join('')}</div><div class="mt16">${btn('Go to approval desk','open-approvals','primary sm','','arrow')}</div>`:`<div class="stack">${btn('Prepare demo candidate','prepare-release','primary')}${e.candidate?btn('Issue demo package','issue-release','soft'):''}</div>`}</div>${e.candidate?`<div class="panel panel-pad"><h3>Frozen candidate</h3><p class="sub">Package v${e.candidate.revision} · generation ${e.candidate.generation}</p><div class="hash mt12">${e.candidate.hash}</div><p class="sub mt12">The hash identifies local manifest bytes. It is not a digital signature.</p></div>`:''}<div class="panel panel-pad"><h3>Archive & retention</h3><p class="sub mt12">${e.archive?'The demonstration manifest was assembled. Storage immutability and legal retention remain unimplemented.':'After demo delivery, a records custodian can assemble the local archive manifest.'}</p><div class="mt16">${btn('Assemble demo archive','archive','soft sm','','archive')}</div><div class="mt12">${badge(e.archive?'Simulation complete':'Not assembled',e.archive?'green':'gray')}</div><div class="divider"></div>${btn('Read offboarding requirements','source','ghost sm','data-section="13"')}</div></div></div>${footer('G01–12 · LC-24–26')}`;}
function portal(){const e=E(),c=C();return head('Client portal preview','A separate, simplified view of requests, responsibilities and released deliverables.',btn('Return to staff workspace','open-overview','','','left'))+context()+note('You are previewing the client experience. This browser demo has no real authentication or access isolation; all available records are synthetic.')+`<div class="portal-hero"><span class="eyebrow" style="color:#aad6bf">${esc(c.name)} · FY ${e.year}</span><h2 class="mt12">Good morning, ${esc(c.contact.split(' ')[0])}.</h2><p>Your engagement team has ${e.pbc.filter(p=>p.status!=='Accepted').length} open document requests. Everything you need is in one place.</p></div><div class="grid-main"><div class="stack">${panel('Your document requests','Supply the requested period and entity; your team reviews adequacy separately.',`<div class="panel-list">${e.pbc.map(p=>`<div class="list-item"><div class="row"><div class="document-icon">DOC</div><div><div class="list-item-title">${esc(p.title)}</div><div class="list-item-sub">Due ${shortDate(p.due)} · ${p.id}</div></div></div><div class="row gap8">${badge(p.status)}${btn(p.file?'View':'Upload','pbc-detail','sm',`data-id="${p.id}"`,p.file?'eye':'upload')}</div></div>`).join('')}</div>`)}${panel('Your deliverables','Only expressly published sample packages appear here.',`<div class="panel-pad" style="padding-top:0">${e.releases.filter(r=>r.delivered).length?e.releases.filter(r=>r.delivered).map(r=>`<div class="list-item"><div><b>${r.id}</b><p class="sub">FY ${e.year} · package v${r.revision} · demo only</p></div>${btn('View package','released-preview','sm',`data-id="${r.id}"`)}</div>`).join(''):'<div class="empty">Your final deliverables will appear after authorized release and delivery.</div>'}</div>`)}</div><div class="stack"><div class="panel panel-pad"><span class="eyebrow">MANAGEMENT REVIEW</span><h2 class="mt12">Financial statements v${e.packageRevision}</h2><p class="sub mt12">Review the exact draft. Management accepts responsibility for its accounts—not the auditor’s independent conclusion.</p><div class="stack mt20">${btn('Review draft statements','preview-report','soft','','eye')}${btn('Record management approval','portal-approve','primary')}</div><div class="mt16">${badge(isCurrent('client')?'Approval current':e.approvals.client?'Approval stale':'Awaiting management')}</div></div><div class="panel panel-pad"><h3>Your engagement team</h3><div class="row mt16">${avatar('LR','purple')}<div><b class="small">Layla Rahman</b><div class="caption">Engagement manager</div></div></div><p class="sub mt16">Demo contact only. Messages are recorded locally; no email is sent.</p><div class="mt16">${btn('Ask a question','client-query','sm','','message')}</div></div></div></div>${footer('LC-07 · LC-11–12 · LC-22')}`;}
function billing(){const list=state.invoices;return head('Practice billing, separate from client books.','Manage professional fees and time without changing client financial statements.',btn('Log demo time','log-time','primary','','plus'))+note('All invoices are sample firm records. Receipts are local demonstrations, not payment requests, transactions or payment-gateway confirmations.')+`<div class="metric-grid">${metric('Invoiced',cash(list.filter(i=>i.status!=='Draft').reduce((s,i)=>s+i.amount,0)),'Issued sample fee invoices','receipt')}${metric('Collected',cash(list.reduce((s,i)=>s+i.paid,0)),'Recorded demo receipts only','checkcircle','blue')}${metric('Outstanding',cash(list.filter(i=>i.status!=='Draft').reduce((s,i)=>s+i.amount-i.paid,0)),'Firm receivables, not client revenue','clock','amber')}${metric('Logged time',state.times.reduce((s,t)=>s+t.hours,0).toFixed(1)+' h','Illustrative practice time entries','brief','purple')}</div>${panel('Fee invoice register','Office ledger scope · client books remain unchanged',`<div class="tablewrap"><table><thead><tr><th>Invoice / client</th><th>Service milestone</th><th class="right">Amount</th><th class="right">Outstanding</th><th>Status</th><th></th></tr></thead><tbody>${list.map(i=>`<tr><td><b>${i.id}</b><div class="cell-sub">${esc(C(i.client)?.name||'Client')}</div></td><td class="small">${esc(i.description)}</td><td class="right">${fmt(i.amount)}</td><td class="right">${fmt(i.amount-i.paid)}</td><td>${badge(i.status,i.status==='Paid'?'green':i.status==='Issued'?'blue':'amber')}</td><td>${btn('Open','invoice','sm',`data-id="${i.id}"`,'arrow')}</td></tr>`).join('')}</tbody></table></div>`)}${panel('Time entries','New demo entries are stored locally.',`<div class="tablewrap"><table><thead><tr><th>Person</th><th>Task</th><th>Engagement</th><th class="right">Hours</th></tr></thead><tbody>${state.times.map(t=>`<tr><td>${esc(t.name)}</td><td>${esc(t.task)}</td><td class="mono">${esc(t.eng)}</td><td class="right">${t.hours.toFixed(1)}</td></tr>`).join('')}</tbody></table></div>`).replace('class="panel"','class="panel mt20"')}${footer('BIL-01–04')}`;}
const annualGroups=[['Y01–Y05','Legal identity & activities','Confirm legal name, registration, addresses, year-end and activities.'],['Y06–Y10','Ownership & relationships','Confirm owners, signatories, group structure, countries and related parties.'],['Y11–Y15','Financial profile','Confirm actual/forecast metrics, financing and going-concern facts.'],['Y16–Y20','Books, systems & records','Confirm framework, currency, interfaces, reconciliations, assets and payroll.'],['Y21–Y25','Obligations & findings','Confirm filing obligations, investigations, controls, findings and events.'],['Y26–Y30','Services, access & attestation','Confirm services, deadlines, responsibilities, access and authorized attestation.']];
function continuance(){const e=E();return head('A fresh year starts with fresh decisions.','Carry forward useful context—not last year’s approvals or conclusions.',btn('Read annual questionnaire','source','','data-section="7"','book'))+context()+`<div class="grid-main"><div class="stack">${panel('Prior period → next period','Grouped exactly as Y01–Y30 in the requirements; confirmations are illustrative.',`<div class="panel-pad" style="padding-top:0">${annualGroups.map(([id,title,desc],i)=>`<div class="question-row"><div><span class="eyebrow">${id}</span><h3 class="mt8">${title}</h3><small>${desc}</small></div><label class="checkbox"><input type="checkbox" data-change="annual" data-id="${i}" ${e.annual.confirmed.includes(i)?'checked':''}><span class="small">Reviewed</span></label></div>`).join('')}</div>`)}${panel('Example change report','A demonstration of field-level comparison, not an actual client declaration.',`<div class="tablewrap"><table><thead><tr><th>Field</th><th>Prior period</th><th>New period</th><th>Action</th></tr></thead><tbody><tr><td>Financial year-end</td><td>31 December</td><td>31 December</td><td>${badge('Reconfirm','gray')}</td></tr><tr class="changed"><td>Ownership</td><td>Existing owner</td><td>New shareholder declared</td><td>${badge('Evidence required','amber')}</td></tr><tr class="changed"><td>Accounting system</td><td>Prior source system</td><td>New export layout</td><td>${badge('Import review','purple')}</td></tr></tbody></table></div>`)}</div><div class="stack"><div class="panel panel-pad"><span class="eyebrow">CONTINUANCE DECISION</span><h2 class="mt12">${e.year} → ${e.year+1}</h2><p class="sub mt12">${e.annual.confirmed.length} of 6 grouped sections reviewed. The partner’s decision remains independent of a “no change” answer.</p><div class="mt16">${badge(e.annual.decision||'Decision pending')}</div><div class="stack mt20">${btn('Record demo continuance','continue-client','primary')}${btn('Create next-year draft','roll-forward','soft')}</div><p class="role-note">A draft shell contains only reference structure. It does not copy current-year balances, samples, evidence or approvals.</p>${e.annual.nextId?`<p class="sub mt12">New draft: ${esc(e.annual.nextId)}</p>`:''}</div><div class="panel panel-pad"><h3>Exit paths remain controlled</h3><p class="sub mt12">Suspension, termination, handover and disposition have separate responsibilities. Termination does not authorize evidence deletion.</p><div class="mt12">${btn('View offboarding path','offboarding','sm','','archive')}</div></div></div></div>${footer('LC-08 · LC-25–26')}`;}
function services(){const services=PRD.services.filter((s,i)=>i<12&&s.title.toLowerCase().includes(ui.serviceFilter.toLowerCase()));const icons=['shield','target','chart','book','search','trend','checkboard','brief','layers','folder','settings','users'];return head('One practice. Different professional services.','Service-specific inputs, procedures, deliverables and approval routes.',btn('Read catalogue requirements','source','','data-section="17"','book'))+note('These cards visualize the service requirements, not delivered calculation engines or approved methodologies. Each service needs its own production workflow and acceptance tests.')+`<div class="toolbar"><div class="input-search">${icon('search')}<input class="input" id="service-filter" value="${esc(ui.serviceFilter)}" placeholder="Search service catalogue" aria-label="Filter services"></div><span class="tag gray">12 source service families</span></div><div class="grid3">${services.map(s=>{const i=PRD.services.indexOf(s);const title=s.title.replace(/^17\.\d+\s*/,''),out=s.blocks.find(b=>b.text?.startsWith('Output and approval'))?.text.replace('Output and approval: ','')||'';return `<section class="service-card"><div class="between" style="width:100%"><div class="icon-tile">${icon(icons[i])}</div><span class="eyebrow">17.${i+1}</span></div><h3>${esc(title)}</h3><p>${esc(out.length>150?out.slice(0,147)+'…':out)}</p>${btn('Explore required workflow','service-detail','ghost sm',`data-id="${i}"`,'arrow')}</section>`;}).join('')}</div><div class="panel panel-pad mt24"><h3>Additional practice templates</h3><div class="row wrap mt16">${['Recurring bookkeeping','Annual accounts / compilation','Review engagements','Agreed-upon procedures','Tax compliance','Payroll administration'].map(s=>`<span class="tag gray">${s}</span>`).join('')}</div><div class="mt12">${btn('Explore additional service requirements','service-detail','ghost sm','data-id="12"')}</div></div>${footer('§17 · AT-11')}`;}
function sourceBlocks(blocks){return `<div class="reference-block">${blocks.map(b=>b.kind==='table'?`<div class="tablewrap"><table><tbody>${b.rows.map(row=>`<tr>${row.map(c=>`<td>${esc(c).replace(/\n/g,'<br>')}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`:b.kind==='heading'?`<h4>${esc(b.text)}</h4>`:`<p>${esc(b.text).replace(/\n/g,'<br>')}</p>`).join('')}</div>`;}
function requirements(){return head('The requirements, made navigable.','STE-PRD-001 · Revision 1.0 · Source-derived requirements, not implementation status.',btn('About this prototype','about','','','info'))+tabs([['lifecycle','26-stage lifecycle'],['modes','Accounting modes'],['architecture','Architecture boundaries'],['source','Original requirements']],'requirementTab')+(ui.requirementTab==='lifecycle'?`<div class="banner">${icon('flow')}<p>Explore any stage to read its required inputs, optional steps, review responsibilities, transformations and exit conditions. Accounting and audit planning may progress in parallel.</p></div><div class="lifecycle-grid">${PRD.lifecycle.map(l=>`<button class="lifecycle-card" data-action="lifecycle-detail" data-id="${l.id}"><span class="eyebrow">${l.id}</span><h4>${esc(l.title)}</h4><span class="caption">Read stage requirements ${icon('arrow')}</span></button>`).join('')}</div><div class="panel panel-pad mt24"><h3>The cycle continues</h3><div class="service-track mt16"><span>Archive</span>${icon('arrow')}<span>Annual change review</span>${icon('arrow')}<span>Human continuance</span>${icon('arrow')}<span>New engagement</span>${icon('refresh')}</div><p class="sub mt12">Alternative path: authorized suspension → termination → scoped handover → access revocation → retained evidence.</p></div>`:ui.requirementTab==='modes'?`<div class="grid3"><div class="mode-card"><span class="tag blue">MODE 01</span><h3 class="mt16">Managed bookkeeping</h3><p>Approved source transactions → reviewed journals → entity ledger → trial balance. One posting authority per book. Shown as a requirement, not implemented in this prototype.</p></div><div class="mode-card"><span class="tag green">MODE 02</span><h3 class="mt16">External books / audit workspace</h3><p>External trial balance + approved reporting adjustments → adjusted reporting TB. This is the interactive accounting scenario in the demo.</p></div><div class="mode-card"><span class="tag purple">MODE 03</span><h3 class="mt16">Reporting-only / advisory</h3><p>Validated financial datasets → mappings, models and deliverables. No operational ledger posting unless separately contracted.</p></div></div>${note('The firm’s fee ledger is separate from client books. A professional-fee invoice must never become client income simply because the CRM client is selected.').replace('class="banner ','class="banner mt24 ')}`:ui.requirementTab==='architecture'?`<div class="panel panel-pad"><div class="architecture-row"><div class="architecture-node"><h3>Staff workspace</h3><p>Practice and professional responsibilities</p></div><div class="architecture-node"><h3>Client portal</h3><p>Requests, accounts approval and published deliverables</p></div></div><div class="architecture-arrow">↓</div><div class="architecture-row"><div class="architecture-node"><h3>Scoped identity & authorized commands</h3><p>Client · legal entity · engagement · period · role · revision</p></div></div><div class="architecture-arrow">↓</div><div class="architecture-row"><div class="architecture-node"><h3>Practice lifecycle</h3><p>CRM · acceptance · continuance · billing</p></div><div class="architecture-node"><h3>Accounting production</h3><p>Sources · mappings · journals · statements</p></div><div class="architecture-node"><h3>Professional delivery</h3><p>Workpapers · review · approvals · reports</p></div></div><div class="architecture-arrow">↓</div><div class="architecture-row"><div class="architecture-node"><h3>Versioned data & evidence</h3><p>Relational records · private documents · durable outbox · retained decisions</p></div></div></div><div class="grid2 mt20"><div class="requirements-note"><b>Source PRD architecture</b><br>ARC-01 specifies Perfex CRM / PHP / CodeIgniter / MySQL. Those are preserved in the source reference below, not silently replaced.</div><div class="requirements-note"><b>This deliverable</b><br>A standalone HTML/CSS/JavaScript product visualization. It does not implement Perfex, the AuditSphere .NET backend, or Microsoft services. Screen behavior demonstrates business intent only.</div></div><div class="panel panel-pad mt20"><h3>Production integrations are not connected</h3><div class="row wrap mt16">${['Microsoft Entra ID','SharePoint / Graph','Word / Excel','Purview records','Signing','Filing providers'].map(p=>`<span class="tag gray">${p} · not connected</span>`).join('')}</div></div>`:`<div class="toolbar"><div class="input-search" style="max-width:470px">${icon('search')}<input class="input" id="reference-search" value="${esc(ui.referenceSearch)}" placeholder="Search original sections and requirement text" aria-label="Search original requirements"></div><span class="tag gray">27 source sections</span></div>${PRD.sections.filter(s=>(s.title+JSON.stringify(s.blocks)).toLowerCase().includes(ui.referenceSearch.toLowerCase())).map(s=>`<details class="ref-details"><summary><span class="tag green">§${s.id}</span>${esc(s.title.replace(/^\d+\.\s*/,''))}</summary>${sourceBlocks(s.blocks)}</details>`).join('')}`)+footer();}
const pages={overview,clients,acquisition,engagements,onboarding,documents,accounting,audit,reviews,delivery,portal,billing,continuance,services,requirements};
/* Dialogs, accessibility, local commands, and strictly synthetic side effects. */
function openModal(title,body,foot='',wide=false){lastFocus=document.activeElement;ui.modal=title;document.getElementById('modal-root').innerHTML=`<div class="modal-backdrop" id="modal-backdrop"><section class="modal ${wide?'wide':''}" role="dialog" aria-modal="true" aria-labelledby="modal-title"><div class="modal-head"><h2 id="modal-title">${esc(title)}</h2><button class="icon-btn" data-action="close-modal" aria-label="Close dialog">${icon('close')}</button></div><div class="modal-body">${body}</div>${foot?`<div class="modal-footer">${foot}</div>`:''}</section></div>`;document.body.style.overflow='hidden';setTimeout(()=>document.querySelector('.modal input:not([type=checkbox]), .modal textarea, .modal select, .modal button')?.focus(),30);}
function closeModal(){document.getElementById('modal-root').innerHTML='';document.body.style.overflow='';ui.modal=null;if(lastFocus?.isConnected)lastFocus.focus();}
function val(id){return document.getElementById(id)?.value?.trim()||'';}
function required(id,label){const v=val(id);if(!v)throw Error(label+' is required.');return v;}
function download(content,name,type='text/plain'){const b=new Blob([content],{type});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),2000);}
function canonical(v){if(Array.isArray(v))return v.map(canonical);if(v&&typeof v==='object')return Object.fromEntries(Object.keys(v).sort().map(k=>[k,canonical(v[k])]));return v;}
async function sha256(text){
 const bytes=typeof text==='string'?new TextEncoder().encode(text):new Uint8Array(text);
 if(globalThis.crypto?.subtle){const digest=await crypto.subtle.digest('SHA-256',bytes);return [...new Uint8Array(digest)].map(x=>x.toString(16).padStart(2,'0')).join('');}
 // Standard SHA-256 fallback for local, opaque-origin previews. Used for demo
 // content identity only, never authentication, signing, or adversarial integrity.
 const K=[0x428a2f98,0x71374491,0xb5c0fbcf,0xe9b5dba5,0x3956c25b,0x59f111f1,0x923f82a4,0xab1c5ed5,0xd807aa98,0x12835b01,0x243185be,0x550c7dc3,0x72be5d74,0x80deb1fe,0x9bdc06a7,0xc19bf174,0xe49b69c1,0xefbe4786,0x0fc19dc6,0x240ca1cc,0x2de92c6f,0x4a7484aa,0x5cb0a9dc,0x76f988da,0x983e5152,0xa831c66d,0xb00327c8,0xbf597fc7,0xc6e00bf3,0xd5a79147,0x06ca6351,0x14292967,0x27b70a85,0x2e1b2138,0x4d2c6dfc,0x53380d13,0x650a7354,0x766a0abb,0x81c2c92e,0x92722c85,0xa2bfe8a1,0xa81a664b,0xc24b8b70,0xc76c51a3,0xd192e819,0xd6990624,0xf40e3585,0x106aa070,0x19a4c116,0x1e376c08,0x2748774c,0x34b0bcb5,0x391c0cb3,0x4ed8aa4a,0x5b9cca4f,0x682e6ff3,0x748f82ee,0x78a5636f,0x84c87814,0x8cc70208,0x90befffa,0xa4506ceb,0xbef9a3f7,0xc67178f2];
 const H=[0x6a09e667,0xbb67ae85,0x3c6ef372,0xa54ff53a,0x510e527f,0x9b05688c,0x1f83d9ab,0x5be0cd19];
 const len=Math.ceil((bytes.length+9)/64)*64,data=new Uint8Array(len);data.set(bytes);data[bytes.length]=128;
 const view=new DataView(data.buffer),bits=bytes.length*8;view.setUint32(len-8,Math.floor(bits/4294967296));view.setUint32(len-4,bits>>>0);
 const rr=(x,n)=>(x>>>n)|(x<<(32-n)),W=new Uint32Array(64);
 for(let off=0;off<len;off+=64){for(let i=0;i<16;i++)W[i]=view.getUint32(off+i*4);for(let i=16;i<64;i++){let x=W[i-15],y=W[i-2];W[i]=((rr(x,7)^rr(x,18)^(x>>>3))+W[i-16]+(rr(y,17)^rr(y,19)^(y>>>10))+W[i-7])>>>0;}
 let [a,b,c,d,e,f,g,h]=H;for(let i=0;i<64;i++){const t1=(h+(rr(e,6)^rr(e,11)^rr(e,25))+((e&f)^(~e&g))+K[i]+W[i])>>>0,t2=((rr(a,2)^rr(a,13)^rr(a,22))+((a&b)^(a&c)^(b&c)))>>>0;h=g;g=f;f=e;e=(d+t1)>>>0;d=c;c=b;b=a;a=(t1+t2)>>>0;}[a,b,c,d,e,f,g,h].forEach((x,i)=>H[i]=(H[i]+x)>>>0);}
 return H.map(x=>x.toString(16).padStart(8,'0')).join('');
}
function plainReport(e,release=null){return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Illustrative financial preview — ${esc(C(e.client)?.name)}</title><style>body{font:14px/1.6 system-ui,sans-serif;color:#233e42;margin:40px auto;padding:24px;max-width:800px}h2{font-size:22px}h3{margin-top:30px}table{width:100%;border-collapse:collapse}td,th{padding:8px;border-bottom:1px solid #e1e9e6;text-align:left}.right{text-align:right}.statement-total{font-weight:bold;background:#f0f7f3}.watermark{color:#9b6a33;font-size:12px;letter-spacing:1px}.caption{font-size:11px;color:#637a80}.letterhead{border-bottom:2px solid #233e42;padding-bottom:14px}.divider{border-top:1px solid #e1e9e6;margin:24px 0}</style>${statementPaper(e,release)}</html>`;}
function blankEng(id,client,service,year){const e=newEng(id,client,service,'Onboarding');e.year=year;e.period=`01 Jan – 31 Dec ${year}`;e.rows=[];e.sourceVersion=0;e.generation=1;e.packageRevision=0;e.builtGeneration=0;e.adjustment=0;e.journalState='Not proposed';e.acceptance=false;e.terms=false;e.planning=false;e.sourceAccepted=false;e.mappingApproved=false;e.supplements=false;e.releases=[];e.archive=null;e.eqrRequired=false;e.reviews=[];e.workpapers=e.workpapers.map(w=>({...w,status:'Planned',version:1,conclusion:''}));if(service!=='External audit')e.workpapers=[{id:'WP-P1',title:'Service plan & deliverable review',objective:'Define service-specific work and review the contracted deliverable.',assertion:'Service-specific criteria',risk:'Scope and deliverable mismatch',version:1,status:'Planned',preparer:'Adam Khan',reviewer:'Sara Malik',conclusion:''}];e.reconciliations=e.reconciliations.map(r=>({...r,status:'Not started'}));e.pbc=e.pbc.map(p=>({...p,status:'Requested',file:'',version:0}));e.questionnaire={answers:{},status:'Not started'};return e;}
function nextEngId(year){return `ENG-${String(year).slice(-2)}${String(state.engagements.length+1).padStart(3,'0')}`;}
function sourceInvalidation(reason){const e=E();changed(reason);e.supplements=false;e.workpapers.forEach(w=>{w.status='Changes required';});e.reviews.forEach(r=>{r.history.push({status:r.status,response:r.response,version:r.version});r.status='Open';r.response='';});e.reconciliations.forEach(r=>r.status='Review required');save();}
function sourceModal(section){const s=PRD.sections.find(s=>s.id===Number(section));if(!s)return;openModal(s.title,`<p class="caption">Read-only text extracted from STE-PRD-001, revision 1.0. Source requirements are not implementation claims.</p>${sourceBlocks(s.blocks)}`,btn('Close','close-modal'),true);}
function workpaperModal(id){const e=E(),w=e.workpapers.find(w=>w.id===id);if(!w)return;const notes=e.reviews.filter(r=>r.wp===w.id);openModal(w.id+' · '+w.title,`${note('An illustrative working-paper record. No real procedures, confirmations or professional conclusions have been performed.')}<div class="between">${badge(w.status)}<span class="mono">Revision ${w.version} · generation ${e.generation}</span></div><div class="info-grid mt20"><div><label>Objective</label><span>${esc(w.objective)}</span></div><div><label>Assertions / criteria</label><span>${esc(w.assertion)}</span></div><div><label>Risk</label><span>${esc(w.risk)}</span></div></div><div class="field mt20"><label for="wp-conclusion">Work performed, evidence and conclusion</label><textarea class="input" id="wp-conclusion">${esc(w.conclusion)}</textarea></div><div class="attachment-list"><span class="attachment-chip">${icon('file')}Sample source TB-v${e.sourceVersion}</span><span class="attachment-chip">${icon('file')}Illustrative supporting schedule</span></div><div class="mt20"><h4>Linked review points</h4>${notes.length?notes.map(n=>`<div class="list-item"><span class="small">${n.id} · ${esc(n.title)}</span>${badge(n.status)}</div>`).join(''):'<p class="sub">No open note. Independent working-paper review is still required.</p>'}</div><p class="role-note">Preparer submits a revision; Senior reviewer clears it only after related review points are cleared.</p>`,`${btn('Submit as preparer','submit-wp','',`data-id="${id}"`)}${btn('Clear as reviewer','clear-wp','primary',`data-id="${id}"`)}`,true);}
function pbcModal(id){const p=E().pbc.find(p=>p.id===id);if(!p)return;const clientPreview=ui.route==='portal';openModal(p.id+' · '+p.title,`<div class="between">${badge(p.status)}<span class="caption">Due ${shortDate(p.due)} · ${esc(p.owner)}</span></div><p class="sub mt16">Please supply the correct entity, period and supporting information. Uploading is not acceptance by the engagement team.</p><div class="dropzone mt20">${icon('upload')}<p>${p.file?esc(p.file):'Choose a synthetic file to demonstrate upload'}</p><small>PDF, DOCX, XLSX, CSV, TXT or image · max 5 MiB</small><small>Metadata and SHA-256 only. File bytes are not stored or sent to a provider.</small><div class="mt16">${btn(p.file?'Replace local file':'Choose local file','pick-document','soft sm',`data-id="${p.id}"`,'upload')}${btn('Use built-in sample','sample-upload','sm',`data-id="${p.id}"`)}</div></div>${p.file?`<div class="info-grid mt20"><div><label>Filename</label><span>${esc(p.file)}</span></div><div><label>Version</label><span>v${p.version}</span></div><div><label>Storage</label><span>Local metadata only</span></div></div>${p.sha?`<div class="hash mt12">SHA-256: ${p.sha}</div>`:''}<p class="sub mt12">${p.local?'The uploaded bytes are not available after this dialog; only metadata remains.':'This named sample is a visual reference, not a stored PDF or Office document.'}</p>`:''}${p.message?`<div class="comment"><h4>Request clarification</h4><p>${esc(p.message)}</p></div>`:''}`,clientPreview?btn('Close','close-modal'):`${btn('Request clarification','pbc-clarify','',`data-id="${id}"`)}${btn('Accept for request','pbc-accept','primary',`data-id="${id}"`)}`);}
function approvalPrecheck(key){const e=E();if(e.builtGeneration!==e.generation)throw Error('Rebuild the current financial package before approval.');if(!e.sourceAccepted||!e.mappingApproved||totals(e).balance!==0)throw Error('Resolve source and mapping checks first.');if(!e.supplements)throw Error('Review the missing supplemental information first.');if(!allNotesClear()||!allWorkClear())throw Error('Independently clear review points and working papers first.');if(key!=='manager'&&!isCurrent('manager'))throw Error('Current manager review must be recorded first.');if(['partner','eqr'].includes(key)&&!isCurrent('client'))throw Error('Current management approval must be recorded first.');if(key==='eqr'&&!e.eqrRequired)throw Error('EQR is not required for this sample profile.');}
function newLeadModal(){openModal('New inquiry',`<div class="field-grid"><div class="field full"><label for="lead-name">Organization name *</label><input id="lead-name" class="input" placeholder="Synthetic organization"></div><div class="field"><label for="lead-contact">Contact name *</label><input id="lead-contact" class="input" placeholder="Demo contact"></div><div class="field"><label for="lead-service">Service interest *</label><select id="lead-service" class="input">${['External audit','Annual accounts','Internal audit','Business valuation','Management consulting'].map(t=>`<option>${t}</option>`).join('')}</select></div><div class="field"><label for="lead-value">Illustrative fee estimate (QAR) *</label><input id="lead-value" type="number" min="0" step="0.01" class="input" value="5000"></div><div class="field"><label>Source</label><span class="input">Manual demo inquiry</span></div></div><p class="sub mt20">This adds a local synthetic opportunity only. It does not accept a client, send a proposal or create an invoice.</p>`,`${btn('Cancel','close-modal')}${btn('Create inquiry','save-lead','primary')}`);}
function leadModal(id){const l=state.leads.find(l=>l.id===id);if(!l)return;openModal(l.name,`<div class="between">${badge(l.stage)}<span class="mono">${l.id}</span></div><div class="info-grid mt20"><div><label>Contact</label><span>${esc(l.contact)}</span></div><div><label>Service</label><span>${esc(l.service)}</span></div><div><label>Illustrative fee</label><span>${cash(l.value)}</span></div></div><div class="divider"></div><div class="controlbox"><span class="statusicon ${l.accepted?'':'pending'}">${icon(l.accepted?'check':'clock')}</span><div><h4>Partner acceptance</h4><p>Explicit demonstration decision, not a real assessment.</p></div></div><div class="controlbox mt12"><span class="statusicon ${l.terms?'':'pending'}">${icon(l.terms?'check':'clock')}</span><div><h4>Effective terms</h4><p>Client signatory demonstration; no document signed.</p></div></div><div class="row wrap mt20">${btn('Partner acceptance','lead-accept','sm',`data-id="${id}"`)}${btn('Record client terms','lead-terms','sm',`data-id="${id}"`)}</div><p class="role-note">Use the role selector above the workspace before opening this dialog. The client and partner decisions remain separate.</p>`,`${btn('Close','close-modal')}${l.stage!=='Converted'?btn(l.stage==='Proposal'?'Convert to client':'Advance stage','lead-advance','primary',`data-id="${id}"`):''}`);}
function engagementModal(){openModal('Create a draft engagement',`<div class="field-grid"><div class="field full"><label for="new-eng-client">Client *</label><select id="new-eng-client" class="input">${state.clients.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label for="new-eng-service">Service *</label><select id="new-eng-service" class="input">${['External audit','Annual accounts','Internal audit','Business valuation','Management consulting','Corporate training'].map(s=>`<option>${s}</option>`).join('')}</select></div><div class="field"><label for="new-eng-year">Reporting year *</label><input id="new-eng-year" class="input" type="number" min="2020" max="2100" value="2026"></div></div>${note('The new shell begins with no source balances, approvals or completed work. Non-audit services do not inherit an audit opinion.').replace('class="banner ','class="banner mt20 ')}`,`${btn('Cancel','close-modal')}${btn('Create draft','save-eng','primary')}`);}
function tour(){openModal('Explore the product, not just the screens.',`<p class="small muted mb16">Use the Example Trading Entity engagement to walk through the core workflow. The top-right selector switches simulated roles.</p>${[['1','Explore the source-to-statement path','Accounting → Trial balance shows the source, QAR 500 adjustment and adjusted balances.'],['2','Respond, then independently review','Reviews → choose Preparer to submit responses, then Senior reviewer to clear each point.'],['3','Rebuild and approve the exact version','Accounting → Rebuild package. Reviews → Package approvals: Manager, Client signatory, Partner, then Quality reviewer.'],['4','Prepare, issue and preserve a demo package','Delivery → prepare candidate and issue as Partner, record delivery, then assemble an archive as Records custodian.'],['5','Prove that changes matter','Simulate a source change. The current package becomes stale while the previously issued demo snapshot remains unchanged.']].map(([n,t,p])=>`<div class="tour-step"><div class="stepnum">${n}</div><div><h4>${t}</h4><p>${p}</p></div></div>`).join('')}<div class="requirements-note">No real authentication, security enforcement, signatures, emails, filings or Microsoft operations are performed. Use synthetic data only. Reset demo restores the initial scenario.</div>`,`${btn('Explore requirements','tour-requirements')}${btn('Start at accounting','tour-accounting','primary')}`);}
function activityModal(){openModal('Activity in this browser',`<p class="sub mb16">This is editable browser storage, not a secure production audit trail.</p>${state.events.map(ev=>`<div class="activity"><div class="activity-dot">${icon(ev.type)}</div><div><p>${esc(ev.text)}</p><small>${esc(ev.ref)} · ${esc(ev.time)}</small></div></div>`).join('')}`,btn('Close','close-modal'));}
function searchModal(){openModal('Find your next action',`<div class="input-search" style="max-width:none">${icon('search')}<input class="input" id="global-search" placeholder="Search clients, workspaces and service requirements" autocomplete="off"></div><div class="search-results" id="search-results"></div>`);updateSearch('');}
function updateSearch(query){const q=query.toLowerCase();const routes=nav.flatMap(x=>x[1]).map(([route,label,ic])=>({title:label,sub:'Workspace',ic,route}));const clients=state.engagements.map(e=>({title:C(e.client)?.name||'Client',sub:e.id+' · '+e.service,ic:'users',eng:e.id}));const services=PRD.services.slice(0,12).map((s,i)=>({title:s.title,sub:'Service requirements',ic:'layers',service:i}));const items=[...routes,...clients,...services].filter(x=>(x.title+' '+x.sub).toLowerCase().includes(q)).slice(0,12);const target=document.getElementById('search-results');if(target)target.innerHTML=items.length?items.map(x=>`<button class="search-result" ${x.route?`data-route="${x.route}"`:x.eng?`data-action="eng-open" data-id="${x.eng}"`:`data-action="service-detail" data-id="${x.service}"`}>${icon(x.ic)}<div><b class="small">${esc(x.title)}</b><small>${esc(x.sub)}</small></div></button>`).join(''):'<div class="empty">No matching demo records.</div>';}
function csvEscape(x){let s=String(x);if(/^[=+@\t\r-]/.test(s))s="'"+s;return '"'+s.replace(/"/g,'""')+'"';}
const actions={
 'menu':()=>{document.getElementById('sidebar')?.classList.toggle('open');document.getElementById('mobile-overlay')?.classList.toggle('open');},
 'close-modal':closeModal,
 'open-reviews':()=>{ui.reviewTab='notes';go('reviews');},
 'open-approvals':()=>{ui.reviewTab='approvals';go('reviews');},
 'open-overview':()=>go('overview'),
 'tab':d=>{ui[d.key]=d.id;render();},
 'search':searchModal,'activity':activityModal,'tour':tour,
 'tour-accounting':()=>{closeModal();ui.accountTab='tb';go('accounting');},
 'tour-requirements':()=>{closeModal();go('requirements');},
 'source':d=>sourceModal(d.section),
 'eng-open':d=>{if(!state.engagements.some(e=>e.id===d.id))return;state.selected=d.id;ui.selectedReview=E().reviews[0]?.id||'';save();closeModal();go('engagements');},
 'new-lead':newLeadModal,'lead-detail':d=>leadModal(d.id),'new-eng':engagementModal,
 'save-lead':()=>{const name=required('lead-name','Organization'),contact=required('lead-contact','Contact'),value=cents(required('lead-value','Fee estimate'));if(value<0)throw Error('Fee estimate must not be negative.');const l={id:'LD-'+String(state.leads.length+1).padStart(3,'0'),name:name.slice(0,100),contact:contact.slice(0,80),value,service:val('lead-service'),stage:'Inquiry',accepted:false,terms:false};state.leads.push(l);log('Demo inquiry created: '+l.name,l.id,'target');closeModal();go('acquisition');toast('Local opportunity created. No acceptance or posting occurred.');},
 'lead-accept':d=>{if(!roleAllowed('partner'))return;const l=state.leads.find(l=>l.id===d.id);l.accepted=true;log('Partner recorded synthetic lead acceptance',l.id);leadModal(d.id);render();},
 'lead-terms':d=>{if(!roleAllowed('client'))return;const l=state.leads.find(l=>l.id===d.id);l.terms=true;log('Client recorded demo terms acceptance',l.id);leadModal(d.id);render();},
 'lead-advance':d=>{const l=state.leads.find(l=>l.id===d.id);const stages=['Inquiry','Discovery','Evaluation','Proposal'];if(l.stage==='Evaluation'&&!l.accepted)throw Error('Partner acceptance is required before this demo opportunity advances.');if(l.stage==='Proposal'){if(!l.accepted||!l.terms)throw Error('Record partner acceptance and client terms separately before conversion.');const id='CL-'+String(state.clients.length+1).padStart(3,'0');state.clients.push({id,name:l.name,initials:l.name.split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase(),color:'blue',industry:'New demo relationship',contact:l.contact,risk:'Review required',status:'Active',country:'Demo jurisdiction',revenue:0});const e=blankEng(nextEngId(2026),id,l.service,2026);e.acceptance=true;e.terms=true;state.engagements.push(e);state.selected=e.id;l.stage='Converted';log('Lead converted once; new draft engagement created',l.id);closeModal();go('engagements');toast('Converted to a synthetic client and draft engagement.');return;}l.stage=stages[stages.indexOf(l.stage)+1];log('Opportunity advanced to '+l.stage,l.id);leadModal(l.id);render();},
 'save-eng':()=>{const client=required('new-eng-client','Client'),service=required('new-eng-service','Service'),year=Number(required('new-eng-year','Year'));if(!Number.isInteger(year)||year<2020||year>2100)throw Error('Choose a year from 2020 to 2100.');const e=blankEng(nextEngId(year),client,service,year);state.engagements.push(e);state.selected=e.id;log('Draft engagement created; gates remain pending',e.id,'brief');closeModal();go('engagements');},
 'client-detail':d=>{const c=C(d.id),es=state.engagements.filter(e=>e.client===d.id);openModal(c.name,`<div class="row">${avatar(c.initials,c.color,'square')}<div><h3>${esc(c.name)}</h3><p class="sub">${esc(c.industry)} · ${c.id}</p></div></div><div class="info-grid mt24"><div><label>Relationship contact</label><span>${esc(c.contact)}</span></div><div><label>Sample risk</label><span>${esc(c.risk)}</span></div><div><label>Active engagements</label><span>${es.length}</span></div></div><div class="divider"></div>${es.map(e=>`<div class="list-item"><div><b class="small">${e.id}</b><p class="sub">${esc(e.service)} · FY ${e.year}</p></div>${btn('Open','eng-open','sm',`data-id="${e.id}"`,'arrow')}</div>`).join('')}<p class="sub mt20">All details are illustrative. Legal identities, contact authority and company records are not verified.</p>`,btn('Close','close-modal'));},
 'service-for-eng':()=>{const s=PRD.services.findIndex(s=>s.title.toLowerCase().includes(E().service.toLowerCase()));actions['service-detail']({id:s>=0?s:12});},
 'service-detail':d=>{const s=PRD.services[Number(d.id)];if(s)openModal(s.title,`<p class="caption">Source-required workflow. Read-only reference; not an implemented specialist engine.</p>${sourceBlocks(s.blocks)}`,btn('Close','close-modal'),true);},
 'lifecycle-detail':d=>{const l=PRD.lifecycle.find(x=>x.id===d.id);if(l)openModal(l.id+' · '+l.title,sourceBlocks(l.blocks),btn('Close','close-modal'),true);},
 'assessment-submit':()=>{if(Object.values(E().questionnaire.answers).filter(Boolean).length!==4)throw Error('Review all four demo information groups before submission.');E().questionnaire.status='Submitted';log('Synthetic acceptance assessment submitted');render();toast('Submitted for partner decision.');},
 'assessment-accept':()=>{if(!roleAllowed('partner'))return;if(E().questionnaire.status!=='Submitted')throw Error('Submit the demo assessment first.');E().acceptance=true;E().questionnaire.status='Accepted in demo';log('Partner recorded a demo acceptance decision');render();toast('Demo acceptance recorded; other activation gates still apply.');},
 'record-terms':()=>{if(!roleAllowed('client'))return;E().terms=true;log('Demo client terms acceptance recorded');render();toast('Local terms record updated; no real signature created.');},
 'pbc-detail':d=>pbcModal(d.id),
 'new-pbc':()=>openModal('Request client evidence',`<div class="field-grid"><div class="field full"><label for="pbc-title">Requested information *</label><input class="input" id="pbc-title" placeholder="e.g. Year-end inventory listing"></div><div class="field"><label for="pbc-date">Due date *</label><input class="input" id="pbc-date" type="date" value="2026-09-28"></div><div class="field"><label for="pbc-owner">Client owner *</label><input class="input" id="pbc-owner" value="${esc(C().contact)}"></div></div><p class="sub mt20">Creates a local request. No notification is sent.</p>`,`${btn('Cancel','close-modal')}${btn('Create request','save-pbc','primary')}`),
 'save-pbc':()=>{const title=required('pbc-title','Request description'),due=required('pbc-date','Due date'),owner=required('pbc-owner','Owner');if(!/^\d{4}-\d{2}-\d{2}$/.test(due))throw Error('Use a valid date.');const p={id:'PBC-'+String(E().pbc.length+1).padStart(2,'0'),title:title.slice(0,150),due,owner:owner.slice(0,80),category:'Other evidence',status:'Requested',file:'',version:0};E().pbc.push(p);log('Local PBC request created: '+p.title,p.id,'folder');closeModal();render();toast('Request created. No email sent.');},
 'pick-document':d=>{uploadTarget={eng:E().id,pbc:d.id};const p=document.getElementById('document-picker');p.value='';p.click();},
 'sample-upload':d=>{const p=E().pbc.find(x=>x.id===d.id);p.history=p.history||[];if(p.file)p.history.push({file:p.file,version:p.version,status:p.status});p.file=p.id+'_synthetic_support.pdf';p.version++;p.status='Received';p.local=false;p.sha=null;changed('New illustrative evidence received for '+p.id);pbcModal(d.id);render();toast('Sample receipt recorded. Acceptance is a separate reviewer decision.');},
 'pbc-accept':d=>{if(!roleAllowed(['reviewer','manager']))return;const p=E().pbc.find(x=>x.id===d.id);if(!p.file)throw Error('Receive a file or built-in sample first.');p.status='Accepted';log('Reviewer accepted sample evidence for '+p.id,p.id);closeModal();render();toast('Accepted for this request, not a conclusion about audit sufficiency.');},
 'pbc-clarify':d=>{openModal('Request clarification',`<div class="field"><label for="pbc-clarification">Client-visible request *</label><textarea class="input" id="pbc-clarification" placeholder="Specify what is missing or which period is required."></textarea></div><p class="sub mt12">Internal review deliberations are not included. No message is sent externally.</p>`,`${btn('Cancel','close-modal')}${btn('Record clarification','save-pbc-clarification','primary',`data-id="${d.id}"`)}`);},
 'save-pbc-clarification':d=>{if(!roleAllowed(['reviewer','manager']))return;const msg=required('pbc-clarification','Clarification');const p=E().pbc.find(x=>x.id===d.id);p.message=msg;p.status='Clarification required';log('Clarification recorded for '+p.id,p.id,'message');closeModal();render();},
 'import-tb':()=>openModal('Import a synthetic trial balance',`${note('Local CSV demonstration only. No file is sent to a server; XLSX parsing is not included. Use synthetic data, maximum 1 MiB / 2,000 rows.','amber')}<p class="small">Required headers:</p><div class="hash mt12">Code,Account,Type,Balance</div><p class="sub mt12">Type must be asset, liability, equity, revenue or expense. Amounts use a debit-positive convention and at most two decimal places. Signed balances must total zero.</p><div class="row mt20">${btn('Download CSV template','tb-template','soft','','download')}</div><p class="sub mt16">A successful import creates a new local source version and invalidates dependent approvals. It does not post to a ledger.</p>`,`${btn('Cancel','close-modal')}${btn('Choose CSV','pick-tb','primary','','upload')}`),
 'pick-tb':()=>{uploadTarget={eng:E().id,type:'tb'};const p=document.getElementById('tb-picker');p.value='';p.click();},
 'tb-template':()=>download('Code,Account,Type,Balance\n'+initialRows().map(r=>[r.code,r.name,r.type,(r.balance/100).toFixed(2)].join(',')).join('\n'),'synthetic_trial_balance.csv','text/csv'),
 'export-tb':()=>{const e=E(),t=totals();const out='Code,Account,Type,SourceBalance,ReportingAdjustment,AdjustedBalance\n'+t.rows.map(r=>{const raw=e.rows.find(x=>x.code===r.code)?.balance||0;return [csvEscape(r.code),csvEscape(r.name),r.type,(raw/100).toFixed(2),((r.balance-raw)/100).toFixed(2),(r.balance/100).toFixed(2)].join(',');}).join('\n');download(out,e.id+'_DEMO_TB.csv','text/csv');toast('Synthetic TB exported; not a posted ledger.');},
 'source-change':()=>{const e=E();const ar=e.rows.find(r=>r.code==='1100'),rev=e.rows.find(r=>r.code==='4000');if(!ar||!rev)throw Error('This demonstration requires the built-in Example Trading account codes.');ar.balance+=100000;rev.balance-=100000;e.sourceVersion++;sourceInvalidation('Synthetic credit sale changed receivables and revenue');render();toast('New source version recorded. Package and approvals are stale.','warn');},
 'replace-tb':()=>{const e=E();if(e.journalState!=='Applied')throw Error('Apply the approved demo journal before simulating a posted-source replacement.');if(e.sourceReflection){toast('The selected source already reflects the adjustment. No duplicate entry applied.');return;}const before=totals(e).profit;e.rows=totals(e).rows;e.sourceReflection=true;e.sourceVersion++;sourceInvalidation('Replacement TB now contains AJ-001; reporting effect is zero');render();toast('Profit remains '+cash(before)+'. AJ-001 was not applied twice.');},
 'approve-mapping':()=>{if(!roleAllowed('reviewer'))return;if(!E().rows.length)throw Error('Import a source trial balance first.');E().mappingApproved=true;log('Senior reviewer approved the demo mapping');render();toast('Mapping approved for this demo source.');},
 'revise-adjustment':()=>openModal('Revise the depreciation proposal',`<p class="small muted">Creates a new proposal and invalidates the current package. A client signatory must authorize it before inclusion.</p><div class="field mt20"><label for="aj-amount">Proposed depreciation (QAR) *</label><input class="input" id="aj-amount" type="number" min="0" step="0.01" value="${(E().adjustment/100).toFixed(2)}"></div>`,`${btn('Cancel','close-modal')}${btn('Save proposal','save-aj','primary')}`),
 'save-aj':()=>{if(!roleAllowed('preparer'))return;const amount=cents(required('aj-amount','Amount'));if(amount<0)throw Error('Use a nonnegative amount.');if(E().sourceReflection)throw Error('This source already includes the prior adjustment. Reset the demo or import a new pre-adjustment source; do not overwrite posted history.');E().adjustment=amount;E().journalState='Management pending';sourceInvalidation('New illustrative depreciation proposal created');closeModal();render();toast('Proposal recorded. Client signatory authorization is still required.');},
 'apply-adjustment':()=>{if(!roleAllowed('client'))return;E().journalState='Applied';changed('Client authorized the synthetic reporting adjustment');render();toast('Included in demo reporting only. No ledger posting occurred.');},
 'build-package':()=>{const e=E();if(!e.sourceAccepted||!e.rows.length)throw Error('An accepted source dataset is required.');if(!e.mappingApproved)throw Error('A reviewer must approve the mapping first.');if(totals(e).balance!==0)throw Error('The trial balance does not balance.');if(e.journalState==='Management pending')throw Error('Resolve the proposed journal before building.');if(e.builtGeneration===e.generation){toast('The current package already matches these inputs.');return;}e.packageRevision++;e.builtGeneration=e.generation;e.candidate=null;log('Rebuilt package v'+e.packageRevision+' for input generation '+e.generation);render();toast('New draft package created. Historical approvals were not copied.');},
 'supplements':()=>openModal('Review illustrative supplemental information',`${note('These are demo assumptions, not information inferred from the closing trial balance. A production workflow requires actual movement schedules and management disclosures.','amber')}<div class="info-grid"><div><label>Original fixture opening cash</label><span>QAR 8,000</span></div><div><label>Original fixture closing cash</label><span>QAR 10,000</span></div><div><label>Original receivables movement</label><span>Increase of QAR 1,000</span></div></div><p class="small muted mt20">The source’s worked example does not supply cash-flow movements. This supplemental illustration exists only to show the review interaction. Any imported or changed source needs separate professionally prepared support.</p><label class="checkbox mt20"><input type="checkbox" id="supp-confirm">I understand these are synthetic assumptions and confirm the demonstration review only.</label>`,`${btn('Cancel','close-modal')}${btn('Confirm demo review','confirm-supplements','primary')}`),
 'confirm-supplements':()=>{if(!roleAllowed(['reviewer','manager']))return;if(!document.getElementById('supp-confirm').checked)throw Error('Acknowledge the illustrative nature of these inputs.');E().supplements=true;log('Supplemental schedule review simulated; no actual evidence verified');closeModal();render();toast('Demo supplementary review recorded.');},
 'preview-report':()=>openModal('Financial statement preview',statementPaper(),`${btn('Close','close-modal')}${btn('Export preview HTML','export-report','soft','','download')}`,true),
 'export-report':()=>download(plainReport(E()),E().id+'_ILLUSTRATIVE_FINANCIALS.html','text/html'),
 'print-report':()=>{ui.accountTab='statements';go('accounting');window.print();},
 'reconciliation':d=>{const r=E().reconciliations[Number(d.id)];openModal(r.name+' · '+r.ref,`${note('This schedule is a visual demonstration. No automatic bank matching or independent verification is performed.')}<div class="info-grid"><div><label>Source</label><span>TB-v${E().sourceVersion}</span></div><div><label>Supporting basis</label><span>${r.evidence}</span></div><div><label>Status</label>${badge(r.status)}</div></div><div class="field mt20"><label for="rec-conclusion">Reviewer conclusion *</label><textarea id="rec-conclusion" class="input" placeholder="Describe the demonstration tie-out, evidence and treatment of differences."></textarea></div>`,`${btn('Cancel','close-modal')}${btn('Record demo clearance','clear-rec','primary',`data-id="${d.id}"`)}`);},
 'clear-rec':d=>{if(!roleAllowed('reviewer'))return;const txt=required('rec-conclusion','Reviewer conclusion');const r=E().reconciliations[Number(d.id)];r.status='Cleared';r.conclusion=txt;log('Sample reconciliation cleared: '+r.ref);closeModal();render();},
 'workpaper':d=>workpaperModal(d.id),
 'submit-wp':d=>{if(!roleAllowed('preparer'))return;const txt=required('wp-conclusion','Conclusion');const w=E().workpapers.find(w=>w.id===d.id);w.conclusion=txt;w.version++;w.status='Submitted';changed('Workpaper '+w.id+' submitted as revision '+w.version);closeModal();render();toast('Submitted for independent review; package re-approval may be required.');},
 'clear-wp':d=>{if(!roleAllowed('reviewer'))return;const w=E().workpapers.find(w=>w.id===d.id);if(E().reviews.some(r=>r.wp===w.id&&r.status!=='Cleared'))throw Error('Clear the linked review points first.');if(!w.conclusion?.trim())throw Error('A preparer must submit a conclusion first.');w.status='Cleared';log('Independent demo review cleared '+w.id);closeModal();render();toast('Working-paper demo review recorded.');},
 'planning':()=>openModal('Strategy & materiality preview',`${note('Materiality percentages are an illustration of workflow, not a recommended or validated audit method.')}<div class="info-grid"><div><label>Scope</label><span>${esc(E().service)}</span></div><div><label>Entity / period</label><span>${esc(C().name)} / FY ${E().year}</span></div><div><label>Methodology</label><span>Approved professional methodology required</span></div></div><div class="field-grid mt20"><div class="field"><label for="mat-benchmark">Illustrative benchmark (QAR)</label><input id="mat-benchmark" class="input" value="1000000" type="number" min="1"></div><div class="field"><label for="mat-percent">Illustrative percentage</label><input id="mat-percent" class="input" value="5" type="number" min="0.01" max="100" step="0.01"></div><div class="field full"><label for="plan-rationale">Scope and selection rationale *</label><textarea id="plan-rationale" class="input" placeholder="Describe the demo planning rationale; production requires professional methodology and evidence."></textarea></div></div><p class="sub mt16">The action records a local planning illustration. It does not select a real audit materiality threshold or issue an audit strategy.</p>`,`${btn('Cancel','close-modal')}${btn('Record demo plan','save-planning','primary')}`,true),
 'save-planning':()=>{if(!roleAllowed('partner'))return;const rationale=required('plan-rationale','Rationale'),b=cents(required('mat-benchmark','Benchmark')),p=Number(required('mat-percent','Percentage'));if(b<=0||!Number.isFinite(p)||p<=0||p>100)throw Error('Use a positive benchmark and percentage from 0 to 100.');E().planning=true;E().materiality={benchmark:b,percent:p,amount:Math.round(b*p/100),rationale};changed('Partner recorded an illustrative planning revision');closeModal();render();toast('Demo planning value: '+cash(E().materiality.amount)+'. Not a recommended method.');},
 'sample-result':()=>openModal('Illustrative sample test',`<div class="field"><label for="sample-result">Work performed and limitation *</label><textarea id="sample-result" class="input" placeholder="Record the inspected demonstration item, evidence and exception. No projection to untested items."></textarea></div>`,`${btn('Cancel','close-modal')}${btn('Save local test note','save-sample','primary')}`),
 'save-sample':()=>{const txt=required('sample-result','Test note');E().sampleNote=txt;changed('Specific-item demonstration test note recorded');closeModal();render();toast('Recorded locally; no statistical assurance claimed.');},
 'summary-memo':()=>openModal('Summary memorandum · illustrative draft',`<h3>${esc(C().name)} · FY ${E().year}</h3><div class="reference-block"><h4>Work performed</h4><p>Sample accounting and workpaper records linked to TB-v${E().sourceVersion} and package-v${E().packageRevision}.</p><h4>Review points</h4><p>${E().reviews.filter(r=>r.status!=='Cleared').length} remain open. Current clearances are specific to their recorded revisions.</p><h4>Significant matters and differences</h4><p>The original example includes a QAR 500 depreciation adjustment. Actual estimates, evidence evaluation, going concern, subsequent events and reporting conclusions require professional work.</p><h4>Conclusion</h4><p>Not a professional conclusion. This preview shows the required structure only.</p></div>`,btn('Close','close-modal')),
 'announcement':()=>openModal('Audit announcement · sample preview',`<div class="paper"><div class="watermark">ILLUSTRATIVE COMMUNICATION — NOT SENT</div><h2>${esc(C().name)}</h2><p class="mt16">Our demonstration engagement plan covers FY ${E().year}. Please provide the requested trial balance, supporting schedules and nominated contact responsibilities through the client portal.</p><p class="mt16">Engagement manager: Layla Rahman<br>Sample target: ${shortDate(E().due)}<br>Scope: ${esc(E().service)}</p><p class="mt16">This is not an engagement letter, actual notification or approved request for professional work.</p></div>`,btn('Close','close-modal')),
 'report-memo':()=>openModal('Report / service conclusion',`${note('No real audit report or opinion is issued here. The partner must make and support any professional conclusion in the production workflow.','amber')}<h3>Required report components</h3><div class="reference-block"><p>Entity, period, reporting framework, engagement type, conclusion and supporting basis, appropriate report sections, management and practitioner responsibilities, signatory and exact statement-package version.</p><p>The selected service is <b>${esc(E().service)}</b>. ${isAudit()?'External-audit reporting requires its own methodology and completion checks.':'Do not attach an external-audit opinion automatically to this service.'}</p></div>`,`${btn('Read source','source','','data-section="11"')}${btn('Close','close-modal')}`),
 'select-review':d=>{ui.selectedReview=d.id;render();},
 'respond-note':d=>{if(!roleAllowed('preparer'))return;const text=required('review-response','Response');const r=E().reviews.find(r=>r.id===d.id);if(r.status==='Cleared')throw Error('A reviewer must reopen this point before a new response.');r.history.push({status:r.status,response:r.response,version:r.version});r.response=text;r.respondedBy=actor().name;r.status='Responded';const w=E().workpapers.find(w=>w.id===r.wp);if(w){w.version++;w.status='Submitted';w.conclusion=text;r.version=w.version;}changed('Response submitted for '+r.id+'; new workpaper revision requires review');render();toast('Response saved. Switch to Senior reviewer for independent clearance.');},
 'clear-note':d=>{if(!roleAllowed('reviewer'))return;const r=E().reviews.find(r=>r.id===d.id);if(r.status!=='Responded')throw Error('A preparer response is required before clearance.');if(r.respondedBy===actor().name)throw Error('The responder cannot clear the same point.');r.status='Cleared';r.clearedBy=actor().name;const w=E().workpapers.find(w=>w.id===r.wp);if(w&&E().reviews.filter(n=>n.wp===w.id).every(n=>n.status==='Cleared'))w.status='Cleared';log('Reviewer independently cleared '+r.id+' at workpaper v'+r.version,r.id,'checkcircle');render();toast('Review point cleared for the submitted revision.');},
 'reopen-note':d=>{if(!roleAllowed(['reviewer','manager']))return;const r=E().reviews.find(r=>r.id===d.id);r.history.push({status:r.status,response:r.response,version:r.version});r.status='Open';r.response='';const w=E().workpapers.find(w=>w.id===r.wp);if(w)w.status='Changes required';changed('Review point '+r.id+' reopened');render();},
 'add-note':()=>openModal('New review point',`<div class="field-grid"><div class="field full"><label for="rn-title">Review point title *</label><input id="rn-title" class="input"></div><div class="field"><label for="rn-wp">Working paper *</label><select class="input" id="rn-wp">${E().workpapers.map(w=>`<option value="${w.id}">${w.id} · ${esc(w.title)}</option>`).join('')}</select></div><div class="field"><label for="rn-severity">Severity</label><select id="rn-severity" class="input"><option>High</option><option>Medium</option><option>Low</option></select></div><div class="field full"><label for="rn-body">Issue and required action *</label><textarea id="rn-body" class="input"></textarea></div></div>`,`${btn('Cancel','close-modal')}${btn('Create point','save-note','primary')}`),
 'save-note':()=>{if(!roleAllowed(['reviewer','manager']))return;const title=required('rn-title','Title'),body=required('rn-body','Required action'),wp=required('rn-wp','Working paper'),w=E().workpapers.find(w=>w.id===wp);const r={id:'RN-'+String(E().reviews.length+1).padStart(3,'0'),wp,title,body,severity:val('rn-severity'),status:'Open',raisedBy:actor().name,assigned:'Adam Khan',due:'2026-09-28',response:'',version:w.version,history:[]};E().reviews.push(r);w.status='Changes required';ui.selectedReview=r.id;changed('New review point '+r.id+' raised');closeModal();render();},
 'package-approve':d=>{if(!roleAllowed(d.key))return;approvalPrecheck(d.key);const e=E();e.approvals[d.key]={generation:e.generation,packageRevision:e.packageRevision,actor:actor().name,time:new Date().toISOString(),scope:d.key};log(actor().label+' approved demo package v'+e.packageRevision+' / generation '+e.generation,e.id,'shield');render();toast('Recorded for this exact demo package. No real signature created.');},
 'portal-approve':()=>{if(!roleAllowed('client'))return;approvalPrecheck('client');actions['package-approve']({key:'client'});},
 'prepare-release':async()=>{if(!roleAllowed('partner'))return;const e=E(),blockers=releaseBlockers();if(blockers.length)throw Error(blockers.join('; '));const manifest={kind:'SYNTHETIC_DEMO_NOT_FOR_PROFESSIONAL_USE',engagement:e.id,client:e.client,revision:e.packageRevision,generation:e.generation,totals:totals(e),approvals:clone(e.approvals)};const serialized=JSON.stringify(canonical(manifest));const hash=await sha256(serialized);if(e.generation!==manifest.generation||e.packageRevision!==manifest.revision)throw Error('The package changed during candidate creation. Try again.');e.candidate={manifest,serialized,hash,revision:e.packageRevision,generation:e.generation};log('Exact demo release candidate prepared',e.id,'lock');render();toast('Candidate manifest frozen and hashed locally.');},
 'issue-release':()=>{if(!roleAllowed('partner'))return;const e=E(),c=e.candidate;if(!c)throw Error('Prepare a release candidate first.');if(releaseBlockers().length||c.generation!==e.generation||c.revision!==e.packageRevision)throw Error('The release candidate is stale or its gates are incomplete.');if(e.releases.some(r=>r.hash===c.hash)){toast('That exact candidate already has a demo release event. No duplicate created.');return;}const r={id:'REL-'+String(e.releases.length+1).padStart(3,'0'),generation:c.generation,revision:c.revision,hash:c.hash,manifest:clone(c.manifest),serialized:c.serialized,totals:clone(c.manifest.totals),author:actor().name,issuedAt:new Date().toLocaleString('en-GB'),delivered:false,artifact:plainReport(e,{totals:c.manifest.totals,revision:c.revision,generation:c.generation})};e.releases.push(r);log('Demo-only release event created: '+r.id,e.id,'lock');render();toast('Demo package issued locally. No report was sent or legally signed.');},
 'demo-deliver':d=>{if(!roleAllowed(['partner','records']))return;const r=E().releases.find(r=>r.id===d.id);if(!r)return;if(r.delivered){toast('Delivery already recorded; no duplicate created.');return;}r.delivered=true;log('Delivery simulation recorded for '+r.id,r.id,'send');render();toast('Demo delivery recorded. No email or provider request was made.');},
 'released-preview':d=>{const r=E().releases.find(r=>r.id===d.id);if(r)openModal(r.id+' · preserved demo package',`<p class="caption mb16">This is a frozen preview saved at demo issuance. Working-source changes do not rewrite it.</p><iframe title="Preserved illustrative report" style="width:100%;height:65vh;border:1px solid #e1e8e6;border-radius:8px" sandbox srcdoc="${esc(r.artifact)}"></iframe>`,`${btn('Close','close-modal')}${btn('Export issued demo HTML','released-download','soft',`data-id="${r.id}"`,'download')}`,true);},
 'released-download':d=>{const r=E().releases.find(r=>r.id===d.id);if(r)download(r.artifact,E().id+'_'+r.id+'_DEMO.html','text/html');},
 'release-export':d=>{const r=E().releases.find(r=>r.id===d.id);if(r)download(JSON.stringify({...r.manifest,manifestSha256:r.hash,deliverySimulated:r.delivered},null,2),E().id+'_'+r.id+'_DEMO_manifest.json','application/json');},
 'archive':()=>{if(!roleAllowed('records'))return;const release=E().releases.filter(r=>r.delivered).at(-1);if(!release)throw Error('Record delivery of an issued demo package first.');E().archive={releaseId:release.id,hash:release.hash,createdAt:new Date().toISOString(),status:'SIMULATION_ONLY_NO_PROVIDER_PROTECTION'};log('Demo archive manifest assembled by records custodian',release.id,'archive');render();toast('Local archive simulation complete. No actual retention or immutability enforced.');},
 'client-query':()=>openModal('Ask the engagement team',`<div class="field"><label for="client-query-text">Your question *</label><textarea class="input" id="client-query-text" placeholder="Describe your question about the requested information."></textarea></div><p class="sub mt12">This records a local demo message only.</p>`,`${btn('Cancel','close-modal')}${btn('Record demo message','save-client-query','primary')}`),
 'save-client-query':()=>{const text=required('client-query-text','Question');E().clientQuery=text;log('Client demo question recorded',E().id,'message');closeModal();render();toast('Question recorded locally. No message was sent.');},
 'invoice':d=>{const i=state.invoices.find(x=>x.id===d.id);openModal(i.id+' · sample firm invoice',`${note('DEMO ONLY. This is not a request for payment, tax invoice or payment receipt.','amber')}<div class="info-grid"><div><label>Bill-to sample client</label><span>${esc(C(i.client)?.name)}</span></div><div><label>Fee milestone</label><span>${esc(i.description)}</span></div><div><label>Amount</label><span>${cash(i.amount)}</span></div><div><label>Recorded demo receipts</label><span>${cash(i.paid)}</span></div><div><label>Outstanding</label><span>${cash(i.amount-i.paid)}</span></div><div><label>Status</label>${badge(i.status)}</div></div>${i.status!=='Draft'&&i.paid<i.amount?`<div class="field mt24"><label for="receipt-amount">Illustrative receipt amount (QAR)</label><input class="input" type="number" id="receipt-amount" min="0.01" step="0.01" value="${((i.amount-i.paid)/100).toFixed(2)}"></div>`:''}`,`${btn('Close','close-modal')}${i.status==='Draft'?btn('Issue demo invoice','invoice-issue','primary',`data-id="${i.id}"`):i.paid<i.amount?btn('Record demo receipt','invoice-receipt','primary',`data-id="${i.id}"`):''}`);},
 'invoice-issue':d=>{if(!roleAllowed(['manager','partner']))return;const i=state.invoices.find(x=>x.id===d.id);i.status='Issued';log('Sample firm invoice issued locally',i.id,'receipt');closeModal();render();toast('Demo invoice state changed. Nothing was sent.');},
 'invoice-receipt':d=>{if(!roleAllowed(['manager','partner']))return;const i=state.invoices.find(x=>x.id===d.id),n=cents(required('receipt-amount','Receipt amount'));if(n<=0||n>i.amount-i.paid)throw Error('Receipt must be positive and no greater than the outstanding amount.');i.paid+=n;i.status=i.paid===i.amount?'Paid':'Partially paid';log('Synthetic receipt '+cash(n)+' recorded in firm context',i.id,'receipt');closeModal();render();toast('Sample firm receipt recorded. Client TB was not changed.');},
 'log-time':()=>openModal('Log demo time',`<div class="field-grid"><div class="field full"><label for="time-task">Work description *</label><input id="time-task" class="input" placeholder="e.g. Review fixed-asset schedule"></div><div class="field"><label for="time-hours">Hours *</label><input id="time-hours" class="input" type="number" min="0.1" max="24" step="0.1" value="1"></div><div class="field"><label>Engagement</label><span class="input">${E().id}</span></div></div>`,`${btn('Cancel','close-modal')}${btn('Save time entry','save-time','primary')}`),
 'save-time':()=>{const task=required('time-task','Description'),hours=Number(required('time-hours','Hours'));if(!Number.isFinite(hours)||hours<=0||hours>24)throw Error('Enter hours greater than 0 and no more than 24.');state.times.push({name:actor().name,task,hours,eng:E().id,date:DEMO_DATE});log('Demo time entry added',E().id,'clock');closeModal();render();},
 'continue-client':()=>{if(!roleAllowed('partner'))return;if(E().annual.confirmed.length!==6)throw Error('Review all six annual-question groups first.');E().annual.decision='Continued in demo';log('Partner recorded synthetic annual continuance');render();toast('Continuance demonstration recorded. No real assessment completed.');},
 'roll-forward':()=>{if(!roleAllowed('partner'))return;const prior=E();if(!prior.annual.decision)throw Error('Record the demo continuance decision first.');if(prior.annual.nextId){toast('The next-year draft already exists: '+prior.annual.nextId);return;}const id=nextEngId(prior.year+1),next=blankEng(id,prior.client,prior.service,prior.year+1);next.priorEngagement=prior.id;state.engagements.push(next);prior.annual.nextId=id;log('Created next-year draft '+id+' without balances, samples or sign-offs');render();toast('Created '+id+'. Its source data, terms and professional approvals are still pending.');},
 'offboarding':()=>openModal('Controlled suspension & handover',`${note('This view demonstrates an alternative lifecycle path. No client account is deleted, and no external access is actually revoked.')}<div class="service-track"><span>Authorized disposition</span>${icon('arrow')}<span>Scoped handover</span>${icon('arrow')}<span>Revoke access</span>${icon('arrow')}<span>Retain required records</span></div>${sourceBlocks(PRD.lifecycle.find(l=>l.id==='LC-26').blocks)}`,`${btn('Close','close-modal')}${btn('Read retention requirements','source','','data-section="22"')}`,true),
 'about':()=>openModal('About this product visualization',`<div class="row">${avatar('STE','','square')}<div><h3>AuditSphere · STE Practice Platform</h3><p class="sub">Interactive prototype · synthetic sample records</p></div></div><div class="reference-block"><h4>Source</h4><p>${esc(PRD.title)} — Project Requirements and End-to-End Client Lifecycle, STE-PRD-001, revision 1.0. All 27 source sections, 26 lifecycle stages and 12 primary service families are included as read-only reference text.</p><h4>Working demonstration</h4><p>Navigation, client selection, local CRM records, evidence metadata, CSV intake, basic integer-minor-unit accounting, source replacement, review responses, role simulations, stale approvals, versioned release previews, demo receipts and annual draft creation.</p><h4>Not implemented</h4><p>Production authentication, secure multi-user authorization, an operational bookkeeping ledger, statistical sampling, full compliant statement generation, real digital signatures, SharePoint/Office/Entra/Purview, email, payments, regulator filing, secure immutability or production retention.</p><h4>Storage</h4><p>State is saved in this browser’s localStorage when available. Uploaded files retain metadata and a local hash only; their bytes are not stored. Do not enter confidential information. Reset affects only this prototype’s local state.</p><h4>Design interpretation</h4><p>Branding, layout, sample people, dates and supplemental balances are illustrative. The source document’s technology requirements remain unchanged. This standalone UI is not a modification of the actual AuditSphere repository.</p></div>`,btn('Close','close-modal'),true),
 'reset':()=>openModal('Reset the local demo?',`<p class="small">This removes this prototype’s local sample changes and restores the initial scenario. It does not change your Google Doc, repository, files or Microsoft services.</p>`,`${btn('Keep changes','close-modal')}${btn('Reset demo','confirm-reset','danger')}`),
 'confirm-reset':()=>{state=seed();save();ui.selectedReview='RN-001';ui.accountTab='tb';ui.reviewTab='notes';closeModal();go('overview');toast('Demo restored to its original synthetic records.');}
};
// Current applicability requires both the input generation and package version.
isCurrent=function(key,e=E()){const a=e.approvals[key];return !!a&&a.generation===e.generation&&a.packageRevision===e.packageRevision;};
document.addEventListener('click',async event=>{
 const route=event.target.closest('[data-route]');if(route){event.preventDefault();closeModal();go(route.dataset.route);return;}
 const target=event.target.closest('[data-action]');if(!target)return;
 const fn=actions[target.dataset.action];if(!fn){toast('This action is not available in the prototype.','warn');return;}
 try{await fn(target.dataset);}catch(error){toast(error.message||'The demo action could not complete.','error');}
});
document.addEventListener('change',event=>{
 const t=event.target,e=E();
 try{
  if(t.id==='role-select'){state.role=t.value;save();render();toast('Simulated role: '+actor().label+'. No real identity changed.');return;}
  if(t.id==='eng-select'){state.selected=t.value;ui.selectedReview=E().reviews[0]?.id||'';save();render();return;}
  if(t.id==='doc-filter'){ui.docFilter=t.value;render();return;}
  if(t.dataset.change==='assessment-item'){e.questionnaire.answers[t.dataset.id]=t.checked;e.questionnaire.status='Draft';save();render();}
  if(t.dataset.change==='annual'){const n=Number(t.dataset.id);e.annual.confirmed=t.checked?[...new Set([...e.annual.confirmed,n])]:e.annual.confirmed.filter(x=>x!==n);e.annual.decision=null;save();render();}
  if(t.dataset.change==='mapping'){if(!roleAllowed('preparer')){render();return;}const r=e.rows.find(r=>r.code===t.dataset.code);r.type=t.value;e.mappingApproved=false;changed('Draft mapping changed for account '+r.code);render();}
 }catch(error){toast(error.message,'error');}
});
let filterTimer;
document.addEventListener('input',event=>{const t=event.target;if(t.id==='global-search'){updateSearch(t.value);return;}const keys={'client-filter':'clientFilter','service-filter':'serviceFilter','reference-search':'referenceSearch'};const key=keys[t.id];if(key){ui[key]=t.value;clearTimeout(filterTimer);const id=t.id,pos=t.selectionStart;filterTimer=setTimeout(()=>{render();const x=document.getElementById(id);x?.focus();try{x?.setSelectionRange(pos,pos);}catch{}},170);}});
document.addEventListener('keydown',event=>{if(event.key==='Escape'){closeModal();document.getElementById('sidebar')?.classList.remove('open');document.getElementById('mobile-overlay')?.classList.remove('open');}if(event.key==='/'&&!['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName)){event.preventDefault();searchModal();}if(event.key==='Tab'&&ui.modal){const focusable=[...document.querySelectorAll('.modal button:not(:disabled), .modal input:not(:disabled), .modal select:not(:disabled), .modal textarea:not(:disabled), .modal a[href], .modal summary')].filter(x=>x.offsetParent!==null);const first=focusable[0],last=focusable.at(-1);if(event.shiftKey&&document.activeElement===first){event.preventDefault();last?.focus();}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first?.focus();}}});
window.addEventListener('hashchange',()=>{const r=location.hash.slice(1);if(knownRoutes.includes(r)&&r!==ui.route){ui.route=r;render();}});
window.addEventListener('storage',event=>{if(event.key===STORAGE_KEY){try{const updated=JSON.parse(event.newValue);if(updated?.schema===SCHEMA){state=updated;closeModal();render();toast('Demo state changed in another tab; view refreshed.','warn');}}catch{}}});
document.getElementById('document-picker').addEventListener('change',async event=>{const file=event.target.files?.[0],target=uploadTarget,requestRole=state.role;if(!file||!target)return;try{if(file.size>5*1024*1024)throw Error('Choose a synthetic file smaller than 5 MiB.');if(!/\.(pdf|docx|xlsx|csv|txt|png|jpe?g)$/i.test(file.name))throw Error('Unsupported demonstration file type.');const bytes=await file.arrayBuffer(),sha=await sha256(bytes);if(state.role!==requestRole||!authorizedEng(target.eng))throw Error('Upload context changed; submit again in the authorized role.');const e=state.engagements.find(e=>e.id===target.eng),p=e?.pbc.find(p=>p.id===target.pbc);if(!p)throw Error('The request is no longer available.');p.history=p.history||[];if(p.file)p.history.push({file:p.file,version:p.version,status:p.status,sha:p.sha});p.file=file.name;p.size=file.size;p.sha=sha;p.version++;p.status='Received';p.local=true;e.generation++;e.candidate=null;state.selected=e.id;log('Local file metadata received for '+p.id,p.id,'folder');pbcModal(p.id);render();toast('Metadata and hash recorded locally. No file bytes uploaded or persisted.');}catch(error){toast(error.message,'error');}});
function parseCsv(text){text=text.replace(/^\uFEFF/,'');const rows=[];let row=[],cell='',quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){cell+='"';i++;}else quoted=!quoted;}else if(c===','&&!quoted){row.push(cell);cell='';}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(cell);if(row.some(x=>x.trim()))rows.push(row);row=[];cell='';}else cell+=c;}if(quoted)throw Error('CSV has an unclosed quoted field.');row.push(cell);if(row.some(x=>x.trim()))rows.push(row);return rows;}
document.getElementById('tb-picker').addEventListener('change',async event=>{const file=event.target.files?.[0],target=uploadTarget,requestRole=state.role;if(!file||!target)return;try{if(file.size>1024*1024)throw Error('CSV must be smaller than 1 MiB in this prototype.');const bytes=await file.arrayBuffer();const raw=parseCsv(new TextDecoder('utf-8',{fatal:true}).decode(bytes));const headers=raw.shift()?.map(x=>x.trim().toLowerCase())||[];for(const key of ['code','account','type','balance'])if(!headers.includes(key))throw Error('CSV must include Code,Account,Type,Balance headers.');if(!raw.length||raw.length>2000)throw Error('CSV must have 1–2,000 data rows.');const idx=Object.fromEntries(headers.map((h,i)=>[h,i]));const codes=new Set();const rows=raw.map((r,i)=>{if(r.length!==headers.length)throw Error('Wrong column count on row '+(i+2));const code=r[idx.code].trim(),name=r[idx.account].trim(),type=r[idx.type].trim().toLowerCase(),balance=cents(r[idx.balance].trim());if(!code||!name||code.length>40||name.length>160)throw Error('Invalid account identity on row '+(i+2));if(codes.has(code))throw Error('Duplicate code in this single-entity demo: '+code);codes.add(code);if(!['asset','liability','equity','revenue','expense'].includes(type))throw Error('Unsupported Type on row '+(i+2));return {code,name,type,balance};});if(rows.reduce((s,r)=>s+r.balance,0)!==0)throw Error('Rejected: debit-positive balances must sum to zero. No partial import was stored.');if(state.role!==requestRole||!authorizedEng(target.eng))throw Error('Import context changed; submit again.');const e=state.engagements.find(e=>e.id===target.eng);if(!e)throw Error('Engagement not found.');const sha=await sha256(bytes);if(state.role!==requestRole||!authorizedEng(target.eng))throw Error('Import context changed; submit again.');state.selected=e.id;if(e.receipt?.sha===sha){toast('Identical file already imported; no duplicate source version created.');return;}e.sourceHistory=e.sourceHistory||[];e.sourceHistory.push({version:e.sourceVersion,rows:clone(e.rows),receipt:e.receipt||null});e.rows=rows;e.sourceVersion++;e.sourceAccepted=true;e.mappingApproved=false;e.adjustment=0;e.journalState='Not proposed';e.sourceReflection=false;e.receipt={name:file.name,sha,size:file.size};sourceInvalidation('Synthetic CSV imported as a new source candidate');closeModal();ui.accountTab='mapping';go('accounting');toast('CSV imported locally. Reviewer mapping and source-dependent work must be reconsidered.');}catch(error){toast(error.message||'CSV import failed.','error');}});


/* Role views v2: presentation and local workflow simulation, NOT a security boundary.
   Source duties: STE-PRD-001 §§2, 5–16, 19–24. Navigation/default grants are proposed UI design. */
const ROLE_PERMISSIONS=JSON.parse(document.getElementById('permission-data').textContent);
const ROLE_ROUTES={
 overview:['My workspace','grid'],acquisition:['Acquisition & proposals','target'], 'client-summary':['Client relationships','users'],intake:['Onboarding cases','checkboard'],invitations:['Invitation requests','send'],compliance:['Restricted compliance','shield'],acceptance:['Acceptance decisions','shield'],clients:['Client portfolio','users'],engagements:['Engagements','brief'],team:['Team & deadlines','calendar'],documents:['Documents & PBC','folder'],accounting:['Accounting workbench','calculator'],audit:['Audit workpapers','checkboard'],reviews:['Review desk','message'],quality:['Independent EQR','shield'],delivery:['Report release','archive'],continuance:['Annual continuance','refresh'],renewal:['Renewal coordination','refresh'],portal:['Client home','globe'],'client-team':['Contacts & access','users'],'client-requests':['Document requests','upload'],'client-approvals':['Management approvals','checkcircle'],'client-deliverables':['Published deliverables','file'],billing:['Invoices & receipts','receipt'],'commercial-review':['Commercial review','receipt'],'commercial-requests':['Commercial requests','receipt'],records:['Records & holds','archive'],handover:['Controlled handover','folder'],administration:['User administration','users'],'access-requests':['Access requests','lock'],operations:['Integration & operations','settings'],'my-time':['My time entries','clock'],services:['Service catalogue','layers'],'role-guide':['All 14 role views','users'],privileges:['Privileges & boundaries','shield'],requirements:['Requirements reference','book']};
knownRoutes.splice(0,knownRoutes.length,...Object.keys(ROLE_ROUTES));
let roleUi={group:'All',selectedRole:null};
function initRoleState(){
 if(!ROLES[state.role])state.role='manager';
 if(!state.roleViews){
  state.roleViews={schema:2,cases:[{id:'CASE-001',client:'CL-001',checks:[true,true,true,true],stage:'Accepted',preparedBy:'Hana Ali',compliance:'Recommend acceptance',comment:'Pre-existing synthetic acceptance used by the workflow demo.',reviewedBy:'Yusuf Ahmed',decisionBy:'Daniel James',history:[]},{id:'CASE-003',client:'CL-003',checks:[true,false,true,false],stage:'Collecting',preparedBy:'Hana Ali',compliance:null,comment:'',history:[]}],tasks:[
   {id:'TASK-001',eng:'ENG-26001',title:'Update asset evidence and respond to review',owner:'preparer',due:'2026-09-23',route:'reviews'},
   {id:'TASK-002',eng:'ENG-26001',title:'Independently review fixed-asset submission',owner:'reviewer',due:'2026-09-24',route:'reviews'},
   {id:'TASK-003',eng:'ENG-26001',title:'Complete significant matters and file consistency',owner:'manager',due:'2026-09-25',route:'reviews'},
   {id:'TASK-004',eng:'ENG-26001',title:'Review the final reporting candidate',owner:'partner',due:'2026-09-28',route:'delivery'},
   {id:'TASK-005',eng:'ENG-26001',title:'Evaluate independent quality-review concerns',owner:'eqr',due:'2026-09-27',route:'quality'}
  ],proposals:[{id:'PROP-001',lead:'LD-002',title:'Summit Engineering · audit proposal',amount:1400000,preparedBy:'Mira Thomas',state:'Draft',note:'Subject to professional acceptance and signed terms.',revision:1}],accessRequests:[{id:'ACCESS-001',name:'Samir Hassan',role:'client_finance',client:'CL-001',state:'Pending verification',requestedBy:'Amal Nasser',reason:'Assist with requested bank statements.'}],users:Object.entries(ROLES).map(([id,r])=>({id,name:r.name,label:r.label,status:'Active'})),eqr:{eligible:false,concerns:[{id:'EQR-001',title:'Evaluate the depreciation judgment and corroboration',status:'Open',response:'',disposition:''}],history:[]},holds:{},handover:[],events:[],commercial:[],representations:{},queries:[],acknowledgements:{},draftChanges:[]};
 }
 state.times.forEach(t=>{if(!t.status)t.status='Submitted';});
 for(const inv of state.invoices){if(inv.status!=='Draft'&&!inv.commercialApproval)inv.commercialApproval={by:'Layla Rahman',basis:'Synthetic prior approval'};if(!inv.preparedBy)inv.preparedBy='Leila Hassan';}
 for(const e of state.engagements){e.pbc.forEach((p,i)=>{if(!p.contributor)p.contributor='Rami Nasser';});}
}
const rv=()=>state.roleViews;
const isClientRole=()=>ROLES[state.role].group==='Client';
function roleEnabled(){return rv().users.find(x=>x.id===state.role)?.status!=='Disabled';}
function permission(key,role=state.role){return !!ROLE_PERMISSIONS.find(p=>p.id===key)?.roles.includes(role);}
function visibleEngagements(){return state.engagements.filter(e=>ROLES[state.role].demoClients.includes(e.client));}
function visibleClients(){return state.clients.filter(c=>ROLES[state.role].demoClients.includes(c.id));}
function visibleInvoices(){return state.invoices.filter(i=>ROLES[state.role].demoClients.includes(i.client));}
function routeAllowed(route){return ROLES[state.role].routes.includes(route)&& (roleEnabled()||['overview','role-guide','privileges','requirements'].includes(route));}
function authorizedEng(id){return visibleEngagements().some(e=>e.id===id);}
function ensureContext(){const v=visibleEngagements();if(v.length&&!v.some(e=>e.id===state.selected))state.selected=v[0].id;}
function scopedView(fn){const raw=state;const role=ROLES[state.role];const engagements=visibleEngagements(),clients=visibleClients();const ids=new Set(engagements.map(e=>e.id));state={...state,engagements,clients,invoices:visibleInvoices(),times:state.times.filter(t=>ids.has(t.eng)),events:state.events.filter(ev=>ids.has(ev.ref)),leads:['relationship','manager','partner'].includes(state.role)?state.leads:[]};try{return fn();}finally{state=raw;}}
function addRoleEvent(text,scope=null){rv().events.unshift({text,role:state.role,actor:actor().name,scope,time:new Date().toISOString()});rv().events=rv().events.slice(0,120);save();}
function demoStatus(s){return badge(s,/not connected|pending|collecting|draft|hold|open|unverified/i.test(s)?'amber':/denied|rejected|disabled|blocked/i.test(s)?'red':/accepted|cleared|approved|complete|active|issued|recorded/i.test(s)?'green':'blue');}
function roleScope(){return `<div class="role-scope">${icon('lock')}<div><b>Visibility for ${esc(actor().label)}</b><br>${esc(ROLES[state.role].scope)}</div></div>`;}
function roleList(items,denied=false){return `<ul class="allowed-list ${denied?'denied':''}">${items.map(s=>`<li>${icon(denied?'close':'check')}<span>${esc(s)}</span></li>`).join('')}</ul>`;}
function queuePanel(title,subtitle,items){return panel(title,subtitle,items.length?`<div class="tablewrap"><table class="queue-table"><thead><tr><th>Work item</th><th>Status / due</th><th>Next action</th></tr></thead><tbody>${items.map(x=>`<tr><td><div class="queue-title">${esc(x.title)}</div><div class="queue-meta">${esc(x.meta||'Assigned synthetic record')}</div></td><td>${demoStatus(x.status||'Pending')}<div class="queue-meta">${esc(x.due||'No deadline recorded')}</div></td><td>${x.action?btn(x.label||'Open',x.action,'sm',x.extra||'','arrow'):linkBtn(x.label||'Open',x.route||'overview','sm')}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">No items in this role’s current scope.</div>');}
function personalQueue(){const r=state.role,e=visibleEngagements()[0];
 if(['preparer','reviewer','manager','partner','eqr'].includes(r))return rv().tasks.filter(t=>t.owner===r&&authorizedEng(t.eng)).map(t=>({title:t.title,meta:t.eng+' · '+actor().label,status:'Awaiting action',due:shortDate(t.due),route:t.route}));
 if(r==='relationship')return state.leads.map(l=>({title:l.name,meta:l.id+' · '+l.service,status:l.stage,route:'acquisition'}));
 if(r==='onboarding'||r==='compliance')return rv().cases.filter(c=>ROLES[r].demoClients.includes(c.client)).map(c=>({title:state.clients.find(x=>x.id===c.client)?.name,meta:c.id,status:c.stage,route:r==='compliance'?'compliance':'intake'}));
 if(r==='client_admin')return [{title:'Verify contact nominations and request ownership',meta:'Example Trading Entity only',status:rv().accessRequests.filter(x=>x.client==='CL-001'&&x.state!=='Approved in demo').length+' pending',route:'client-team'},...e.pbc.slice(0,2).map(p=>({title:p.title,meta:p.contributor,status:p.status,due:shortDate(p.due),route:'client-requests'}))];
 if(r==='client_finance')return e.pbc.filter(p=>p.contributor===actor().name).map(p=>({title:p.title,meta:p.id+' · '+e.id,status:p.status,due:shortDate(p.due),action:'pbc-detail',extra:`data-id="${p.id}"`}));
 if(r==='client')return [{title:'Review management accounts',meta:'Presented version only; not the audit opinion',status:isCurrent('client',e)?'Approved':'Awaiting reviewed package',route:'client-approvals'},{title:'Confirm management representations',meta:'Separate responsibility statement',status:rv().representations[e.id]?.generation===e.generation?'Recorded':'Pending',route:'client-approvals'}];
 if(r==='billing')return visibleInvoices().map(i=>({title:i.id,meta:state.clients.find(c=>c.id===i.client)?.name,status:i.status,due:shortDate(i.due),action:'invoice',extra:`data-id="${i.id}"`}));
 if(r==='records')return visibleEngagements().flatMap(e=>e.releases.map(r=>({title:r.id+' · '+e.id,meta:'Preserved issued version '+r.revision,status:r.delivered?'Ready for assembly':'Delivery pending',route:'records'}))).concat([{title:'Prepare controlled handover or disposition',meta:'No destructive action is performed',status:'Approval required',route:'handover'}]);
 return [{title:'Pending client access verification',meta:'No live directory connection',status:rv().accessRequests.filter(r=>r.state==='Pending verification').length+' pending',route:'access-requests'},{title:'Review integration and recovery configuration',meta:'All provider connections are disabled',status:'Not connected',route:'operations'}];
}
function roleMetrics(){const r=state.role,v=visibleEngagements(),q=personalQueue();
 const base=[['My work items',q.length,'Role-specific queue','checkboard'],['Authorized entities',visibleClients().length,'Explicit demo assignment','users'],['Role activities',ROLE_PERMISSIONS.filter(p=>p.roles.includes(r)).length,'Always subject to scope and state','shield'],['External connections',0,'No live providers connected','cloud']];
 if(r==='client_finance'){base[0]=['Assigned requests',v[0].pbc.filter(p=>p.contributor===actor().name).length,'Only your assigned requests','folder'];base[2]=['Awaiting evidence',v[0].pbc.filter(p=>p.contributor===actor().name&&p.status!=='Accepted').length,'Upload is not acceptance','upload'];}
 if(r==='client'){base[0]=['Management decisions',3,'Terms, adjustments, accounts','checkcircle'];base[2]=['Current account approval',isCurrent('client',v[0])?'Yes':'Pending','Exact package version','file'];}
 if(r==='billing'){base[0]=['Outstanding fees',cash(visibleInvoices().reduce((s,i)=>s+i.amount-i.paid,0)),'Firm books only','receipt'];base[2]=['Draft invoices',visibleInvoices().filter(i=>i.status==='Draft').length,'Independent review first','file'];}
 if(r==='admin'){base[1]=['Demo personas',14,'No real user accounts','users'];base[2]=['Professional powers',0,'Technical access is not sign-off','lock'];}
 return `<div class="metric-grid">${base.map((m,i)=>metric(...m,['','purple','amber','blue'][i])).join('')}</div>`;
}
function roleHome(){const r=ROLES[state.role];if(!roleEnabled())return head('Access suspended in this simulation','This persona was disabled by the simulated system administrator.')+note('Business actions are disabled. Switch to System administrator to review the demo user state. No Microsoft identity or real account was changed.','red')+linkBtn('Inspect role boundaries','privileges');return head(r.home,'Role-specific view · STE-PRD-001 · synthetic demonstration',btn('Browse all roles','r-role-directory','soft','','users'))+`<section class="role-banner ${isClientRole()?'client-hero':''}"><div><div class="eyebrow">${esc(r.group)} / ${esc(r.label)}</div><h2>${esc(r.tagline)}</h2><p>Welcome, ${esc(r.name)}. Your navigation, records and available decisions reflect this selected persona.</p></div>${btn('View my privileges','r-my-privileges','','','shield')}</section>`+roleMetrics()+roleScope()+`<div class="grid-main"><div class="stack">${queuePanel('Your next actions','A task does not grant approval authority.',personalQueue())}${panel('Features in this role','Proposed screen design derived from the source responsibilities.',`<div class="panel-pad" style="padding-top:0"><ul class="feature-list">${r.features.map(f=>`<li>${icon('arrow')}<span>${esc(f)}</span></li>`).join('')}</ul></div>`)}</div><div class="stack">${panel('What you may do','Within assigned scope and required gates.',`<div class="panel-pad" style="padding-top:0">${roleList(r.can)}</div>`)}${panel('Not permitted','Role switching is a demo tool, not real authorization.',`<div class="panel-pad" style="padding-top:0">${roleList(r.cannot,true)}</div>`)}<div class="panel panel-pad"><span class="eyebrow">NEXT HANDOFF</span><p class="small mt12">${esc(r.handoff)}</p><p class="policy-footnote">Source: ${esc(r.refs.join(' · '))}. Detailed navigation and default grants are proposed UI choices; firm policy must approve production privileges.</p></div></div></div>`+footer('§2 · GOV-01–04 · §21');}
function roleDirectory(){const groups=['All','Commercial','Professional','Client','Operations'];const list=Object.entries(ROLES).filter(([k,r])=>roleUi.group==='All'||r.group===roleUi.group);return head('Every role. One connected practice.','Explore all 14 source roles and their separate responsibilities.',btn('Download role catalogue','r-export-catalog','soft','','download'))+note('Design explorer only. The role switcher lets a presenter simulate another user. A production user must never grant themselves access this way.','amber')+`<div class="profile-tabs">${groups.map(g=>`<button data-action="r-filter-group" data-group="${g}" class="${roleUi.group===g?'active':''}">${g}${g==='All'?' · 14':''}</button>`).join('')}</div><div class="role-card-grid">${list.map(([k,r])=>`<section class="role-card ${k===state.role?'active':''}"><div class="between">${avatar(r.initials,r.group==='Client'?'blue':r.group==='Operations'?'amber':r.group==='Commercial'?'purple':'','square')}<span class="tag gray">${r.group}</span></div><h3>${esc(r.label)}</h3><p>${esc(r.tagline)}</p><small>${r.features.length} feature areas · ${ROLE_PERMISSIONS.filter(p=>p.roles.includes(k)).length} scoped activities</small><div class="row wrap">${btn('Enter role view','r-switch-role','primary sm',`data-role="${k}"`)}${btn('Features & privileges','r-role-detail','sm',`data-role="${k}"`)}</div></section>`).join('')}</div>`+footer('§2.1 · Fourteen source roles');}
function privilegePage(){const r=ROLES[state.role];return head('Privileges are not a hierarchy.','A higher title does not automatically inherit another person’s professional authority.',btn('Export permission matrix','r-export-permissions','soft','','download'))+roleScope()+`<div class="grid2 mb16">${panel('Allowed for '+esc(r.label),'Subject to individual assignment and action prerequisites.',`<div class="panel-pad" style="padding-top:0">${roleList(r.can)}</div>`)}${panel('Explicit boundaries','These restrictions are not removable by completing a task.',`<div class="panel-pad" style="padding-top:0">${roleList(r.cannot,true)}</div>`)}</div>`+note('The matrix is a proposed least-privilege UI policy derived from STE-PRD-001. “Scoped” is eligibility, not an unconditional grant; read each condition. The frontend simulates selected actions only. Backend enforcement is not implemented.')+panel('Activity × role matrix','44 defined activity families; scroll horizontally to compare all fourteen roles.',`<div class="tablewrap"><table class="priv-table"><thead><tr><th>Activity and condition</th>${Object.values(ROLES).map(r=>`<th>${esc(r.label)}</th>`).join('')}</tr></thead><tbody>${ROLE_PERMISSIONS.map(p=>`<tr><td><b>${esc(p.label)}</b><div class="action-note">${esc(p.condition)}</div></td>${Object.keys(ROLES).map(k=>`<td class="${p.roles.includes(k)?'priv-yes':'priv-no'}">${p.roles.includes(k)?'Scoped':'—'}</td>`).join('')}</tr>`).join('')}</tbody></table></div>`)+footer('§2 · §5 · LC-01–26');}
function safeClientSummary(){return head('Client relationships','Commercial/factual summaries only. Internal financial and review data is not exposed.')+roleScope()+`<div class="grid2">${visibleClients().map(c=>panel(esc(c.name),c.id,`<div class="panel-pad" style="padding-top:0"><div class="info-grid"><div><label>Sector</label><span>${esc(c.industry)}</span></div><div><label>Contact</label><span>${esc(c.contact)}</span></div><div><label>Relationship</label>${demoStatus(c.status)}</div></div><p class="sub mt16">Legal entity, permitted service and contact information. This summary does not reveal TB balances, materiality or private working papers.</p></div>`)).join('')}</div>`+footer('LC-01–09 · Client 360');}
function intakePage(){const list=rv().cases.filter(c=>ROLES[state.role].demoClients.includes(c.client));return head('Collection before verification','Coordinate identity information, supporting documents and signatory requests.')+roleScope()+queuePanel('Onboarding cases','Separate coordinator collection, compliance review and partner acceptance.',list.map(c=>({title:state.clients.find(x=>x.id===c.client)?.name,meta:c.id+' · '+c.checks.filter(Boolean).length+'/4 collection checks',status:c.stage,action:'r-intake-open',extra:`data-id="${c.id}"`,label:'Review collection'})))+`<div class="grid2 mt20">${panel('Invitation requests','Prepared for verification; not automatically active accounts.',`<div class="panel-pad" style="padding-top:0"><p class="small muted">Collect the requested contact, entity and intended role. Verify signatory authority separately from contactability.</p><div class="mt16">${linkBtn('View invitation register','invitations')}</div></div>`)}${panel('No automatic acceptance','Received → checked → decided.',`<div class="panel-pad" style="padding-top:0">${roleList(ROLES.onboarding.cannot,true)}</div>`)}</div>`+footer('LC-04 · LC-06–07');}
function compliancePage(){const list=rv().cases.filter(c=>ROLES[state.role].demoClients.includes(c.client));return head('Restricted compliance review','Keep verification, professional acceptance and client communication separate.')+note('All cases are fictional. No identity, sanctions, PEP or regulatory lookup is performed. No report is transmitted to any authority.','amber')+roleScope()+queuePanel('Assigned review cases','Only explicitly scoped cases appear.',list.map(c=>({title:state.clients.find(x=>x.id===c.client)?.name,meta:c.id+' · prepared by '+c.preparedBy,status:c.stage,action:'r-compliance-open',extra:`data-id="${c.id}"`,label:'Open restricted case'})))+footer('LC-04 · §7 · §17.7');}
function acceptancePage(){return head('Partner acceptance decisions','Compliance clearance supports—but never replaces—the partner’s decision.')+roleScope()+queuePanel('Permitted decision queue','Restricted case detail remains in the compliance workspace.',rv().cases.filter(c=>ROLES[state.role].demoClients.includes(c.client)).map(c=>({title:state.clients.find(x=>x.id===c.client)?.name,meta:c.id+' · clearance outcome: '+(c.compliance||'Not reviewed'),status:c.stage,action:'r-partner-case',extra:`data-id="${c.id}"`,label:'Decide scope'})))+footer('G01–02 · LC-04');}
function teamPage(){return head('Responsibilities and due dates','A task owner is accountable for the next action—not entitled to bypass a review gate.')+roleScope()+queuePanel('Engagement task ownership','Changes are recorded locally with a reason.',rv().tasks.filter(t=>authorizedEng(t.eng)).map(t=>({title:t.title,meta:t.id+' · '+t.eng+' · '+ROLES[t.owner].name,status:'Assigned',due:shortDate(t.due),action:'r-task-open',extra:`data-id="${t.id}"`,label:'Assign / schedule'})))+queuePanel('Time awaiting review','A manager cannot independently approve their own time.',state.times.filter(t=>authorizedEng(t.eng)).map((t,i)=>({title:t.task,meta:t.name+' · '+t.hours+' hours · '+t.eng,status:t.status||'Submitted',action:'r-time-review',extra:`data-index="${state.times.indexOf(t)}"`,label:'Review time'})))+footer('LC-09 · GOV-04 · BIL-01');}
function renewalPage(){return head('Renewal coordination','Prepare the next-period assessment without copying old decisions.')+roleScope()+queuePanel('Relationship follow-up','Partner continuance is a separate decision.',visibleEngagements().map(e=>({title:state.clients.find(c=>c.id===e.client)?.name,meta:e.id+' → FY '+(e.year+1),status:e.annual.decision?'Demo continued':'Reassessment required',action:'r-renewal-note',extra:`data-id="${e.id}"`,label:'Record follow-up'})))+note('Coordination does not renew acceptance. Current evidence, independence, capacity, scope and terms must be reassessed by the authorized roles.')+footer('LC-08 · LC-25');}
function clientContext(){const e=E();return `<div class="contextbar"><div class="context-item"><label>Authorized entity</label>${esc(C().name)}</div><div class="context-item"><label>Engagement</label>${esc(e.id)}</div><div class="context-item"><label>Period</label>FY ${e.year}</div><span class="tag client-scope-badge">Client-visible records only</span></div>`;}
function clientRequests(){const e=E();let list=e.pbc;if(state.role==='client_finance')list=list.filter(p=>p.contributor===actor().name);return head(state.role==='client_admin'?'Coordinate your evidence requests':'Your document requests','A file submission is received first and accepted by the firm separately.')+clientContext()+queuePanel('Prepared by client','Uploads store local metadata/hash only; no document bytes are retained.',list.map(p=>({title:p.title,meta:p.id+' · '+p.contributor,status:p.status,due:shortDate(p.due),action:state.role==='client_admin'?'r-pbc-assign':'pbc-detail',extra:`data-id="${p.id}"`,label:state.role==='client_admin'?'Assign owner':p.file?'View / replace':'Supply evidence'})))+`<div class="panel panel-pad mt20"><h3>Client-facing clarification</h3><p class="sub mt8">Private reviewer discussions are not included in this view.</p><div class="mt16">${btn('Ask a question','client-query','soft','','message')}</div>${rv().queries.filter(q=>q.eng===e.id).map(q=>`<div class="comment"><b class="small">${esc(q.by)}</b><p>${esc(q.text)}</p><p class="caption">Local message only · ${esc(q.status)}</p></div>`).join('')}</div>`+footer('LC-11–12');}
function clientPortalHome(){const e=E();return head(ROLES[state.role].home,'Your authorized client entity, requests and decisions in one place.')+clientContext()+`<div class="role-banner client-hero"><div><div class="eyebrow">${esc(C().name)} / ${esc(actor().label)}</div><h2>Hello, ${esc(actor().name.split(' ')[0])}.</h2><p>${esc(ROLES[state.role].tagline)}</p></div>${btn('View my privileges','r-my-privileges','','','shield')}</div>`+roleMetrics()+`<div class="grid-main"><div class="stack">${queuePanel('Your next actions','Only the information permitted for your client role appears.',personalQueue())}${panel('Engagement team','Ask a factual question; no message is sent outside this browser.',`<div class="panel-pad" style="padding-top:0"><div class="row">${avatar('LR','purple')}<div><h3>Layla Rahman</h3><p class="sub">Engagement manager · synthetic contact</p></div></div><div class="mt16">${btn('Ask the team','client-query','soft sm','','message')}</div></div>`)}</div><div class="stack">${panel('Your boundaries','Your client role does not become an auditor role.',`<div class="panel-pad" style="padding-top:0">${roleList(ROLES[state.role].cannot,true)}</div>`)}${panel('Published material','No private workpapers or firm-only economics.',`<div class="panel-pad" style="padding-top:0"><p class="sub">Final delivered packages appear only after the firm’s release workflow.</p><div class="mt12">${linkBtn('Your deliverables','client-deliverables')}</div></div>`)}</div></div>`+footer('LC-07 · LC-11–12 · LC-22');}
function clientTeam(){return head('Your contacts and responsibilities','Nominate people within your authorized entity. The firm verifies access and signatory authority.',btn('Nominate contact','r-contact-new','primary','','plus'))+clientContext()+queuePanel('Contact requests','Requested roles do not become active permissions automatically.',rv().accessRequests.filter(r=>r.client==='CL-001').map(r=>({title:r.name,meta:r.id+' · '+ROLES[r.role]?.label,status:r.state,action:'r-contact-detail',extra:`data-id="${r.id}"`,label:'View request'})))+`<div class="grid2 mt20">${panel('Permitted nominations','An assigned client administrator cannot widen entity scope.',`<div class="panel-pad" style="padding-top:0">${roleList(['Client finance contributor for existing authorized entities','Client signatory nomination, subject to separate authority verification','Removal request with an effective scope and reason'])}</div>`)}${panel('Never granted here','No staff or professional roles.',`<div class="panel-pad" style="padding-top:0">${roleList(ROLES.client_admin.cannot,true)}</div>`)}</div>`+footer('§2.1 · LC-07 · GOV-04');}
function clientApprovals(){const e=E(),ready=isCurrent('manager');return head('Your management decisions','Approval belongs to the displayed version and your verified authority.')+clientContext()+note('This is a browser simulation. These decisions do not create legally signed documents, post journals, or approve an independent audit opinion.','amber')+`<div class="grid2">${panel('1. Engagement terms','Scope and responsibilities; separate from account approval.',`<div class="panel-pad" style="padding-top:0">${demoStatus(e.terms?'Demo terms recorded':'Pending')}<p class="sub mt12">Entity ${esc(C().name)} · FY ${e.year} · ${esc(e.service)}. Actual terms must identify scope, fees, responsibilities and signatory evidence.</p><div class="mt16">${btn('Review and accept demo terms','r-client-terms','soft sm')}</div></div>`)}${panel('2. Proposed adjustment','Technical review precedes management authorization.',`<div class="panel-pad" style="padding-top:0"><h3>${cash(e.adjustment||50000)} · depreciation</h3><p class="sub mt12">Reporting-layer proposal only. Rejected adjustments remain available for professional evaluation.</p><div class="mt12">${demoStatus(e.journalState)}</div><div class="row wrap mt16">${btn('Review management decision','r-journal-consent','soft sm')}</div></div>`)}${panel('3. Financial statements','Manager-cleared exact draft; no internal review history.',`<div class="panel-pad" style="padding-top:0"><h3>Package v${e.packageRevision}</h3><p class="sub mt12">${ready?'An internally reviewed version is available.':'The firm has not yet made a current manager-reviewed draft available.'}</p><div class="mt12">${demoStatus(isCurrent('client')?'Management approval current':'Management approval pending')}</div><div class="row wrap mt16">${ready?btn('Preview presented draft','preview-report','soft sm','','eye'):''}${btn('Approve management accounts','r-management-approve','primary sm',ready?'':'disabled')}</div></div>`)}${panel('4. Representations','Recorded separately from approving the statements.',`<div class="panel-pad" style="padding-top:0"><p class="sub">Confirm the management responsibilities and statement coverage requested by the engagement team. This demonstration is not a full legal representation letter.</p><div class="mt12">${demoStatus(rv().representations[e.id]?.generation===e.generation?'Current demo attestation':'Not recorded / stale')}</div><div class="mt16">${btn('Record responsibility attestation','r-representation','soft sm')}</div></div>`)}</div>`+footer('LC-06 · LC-17 · LC-22 · GOV-03');}
function clientDeliverables(){const e=E();return head('Your published deliverables','Only delivered client packages—not internal working papers.')+clientContext()+queuePanel('Delivered packages','A delivery acknowledgement is distinct from management approval.',e.releases.filter(r=>r.delivered).map(r=>({title:r.id+' · Financial package v'+r.revision,meta:'FY '+e.year+' · preserved issued preview',status:rv().acknowledgements[e.id+'/'+r.id]?'Acknowledged':'Available',action:'released-preview',extra:`data-id="${r.id}"`,label:'View package'})))+`<div class="row wrap mt16">${e.releases.filter(r=>r.delivered).map(r=>btn('Acknowledge '+r.id,'r-ack-delivery','soft sm',`data-id="${r.id}"`)).join('')}</div>`+panel('Client commercial invoices','Only invoices addressed to this client; no firm margin or staff costs.',`<div class="tablewrap"><table><thead><tr><th>Reference</th><th>Service fee</th><th>Outstanding</th><th>Status</th></tr></thead><tbody>${visibleInvoices().filter(i=>i.client===e.client).map(i=>`<tr><td>${i.id}<div class="cell-sub">${esc(i.description)}</div></td><td>${cash(i.amount)}</td><td>${cash(i.amount-i.paid)}</td><td>${demoStatus(i.status)}</td></tr>`).join('')}</tbody></table></div>`)+footer('LC-24 · BIL-01–04 · UX-03');}
function qualityPage(){const e=E(),q=rv().eqr;return head('Independent engagement quality review','Selected significant judgments and exact versions—not a second preparation team.')+roleScope()+context()+note('Eligibility confirmation and completion are simulated. The production firm must establish independence, competence, scope and appropriate timing.','amber')+`<div class="grid-main"><div class="stack">${panel('Significant judgments selected for review','Read-only evidence summary for this assigned engagement.',`<div class="panel-pad" style="padding-top:0">${e.workpapers.filter(w=>['WP-C1','WP-F1'].includes(w.id)).map(w=>`<div class="borderbox mb16"><div class="between"><h3>${esc(w.title)}</h3>${demoStatus(w.status)}</div><p class="sub mt12">${esc(w.conclusion)}</p><p class="caption mt12">${w.id} · version ${w.version} · source generation ${e.generation}</p></div>`).join('')}</div>`)}${queuePanel('EQR concerns','A team response is not automatic clearance.',q.concerns.map(c=>({title:c.title,meta:c.id,status:c.status,action:'r-eqr-concern',extra:`data-id="${c.id}"`,label:'Evaluate concern'})))}<div class="row">${btn('Raise a concern','r-eqr-add','soft','','plus')}</div></div><div class="stack">${panel('Reviewer eligibility','Must be established outside a simple role selection.',`<div class="panel-pad" style="padding-top:0">${demoStatus(q.eligible?'Demo eligibility recorded':'Eligibility pending')}<div class="mt16">${btn('Record eligibility declaration','r-eqr-eligibility','soft sm')}</div></div>`)}${panel('Completion','Independent completion does not replace the partner.',`<div class="panel-pad" style="padding-top:0"><h3>Package v${e.packageRevision}</h3><div class="mt12">${demoStatus(isCurrent('eqr')?'Current EQR completion':'Not complete / stale')}</div><p class="sub mt12">Open concerns, missing management review or changed dependencies block completion.</p><div class="mt16">${btn('Complete EQR for this version','r-eqr-complete','primary sm')}</div></div>`)}</div></div>`+footer('LC-23 · G09');}
function billingPage(){return head('Firm billing and collections','Client fees belong to the firm’s ledger—not the client reporting TB.',btn('Prepare invoice draft','r-new-invoice','primary','','plus'))+roleScope()+queuePanel('Invoice register','Issue only a reviewed current commercial draft.',visibleInvoices().map(i=>({title:i.id+' · '+cash(i.amount),meta:state.clients.find(c=>c.id===i.client)?.name+' · '+i.description,status:i.status,due:shortDate(i.due),action:'invoice',extra:`data-id="${i.id}"`,label:'Open invoice'})))+`<div class="grid2 mt20">${panel('Commercial separation','Receipts and credits are not professional sign-offs.',`<div class="panel-pad" style="padding-top:0">${roleList(ROLES.billing.cannot,true)}</div>`)}${panel('Credits and exceptions','Separate authorization is required.',`<div class="panel-pad" style="padding-top:0"><p class="sub">Prepare a scoped credit, write-off or refund request. No money is moved and no ledger is posted by this prototype.</p><div class="mt16">${btn('Prepare exception request','r-commercial-new','soft sm')}</div></div>`)}</div>`+footer('BIL-01–04 · DAT-04');}
function commercialReview(){const inv=visibleInvoices().filter(i=>i.status==='Submitted for review');const props=rv().proposals.filter(p=>p.state==='Submitted');return head('Commercial review','Independent fee decisions remain separate from professional report authorization.')+queuePanel('Invoice drafts awaiting review','Only drafts prepared by a different individual are eligible.',inv.map(i=>({title:i.id+' · '+cash(i.amount),meta:i.description+' · '+i.preparedBy,status:i.status,action:'r-review-invoice',extra:`data-id="${i.id}"`,label:'Review'})))+`<div class="mt20">${queuePanel('Proposal drafts','An approved proposal does not accept the client.',props.map(p=>({title:p.title,meta:p.id+' · '+cash(p.amount),status:p.state,action:'r-review-proposal',extra:`data-id="${p.id}"`,label:'Review'})))}</div>`+footer('LC-05 · BIL-02');}
function commercialRequests(){return head('Commercial approval requests','Draft, review and issue remain separately recorded.',btn('New exception request','r-commercial-new','soft','','plus'))+queuePanel('Pending exceptions','Requests do not reduce balances automatically.',rv().commercial.map(c=>({title:c.kind+' · '+c.invoice,meta:c.reason,status:c.state,route:'billing',label:'Invoice register'})))+footer('BIL-02');}
function recordsPage(){const released=visibleEngagements().flatMap(e=>e.releases.map(r=>({e,r})));return head('Records, preservation and holds','Only authorized issued artifacts enter assembly. Working papers are not edited here.')+roleScope()+note('Archive flags and hold records are demonstrations. No retention policy, SharePoint lock or legal hold is actually applied.','amber')+queuePanel('Issued packages','Select a preserved package; never substitute a current working file.',released.map(({e,r})=>({title:r.id+' · '+e.id,meta:'Package v'+r.revision+' · '+(state.clients.find(c=>c.id===e.client)?.name),status:e.archive?.releaseId===r.id?'Demo assembly recorded':r.delivered?'Delivered':'Dispatch pending',action:'r-record-open',extra:`data-eng="${e.id}" data-id="${r.id}"`,label:'Inspect record'})))+`<div class="mt20">${queuePanel('Hold instructions','A hold prevents the simulated disposition request.',visibleEngagements().map(e=>({title:state.clients.find(c=>c.id===e.client)?.name,meta:e.id,status:rv().holds[e.id]?'Active demo hold':'No recorded hold',action:'r-hold-open',extra:`data-eng="${e.id}"`,label:'Hold / disposition'})))}</div>`+footer('LC-25–26 · G11–12');}
function handoverPage(){return head('Controlled handover','Prepare a request with scope, recipient and reason. No files are sent or deleted.',btn('Prepare handover request','r-handover-new','primary','','plus'))+roleScope()+queuePanel('Handover requests','Client-owned records and firm working papers require distinct authority.',rv().handover.filter(h=>authorizedEng(h.eng)).map(h=>({title:h.id+' · '+h.eng,meta:h.recipient+' · '+h.scope,status:h.status,route:'records',label:'Records queue'})))+note('Termination does not authorize immediate deletion. Approval, legal holds, preserved history, access revocation and delivery evidence remain separate obligations.')+footer('LC-26 · G12');}
function invitationPage(){return head('Client invitation requests','Onboarding prepares access; system administration executes only approved scoped instructions.',btn('Prepare invitation request','r-invitation-new','soft','','plus'))+queuePanel('Request register','Pending requests do not grant access.',rv().accessRequests.filter(r=>ROLES[state.role].demoClients.includes(r.client)).map(r=>({title:r.name,meta:r.id+' · '+ROLES[r.role]?.label,status:r.state,action:'r-contact-detail',extra:`data-id="${r.id}"`,label:'View request'})))+footer('LC-07 · GOV-04');}
function adminPage(){return head('Platform user administration','Operate the system without assuming a professional role.')+note('These are synthetic personas, not real directory accounts. Technical administration has no automatic access to financial content or report approval.','amber')+`<div class="metric-grid">${metric('Demo personas',14,'From the 14 source roles','users')}${metric('Pending access',rv().accessRequests.filter(r=>r.state==='Pending verification').length,'Separate authority verification','lock','purple')}${metric('Live directories',0,'Entra is not connected','cloud','blue')}${metric('Professional sign-off',0,'Not granted to administrators','shield','amber')}</div>`+queuePanel('User states','Disabling a demo persona blocks its frontend business actions only.',rv().users.map(u=>({title:u.name,meta:u.label,status:u.status,action:'r-user-open',extra:`data-id="${u.id}"`,label:'Review state'})))+footer('§2.1 · GOV-04 · NFR-02');}
function accessRequestsPage(){return head('Approved access operations','Verify entity scope and signing authority before applying a requested role.')+queuePanel('Pending verification','Client-role nominations never become firm staff grants.',rv().accessRequests.map(r=>({title:r.name,meta:r.id+' · '+ROLES[r.role]?.label,status:r.state,action:'r-access-verify',extra:`data-id="${r.id}"`,label:'Verify request'})))+note('The prototype does not provision accounts or expand the 14 fixed role personas. Approval records here illustrate the handoff only.')+footer('LC-07 · GOV-04 · NFR-01–02');}
function operationsPage(){const integrations=[['Microsoft Entra ID','Interactive identity and session lifecycle'],['SharePoint / Microsoft Graph','Selected-resource documents and permission mapping'],['Microsoft 365','Authenticated human document editing'],['Purview / records','Approved retention and observed record controls'],['Email / signing / filing','Separately authorized channels and artifacts'],['Backup and restore','Cross-store recovery and outward-action quarantine']];return head('Integration and operations','Configuration examples, health placeholders and safe recovery boundaries.',btn('Prepare configuration change','r-config-new','soft','','settings'))+note('Every connection is OFF. A configuration draft does not connect an account, create credentials or make a provider request.','amber')+panel('External capability status','No credentials are requested or stored.',`<div class="config-list">${integrations.map(([name,desc])=>`<div class="config-row"><div><h4>${name}</h4><p>${desc}</p></div>${demoStatus('Not connected')}</div>`).join('')}</div>`)+`<div class="mt20">${queuePanel('Configuration change requests','Separate review and environment-specific authorization are required.',rv().draftChanges.map(c=>({title:c.title,meta:c.reason,status:'Draft — not applied',action:'r-config-detail',extra:`data-id="${c.id}"`,label:'View draft'})))}</div>`+panel('Recovery rule','No blind replay of restored operations.',`<div class="panel-pad" style="padding-top:0"><p class="small muted">Pause outward actions, restore records, reconcile provider state and permissions, verify issued-package identities, then obtain authority before resuming. This screen does not execute a restore.</p></div>`)+footer('ARC-11–13 · NFR-04–09');}
function myTimePage(){return head('My time entries','Record your own effort. Independent review is a separate action.',btn('Record time','log-time','primary','','plus'))+queuePanel('Your submitted time','No self-approval.',state.times.filter(t=>t.name===actor().name&&authorizedEng(t.eng)).map(t=>({title:t.task,meta:t.eng+' · '+t.hours+' hours',status:t.status||'Submitted',due:t.date,route:'overview',label:'My workspace'})))+footer('BIL-01–02');}
let pendingRoleCommand=null;
function textField(id,label,placeholder='',value=''){return `<div class="field mt16"><label for="${id}">${esc(label)} *</label><textarea id="${id}" class="input" placeholder="${esc(placeholder)}">${esc(value)}</textarea></div>`;}
function checkField(id,label,checked=false){return `<label class="checkline"><input type="checkbox" id="${id}" ${checked?'checked':''}><span>${esc(label)}</span></label>`;}
function showCommand(title,body,capability,run,label='Record demo action'){pendingRoleCommand={role:state.role,capability,run,selected:state.selected};openModal(title,note('Local demonstration only. No real user, financial transaction, document provider or professional signature is changed.','amber')+body,`${btn('Cancel','close-modal')}${btn(label,'r-confirm','primary')}`,true);}
function reqText(id){return required(id,'Required explanation');}
function checked(id){return !!document.getElementById(id)?.checked;}
function getCase(id){const c=rv().cases.find(x=>x.id===id&&ROLES[state.role].demoClients.includes(x.client));if(!c)throw Error('The requested record is unavailable.');return c;}
function scopedInvoice(id){const i=visibleInvoices().find(x=>x.id===id);if(!i)throw Error('The requested record is unavailable.');return i;}
function requestedContact(id){const r=rv().accessRequests.find(x=>x.id===id);if(!r||(state.role!=='admin'&&!ROLES[state.role].demoClients.includes(r.client)))throw Error('The requested record is unavailable.');return r;}
function roleDetail(k){const r=ROLES[k];if(!r)return;openModal(r.label,`<p class="sub">Source-defined role; proposed screen/grant design.</p><h3 class="mt16">${esc(r.home)}</h3><p class="small mt12">${esc(r.scope)}</p><div class="grid2 mt20"><div><h4 class="mb16">Features and functionalities</h4><ul class="feature-list">${r.features.map(f=>`<li>${icon('arrow')}${esc(f)}</li>`).join('')}</ul><h4 class="mt20 mb16">Eligible actions</h4>${roleList(r.can)}</div><div><h4 class="mb16">Explicit restrictions</h4>${roleList(r.cannot,true)}<h4 class="mt20 mb16">Workflow handoff</h4><p class="small muted">${esc(r.handoff)}</p></div></div><p class="policy-footnote">Source ${esc(r.refs.join(' · '))}. This role has ${ROLE_PERMISSIONS.filter(p=>p.roles.includes(k)).length} proposed scoped activity families. Individual authority, ownership and workflow gates remain mandatory.</p>`,`${btn('Close','close-modal')}${btn('Enter this role view','r-switch-role','primary',`data-role="${k}"`)}`,true);}
function switchRole(k){if(!ROLES[k])return;closeModal();pendingRoleCommand=null;uploadTarget=null;state.role=k;ensureContext();ui.selectedReview=E()?.reviews[0]?.id||'';ui.route='overview';location.hash='overview';save();render();toast('Simulated persona: '+ROLES[k].label+'. No real identity changed.');}
function invoiceDialog(id){const i=scopedInvoice(id);let foot=btn('Close','close-modal');if(i.status==='Draft')foot+=btn('Submit for independent review','r-invoice-submit','primary',`data-id="${i.id}"`);if(i.status==='Approved for issue')foot+=btn('Issue approved demo invoice','invoice-issue','primary',`data-id="${i.id}"`);if(['Issued','Partially paid'].includes(i.status))foot+=btn('Record demo receipt','invoice-receipt','primary',`data-id="${i.id}"`);openModal(i.id+' · firm billing only',note('Synthetic commercial record. Not a payment request, tax invoice or bank receipt.','amber')+`<div class="info-grid"><div><label>Client</label><span>${esc(state.clients.find(c=>c.id===i.client)?.name)}</span></div><div><label>Amount</label><span>${cash(i.amount)}</span></div><div><label>Outstanding</label><span>${cash(i.amount-i.paid)}</span></div></div><p class="sub mt16">${esc(i.description)}</p><div class="mt12">${demoStatus(i.status)}</div><p class="sub mt12">Preparer: ${esc(i.preparedBy)} · Reviewer: ${esc(i.commercialApproval?.by||'Pending')}</p>${['Issued','Partially paid'].includes(i.status)?`<div class="field mt16"><label for="receipt-amount">Demo receipt amount *</label><input class="input" id="receipt-amount" value="${((i.amount-i.paid)/100).toFixed(2)}" inputmode="decimal"></div>`:''}`,foot);}
Object.assign(actions,{
 'r-role-directory':()=>go('role-guide'),'r-my-privileges':()=>go('privileges'),
 'r-filter-group':d=>{roleUi.group=d.group;render();},'r-switch-role':d=>switchRole(d.role),'r-role-detail':d=>roleDetail(d.role),
 'r-export-catalog':()=>download(JSON.stringify({source:'STE-PRD-001 revision 1.0',status:'PROPOSED_ROLE_UI_NOT_PRODUCTION_AUTHORIZATION',roles:ROLES},null,2),'AuditSphere_role_catalogue.json','application/json'),
 'r-export-permissions':()=>download(JSON.stringify({status:'PROPOSED_SCOPED_ACTIVITY_POLICY',permissions:ROLE_PERMISSIONS},null,2),'AuditSphere_role_privileges.json','application/json'),
 'r-confirm':()=>{const c=pendingRoleCommand;if(!c||c.role!==state.role||c.selected!==state.selected||!permission(c.capability)||!roleEnabled())throw Error('This action is unavailable. Reopen it in an authorized role.');c.run();save();pendingRoleCommand=null;closeModal();render();toast('Recorded in the local demonstration only.');},
 'r-intake-open':d=>{const c=getCase(d.id);showCommand(c.id+' · collection checklist',`<p class="small">This is the coordinator’s receipt checklist, not independent verification.</p>${['Entity profile supplied','Ownership/director evidence received','Contact and signatory information received','Scope and terms information collected'].map((label,i)=>checkField('intake-'+i,label,c.checks[i])).join('')}<div class="field mt16"><label for="intake-next">Requested transition</label><select class="input" id="intake-next"><option value="draft">Save collection draft</option><option value="submit">Submit to compliance</option></select></div>`,'intake.write',()=>{const vals=[0,1,2,3].map(i=>checked('intake-'+i));const submit=val('intake-next')==='submit';if(submit&&!vals.every(Boolean))throw Error('Complete all required collection checks before submission.');c.history.push({stage:c.stage,actor:actor().name,time:new Date().toISOString()});c.checks=vals;c.preparedBy=actor().name;c.stage=submit?'Awaiting compliance':'Collecting';c.compliance=null;c.decisionBy=null;addRoleEvent('Intake '+c.id+' '+(submit?'submitted':'saved'),c.client);},'Save / submit checklist');},
 'r-compliance-open':d=>{const c=getCase(d.id);if(!['Awaiting compliance','Compliance hold'].includes(c.stage)){openModal(c.id+' · compliance state',`<p class="small">Current state: ${esc(c.stage)}. An independent review needs a submitted collection file. Existing decisions remain historical.</p>${c.comment?`<p class="sub mt16">${esc(c.comment)}</p>`:''}`,btn('Close','close-modal'));return;}showCommand(c.id+' · restricted disposition',`<p class="small">Prepared by ${esc(c.preparedBy)}. Checks below attest only to the synthetic scenario.</p>${checkField('comp-identity','Identity and ownership evidence reviewed')}${checkField('comp-conflicts','Applicable conflicts and independence concerns considered')}${checkField('comp-open','No unresolved mandatory information/prohibition for an acceptance recommendation')}<div class="field mt16"><label for="comp-decision">Disposition</label><select class="input" id="comp-decision"><option>Compliance hold</option><option>Recommend acceptance</option><option>Recommend decline</option></select></div>${textField('comp-reason','Evidence-based rationale')}`,'compliance.decide',()=>{const decision=val('comp-decision'),reason=reqText('comp-reason');if(c.preparedBy===actor().name)throw Error('An independent checker is required.');if(decision==='Recommend acceptance'&&![checked('comp-identity'),checked('comp-conflicts'),checked('comp-open')].every(Boolean))throw Error('An acceptance recommendation needs all mandatory review checks.');c.compliance=decision;c.comment=reason;c.reviewedBy=actor().name;c.stage=decision==='Compliance hold'?'Compliance hold':'Awaiting partner';c.history.push({decision,reason,actor:actor().name,time:new Date().toISOString()});addRoleEvent('Compliance disposition recorded for '+c.id,c.client);});},
 'r-partner-case':d=>{const c=getCase(d.id);showCommand(c.id+' · partner decision',`<p class="small">Clearance outcome: ${esc(c.compliance||'Pending')}. This view does not disclose restricted case documents.</p><div class="field mt16"><label for="accept-decision">Decision</label><select class="input" id="accept-decision"><option>Hold</option><option>Accept</option><option>Decline</option></select></div>${textField('accept-reason','Decision rationale and permitted scope')}`,'acceptance.decide',()=>{const decision=val('accept-decision'),reason=reqText('accept-reason');if(decision==='Accept'&&(c.compliance!=='Recommend acceptance'||c.stage!=='Awaiting partner'||!c.checks.every(Boolean)))throw Error('Current independent clearances and a submitted partner decision are required.');if(c.preparedBy===actor().name||c.reviewedBy===actor().name)throw Error('Do not self-approve this assessment.');c.stage=decision==='Accept'?'Accepted':decision==='Decline'?'Declined':'Partner hold';c.decisionBy=actor().name;c.history.push({decision,reason,actor:actor().name,time:new Date().toISOString()});state.engagements.filter(e=>e.client===c.client).forEach(e=>{e.acceptance=decision==='Accept';e.generation++;e.candidate=null;});addRoleEvent('Partner '+decision.toLowerCase()+' for '+c.id,c.client);});},
 'r-task-open':d=>{const t=rv().tasks.find(x=>x.id===d.id&&authorizedEng(x.eng));if(!t)throw Error('Unavailable task.');showCommand(t.id+' · ownership and date',`<p class="small">${esc(t.title)} · ${t.eng}</p><div class="field-grid mt16"><div class="field"><label for="task-owner">Responsible existing role</label><select class="input" id="task-owner">${['preparer','reviewer','manager','partner','eqr'].map(k=>`<option value="${k}" ${t.owner===k?'selected':''}>${ROLES[k].name} · ${ROLES[k].label}</option>`).join('')}</select></div><div class="field"><label for="task-due">Due date *</label><input class="input" type="date" id="task-due" value="${t.due}"></div></div>${textField('task-reason','Assignment/change reason')}`,'team.assign',()=>{const owner=val('task-owner'),due=required('task-due','Due date'),reason=reqText('task-reason');const e=state.engagements.find(e=>e.id===t.eng);if(!ROLES[owner].demoClients.includes(e.client))throw Error('The selected individual does not have this engagement scope.');if(owner!==t.owner)throw Error('This task requires its existing professional function. Use a separately verified same-function substitute in production; do not turn a preparer into a reviewer by assignment.');t.due=due;addRoleEvent('Task deadline updated: '+t.id+' · '+reason,e.client);});},
 'r-time-review':d=>{const t=state.times[Number(d.index)];if(!t||!authorizedEng(t.eng))throw Error('Unavailable time entry.');showCommand('Review time entry',`<p class="small">${esc(t.name)} · ${esc(t.task)} · ${t.hours} hours</p>${textField('time-reason','Review explanation')}`,'time.review',()=>{if(t.name===actor().name)throw Error('You cannot independently approve your own time.');t.status='Approved';t.reviewedBy=actor().name;t.reviewReason=reqText('time-reason');addRoleEvent('Time entry reviewed',state.engagements.find(e=>e.id===t.eng).client);});},
 'r-renewal-note':d=>{if(!authorizedEng(d.id))throw Error('Unavailable relationship.');showCommand('Record renewal follow-up',textField('renewal-reason','New-period facts / follow-up needed'),(state.role==='onboarding'?'intake.write':'lead.write'),()=>{rv().events.unshift({text:'Renewal coordination: '+reqText('renewal-reason'),scope:state.engagements.find(e=>e.id===d.id).client,role:state.role,actor:actor().name,time:new Date().toISOString()});});},
 'r-contact-new':()=>nominationDialog('client.nominate'),'r-invitation-new':()=>nominationDialog('intake.write'),
 'r-contact-detail':d=>{const r=requestedContact(d.id);openModal(r.id+' · access request',`<div class="info-grid"><div><label>Name</label><span>${esc(r.name)}</span></div><div><label>Requested role</label><span>${esc(ROLES[r.role].label)}</span></div><div><label>Status</label>${demoStatus(r.state)}</div></div><p class="sub mt16">${esc(r.reason)}</p>${note('A nomination is not active access or verified signatory authority.')}`,btn('Close','close-modal'));},
 'r-pbc-assign':d=>{const p=E().pbc.find(p=>p.id===d.id);if(!p)throw Error('Unavailable request.');showCommand('Assign client contributor',`<p class="small">${esc(p.title)}</p><div class="field mt16"><label for="pbc-owner">Verified contributor for this entity</label><select class="input" id="pbc-owner"><option>Rami Nasser</option></select></div>${textField('pbc-owner-reason','Assignment reason')}`,'pbc.assign',()=>{p.contributor=val('pbc-owner');addRoleEvent('PBC contributor assigned: '+p.id+' · '+reqText('pbc-owner-reason'),E().client);});},
 'r-client-terms':()=>showCommand('Management acceptance of terms',`<p class="small">${esc(C().name)} · ${E().service} · FY ${E().year}</p>${checkField('terms-authority','I represent the verified signatory in this synthetic scenario')}${textField('terms-statement','Acceptance statement')}`,'management.approve',()=>{if(!checked('terms-authority'))throw Error('Confirm the simulated signing authority.');E().terms=true;addRoleEvent('Management terms acceptance: '+reqText('terms-statement'),E().client);}),
 'r-journal-review':()=>showCommand('Independent adjustment review',`<p class="small">Depreciation reporting adjustment: ${cash(E().adjustment)}.</p>${textField('aj-check','Technical review evidence')}`,'journal.review',()=>{if(E().journalState!=='Management pending')throw Error('A current proposed journal is required.');E().journalTechnical={by:actor().name,amount:E().adjustment,revision:E().journalRevision||1,reason:reqText('aj-check')};addRoleEvent('Adjustment technical review recorded',E().client);}),
 'r-journal-consent':()=>showCommand('Management adjustment decision',`<p class="small">Proposed amount ${cash(E().adjustment)}. Existing status: ${esc(E().journalState)}. Approved reporting adjustments do not automatically post into an external or firm ledger.</p><div class="field mt16"><label for="aj-consent">Decision</label><select class="input" id="aj-consent"><option>Accept</option><option>Reject</option></select></div>${textField('aj-consent-reason','Management reason')}`,'journal.consent',()=>{const e=E();if(e.journalState!=='Management pending')throw Error('This journal is not awaiting a new management decision.');if(!e.journalTechnical||e.journalTechnical.amount!==e.adjustment||e.journalTechnical.revision!==(e.journalRevision||1))throw Error('Current independent technical review is required first.');const accepted=val('aj-consent')==='Accept';e.journalState=accepted?'Applied':'Rejected';e.journalManagement={by:actor().name,decision:val('aj-consent'),reason:reqText('aj-consent-reason')};changed('Management '+(accepted?'accepted':'rejected')+' reporting adjustment');}),
 'r-management-approve':()=>showCommand('Approve the exact management package',`<p class="small">Package v${E().packageRevision} · input generation ${E().generation}. This is management’s responsibility, not approval of the auditor’s opinion.</p>${checkField('mgmt-responsible','I accept management responsibility for the presented accounts in this synthetic scenario')}${textField('mgmt-reason','Approval statement')}`,'management.approve',()=>{approvalPrecheck('client');if(!checked('mgmt-responsible'))throw Error('Management responsibility must be confirmed.');E().approvals.client={generation:E().generation,packageRevision:E().packageRevision,actor:actor().name,time:new Date().toISOString(),scope:'client',statement:reqText('mgmt-reason')};addRoleEvent('Management approved exact package v'+E().packageRevision,E().client);}),
 'r-representation':()=>showCommand('Management responsibility attestation',`${checkField('rep-facts','I confirm the factual information and responsibilities requested for this demo package')}${textField('rep-reason','Attestation statement')}`,'management.approve',()=>{if(!isCurrent('manager'))throw Error('A current internally reviewed package must be presented first.');if(!checked('rep-facts'))throw Error('Confirm the responsibility statement.');rv().representations[E().id]={generation:E().generation,revision:E().packageRevision,by:actor().name,text:reqText('rep-reason')};addRoleEvent('Management representation recorded for exact package',E().client);}),
 'r-ack-delivery':d=>{const r=E().releases.find(r=>r.id===d.id&&r.delivered);if(!r)throw Error('No delivered package is available.');rv().acknowledgements[E().id+'/'+r.id]={by:actor().name,time:new Date().toISOString()};addRoleEvent('Client acknowledged delivered package '+r.id,E().client);render();toast('Demo acknowledgement recorded; not account approval.');},
 'r-eqr-eligibility':()=>showCommand('Independent reviewer eligibility',`${checkField('eqr-independent','I am not part of the engagement preparation/partner team in this synthetic scenario')}${checkField('eqr-competent','The assigned scope and competence conditions have been considered for this demonstration')}${textField('eqr-eligibility-reason','Eligibility rationale')}`,'eqr.complete',()=>{if(!checked('eqr-independent')||!checked('eqr-competent'))throw Error('Both eligibility declarations are required.');rv().eqr.eligible=true;rv().eqr.eligibilityReason=reqText('eqr-eligibility-reason');addRoleEvent('Independent EQR eligibility declared in demo',E().client);}),
 'r-eqr-add':()=>showCommand('Raise EQR concern',textField('eqr-title','Significant judgment and required response'),'eqr.complete',()=>{rv().eqr.concerns.push({id:'EQR-'+String(rv().eqr.concerns.length+1).padStart(3,'0'),title:reqText('eqr-title'),status:'Open',response:'',disposition:''});E().approvals.eqr=null;addRoleEvent('New EQR concern raised',E().client);}),
 'r-eqr-concern':d=>{const c=rv().eqr.concerns.find(c=>c.id===d.id);if(!c)throw Error('Unavailable concern.');showCommand(c.id+' · independent evaluation',`<p class="small">${esc(c.title)}</p><p class="sub mt12">Team response: ${esc(c.response||'No response recorded by the engagement manager yet.')}</p><div class="field mt16"><label for="eqr-disposition">Disposition</label><select class="input" id="eqr-disposition"><option>Remain open</option><option>Clear concern</option></select></div>${textField('eqr-review','Independent evaluation')}`,'eqr.complete',()=>{const clear=val('eqr-disposition')==='Clear concern';if(clear&&!c.response)throw Error('The engagement team must respond before independent clearance.');c.status=clear?'Cleared':'Open';c.disposition=reqText('eqr-review');c.reviewedBy=actor().name;addRoleEvent('EQR concern evaluated: '+c.id,E().client);});},
 'r-eqr-response':d=>{const c=rv().eqr.concerns.find(c=>c.id===d.id);if(!c)throw Error('Unavailable concern.');showCommand('Engagement team response',`<p class="small">${esc(c.title)}</p>${textField('eqr-team-response','Team response and evidence references')}`,'manager.approve',()=>{c.response=reqText('eqr-team-response');c.status='Responded';c.respondedBy=actor().name;E().approvals.eqr=null;addRoleEvent('Team responded to EQR concern '+c.id,E().client);});},
 'r-eqr-complete':()=>showCommand('Record independent EQR completion',textField('eqr-complete-reason','Completion rationale for the exact package'),'eqr.complete',()=>{if(!rv().eqr.eligible)throw Error('Reviewer eligibility must be recorded.');if(rv().eqr.concerns.some(c=>c.status!=='Cleared'))throw Error('All significant EQR concerns need independent resolution.');approvalPrecheck('eqr');E().approvals.eqr={generation:E().generation,packageRevision:E().packageRevision,actor:actor().name,time:new Date().toISOString(),scope:'eqr',reason:reqText('eqr-complete-reason')};addRoleEvent('Independent EQR completed for the exact package',E().client);}),
 'r-new-invoice':()=>showCommand('Prepare firm invoice draft',`<div class="field-grid"><div class="field"><label for="new-inv-client">Bill-to entity</label><select class="input" id="new-inv-client">${visibleClients().map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label for="new-inv-amount">Amount QAR *</label><input class="input" id="new-inv-amount" inputmode="decimal" value="1500.00"></div></div>${textField('new-inv-desc','Service and billing basis')}`,'invoice.draft',()=>{const amount=cents(required('new-inv-amount','Amount'));if(amount<=0)throw Error('The invoice amount must be positive.');const client=val('new-inv-client'),eng=state.engagements.find(e=>e.client===client);state.invoices.push({id:'INV-DEMO-'+String(state.invoices.length+1).padStart(3,'0'),client,eng:eng.id,amount,paid:0,description:reqText('new-inv-desc'),status:'Draft',preparedBy:actor().name,due:'2026-10-05'});addRoleEvent('New firm invoice draft prepared',client);}),
 'r-invoice-submit':d=>{const i=scopedInvoice(d.id);if(i.status!=='Draft')throw Error('Only a draft may be submitted.');i.status='Submitted for review';addRoleEvent('Invoice submitted for independent review: '+i.id,i.client);closeModal();render();},
 'r-review-invoice':d=>{const i=scopedInvoice(d.id);showCommand('Review '+i.id,`<p class="small">${esc(i.description)} · ${cash(i.amount)} · prepared by ${esc(i.preparedBy)}</p><div class="field mt16"><label for="inv-decision">Decision</label><select class="input" id="inv-decision"><option>Approve</option><option>Return for rework</option></select></div>${textField('inv-review','Commercial review rationale')}`,'invoice.review',()=>{if(i.preparedBy===actor().name)throw Error('The invoice needs an independent reviewer.');if(i.status!=='Submitted for review')throw Error('The invoice is no longer submitted.');const reason=reqText('inv-review');i.status=val('inv-decision')==='Approve'?'Approved for issue':'Draft';i.commercialApproval=i.status==='Approved for issue'?{by:actor().name,amount:i.amount,reason}:null;addRoleEvent('Commercial review for '+i.id,i.client);});},
 'invoice':d=>invoiceDialog(d.id),
 'invoice-issue':d=>{const i=scopedInvoice(d.id);if(i.status!=='Approved for issue'||!i.commercialApproval||i.commercialApproval.by===i.preparedBy||i.commercialApproval.amount!==i.amount)throw Error('A current independent commercial approval is required.');i.status='Issued';addRoleEvent('Demo firm invoice issued: '+i.id,i.client);closeModal();render();toast('Local issue recorded; no invoice was sent.');},
 'invoice-receipt':d=>{const i=scopedInvoice(d.id);if(!['Issued','Partially paid'].includes(i.status))throw Error('An issued invoice is required.');const n=cents(required('receipt-amount','Receipt amount'));if(n<=0||n>i.amount-i.paid)throw Error('Receipt must be positive and within the outstanding amount.');i.paid+=n;i.status=i.paid===i.amount?'Paid':'Partially paid';addRoleEvent('Synthetic firm receipt allocated: '+i.id,i.client);closeModal();render();toast('No money moved; client TB was unchanged.');},
 'r-commercial-new':()=>showCommand('Prepare billing exception request',`<div class="field-grid"><div class="field"><label for="exc-invoice">Invoice</label><select class="input" id="exc-invoice">${visibleInvoices().map(i=>`<option>${i.id}</option>`).join('')}</select></div><div class="field"><label for="exc-kind">Request</label><select class="input" id="exc-kind"><option>Credit note</option><option>Write-off</option><option>Refund</option></select></div></div>${textField('exc-reason','Reason and approval required')}`,'invoice.draft',()=>{rv().commercial.push({invoice:val('exc-invoice'),kind:val('exc-kind'),reason:reqText('exc-reason'),state:'Pending separate authorization'});addRoleEvent('Billing exception request prepared');}),
 'r-proposal-new':()=>showCommand('Prepare a commercial proposal',`<div class="field"><label for="prop-lead">Assigned lead</label><select class="input" id="prop-lead">${state.leads.map(l=>`<option value="${l.id}">${esc(l.name)}</option>`).join('')}</select></div><div class="field mt16"><label for="prop-amount">Illustrative fee QAR *</label><input class="input" id="prop-amount" value="5000.00"></div>${textField('prop-scope','Scope, deliverables and exclusions')}`,'proposal.submit',()=>{const lead=state.leads.find(l=>l.id===val('prop-lead')),amount=cents(required('prop-amount','Fee'));if(amount<=0)throw Error('The fee must be positive.');rv().proposals.push({id:'PROP-'+String(rv().proposals.length+1).padStart(3,'0'),lead:lead.id,title:lead.name+' · '+lead.service,amount,preparedBy:actor().name,state:'Submitted',note:reqText('prop-scope'),revision:1});addRoleEvent('Proposal submitted for independent review');}),
 'r-review-proposal':d=>{const p=rv().proposals.find(p=>p.id===d.id);if(!p)throw Error('Unavailable proposal.');showCommand('Review '+p.id,`<p class="small">${esc(p.title)} · ${cash(p.amount)}</p><p class="sub mt12">${esc(p.note)}</p>${textField('prop-review','Commercial approval rationale')}`,'proposal.review',()=>{if(p.preparedBy===actor().name)throw Error('Independent commercial review is required.');if(p.state!=='Submitted')throw Error('The proposal is not submitted.');p.state='Approved commercially';p.reviewedBy=actor().name;p.reviewReason=reqText('prop-review');addRoleEvent('Commercial proposal approved; professional acceptance remains separate');});}
});
function nominationDialog(cap){showCommand('Nominate a scoped client contact',`<p class="small">${cap==='client.nominate'?'Example Trading Entity only. No sibling or new entity access can be granted here.':'The request remains pending until entity and authority verification.'}</p><div class="field-grid mt16"><div class="field"><label for="contact-name">Synthetic contact name *</label><input class="input" id="contact-name" placeholder="Demo contact"></div><div class="field"><label for="contact-role">Requested client role</label><select class="input" id="contact-role"><option value="client_finance">Client finance contributor</option><option value="client">Client authorized signatory — needs authority evidence</option></select></div></div>${textField('contact-reason','Reason and intended scope')}`,cap,()=>{const name=required('contact-name','Name'),role=val('contact-role');if(!['client_finance','client'].includes(role))throw Error('Only permitted client-role nominations are supported.');rv().accessRequests.push({id:'ACCESS-'+String(rv().accessRequests.length+1).padStart(3,'0'),name,role,client:'CL-001',state:'Pending verification',requestedBy:actor().name,reason:reqText('contact-reason')});addRoleEvent('Client contact nominated for separate verification','CL-001');});}
Object.assign(actions,{
 'r-record-open':d=>{if(!authorizedEng(d.eng))throw Error('Unavailable record.');state.selected=d.eng;const r=E().releases.find(x=>x.id===d.id);if(!r)throw Error('Unavailable record.');openModal(r.id+' · preserved package',`<p class="small">${esc(C().name)} · ${E().id} · package v${r.revision}</p><div class="hash mt16">${esc(r.hash)}</div><p class="sub mt12">The exact package is selected; a new working revision is not substituted.</p><div class="row wrap mt20">${btn('View preserved preview','released-preview','soft sm',`data-id="${r.id}"`)}${btn('Export manifest','release-export','sm',`data-id="${r.id}"`)}</div>${note('All preservation states are local demonstrations. No provider lock is applied.','amber')}`,`${btn('Close','close-modal')}${r.delivered?btn('Assemble demo archive','archive','primary'):btn('Record authorized demo dispatch','demo-deliver','primary',`data-id="${r.id}"`)}`);},
 'r-hold-open':d=>{if(!authorizedEng(d.eng))throw Error('Unavailable record.');state.selected=d.eng;showCommand('Hold / disposition instruction',`<p class="small">${E().id} · ${esc(C().name)}</p><div class="mt12">${demoStatus(rv().holds[E().id]?'Active demo hold':'No recorded hold')}</div><div class="field mt16"><label for="hold-kind">Requested action</label><select class="input" id="hold-kind"><option>Record authorized hold</option><option>Request disposition review</option></select></div>${textField('hold-reason','Instruction reference and reason')}`,'hold.record',()=>{const reason=reqText('hold-reason');if(val('hold-kind')==='Record authorized hold'){rv().holds[E().id]={reason,by:actor().name,time:new Date().toISOString(),state:'SIMULATED_HOLD'};addRoleEvent('Synthetic hold instruction recorded',E().client);}else{if(rv().holds[E().id])throw Error('An active hold blocks disposition. Hold release requires separate authorized evidence and is not simulated here.');rv().handover.push({id:'DISP-'+(rv().handover.length+1),eng:E().id,scope:'Disposition review only — no deletion',recipient:'Authorized records approver',reason,status:'Pending expiry and authority verification'});addRoleEvent('Disposition review requested; no deletion performed',E().client);}});},
 'r-handover-new':()=>showCommand('Prepare controlled handover',`<div class="field"><label for="handover-eng">Authorized engagement</label><select class="input" id="handover-eng">${visibleEngagements().map(e=>`<option value="${e.id}">${e.id} · ${esc(state.clients.find(c=>c.id===e.client)?.name)}</option>`).join('')}</select></div><div class="field mt16"><label for="handover-recipient">Synthetic authorized recipient *</label><input class="input" id="handover-recipient"></div><div class="field mt16"><label for="handover-scope">Requested scope</label><select class="input" id="handover-scope"><option>Client-owned supplied records</option><option>Already delivered client packages</option><option>Restricted workpaper release — separate approval required</option></select></div>${textField('handover-reason','Reason and release authority needed')}`,'handover.request',()=>{const eng=val('handover-eng');if(!authorizedEng(eng))throw Error('Unavailable scope.');rv().handover.push({id:'HAND-'+String(rv().handover.length+1).padStart(3,'0'),eng,recipient:required('handover-recipient','Recipient'),scope:val('handover-scope'),reason:reqText('handover-reason'),status:'Pending independent release approval'});addRoleEvent('Scoped handover request prepared',state.engagements.find(e=>e.id===eng).client);}),
 'r-user-open':d=>{const u=rv().users.find(u=>u.id===d.id);if(!u)throw Error('Unavailable user.');showCommand('Technical user-state request',`<p class="small">${esc(u.name)} · ${esc(u.label)}</p><p class="sub mt12">No role or authority is added by this operation.</p><div class="field mt16"><label for="user-next">Local simulation state</label><select class="input" id="user-next"><option ${u.status==='Disabled'?'selected':''}>Disabled</option><option ${u.status==='Active'?'selected':''}>Active</option></select></div>${textField('user-reason','Authorized instruction / reason')}`,'access.execute',()=>{if(u.id===state.role&&val('user-next')==='Disabled')throw Error('Keep the current demo administrator active to avoid locking out the presenter.');u.status=val('user-next');u.reason=reqText('user-reason');addRoleEvent('Demo user state changed: '+u.id+' · '+u.status);});},
 'r-access-verify':d=>{const r=requestedContact(d.id);showCommand(r.id+' · verify scoped access',`<p class="small">${esc(r.name)} · requested ${esc(ROLES[r.role].label)} · ${r.client}</p>${checkField('access-entity','Entity ownership and requested scope are verified in this synthetic scenario')}${checkField('access-identity','Identity/contact channel and applicable approver instruction are verified')}${r.role==='client'?checkField('access-signatory','Separate management signing authority is verified'):''}${textField('access-reason','Verification evidence reference')}`,'access.execute',()=>{if(!checked('access-entity')||!checked('access-identity')||(r.role==='client'&&!checked('access-signatory')))throw Error('All required identity, scope and signatory checks must be completed.');if(r.requestedBy===actor().name)throw Error('Independent authorization of the requested scope is required.');r.state='Approved in demo';r.verifiedBy=actor().name;r.evidence=reqText('access-reason');addRoleEvent('Access request verified locally: '+r.id);});},
 'r-config-new':()=>showCommand('Prepare technical configuration change',`<div class="field"><label for="config-title">Change title *</label><input class="input" id="config-title" placeholder="Example: staging document repository binding"></div>${textField('config-reason','Purpose and required approval — do not enter secrets')}`,'system.configure',()=>{rv().draftChanges.push({id:'CONFIG-'+(rv().draftChanges.length+1),title:required('config-title','Title'),reason:reqText('config-reason'),state:'Draft'});addRoleEvent('Configuration draft recorded; no integration enabled');}),
 'r-config-detail':d=>{const c=rv().draftChanges.find(c=>c.id===d.id);if(!c)throw Error('Unavailable draft.');openModal(c.id,`<h3>${esc(c.title)}</h3><p class="sub mt16">${esc(c.reason)}</p>${note('Draft only. No credentials, tenant settings or runtime configuration were changed.')}`,btn('Close','close-modal'));}
});
// Keep the existing accounting/review demo, adding narrower projections and explicit role boundaries.
const originalAccounting=pages.accounting,originalReview=pages.reviews,originalAcquisition=pages.acquisition;
pages.accounting=()=>originalAccounting()+(['reviewer'].includes(state.role)?`<div class="panel panel-pad mt20"><h3>Independent adjustment review</h3><p class="sub mt8">The management signatory cannot consent to a new reporting journal before its technical review.</p><div class="mt16">${btn('Review current proposed adjustment','r-journal-review','soft sm')}</div></div>`:'');
pages.reviews=()=>originalReview()+(state.role==='manager'&&E().eqrRequired?`<div class="mt20">${queuePanel('Respond to independent EQR concerns','Only the quality reviewer can clear the concerns.',rv().eqr.concerns.map(c=>({title:c.title,meta:c.id,status:c.status,action:'r-eqr-response',extra:`data-id="${c.id}"`,label:'Respond as manager'})))}</div>`:'');
pages.acquisition=()=>originalAcquisition()+`<div class="panel panel-pad mt20"><div class="between"><div><h3>Versioned commercial proposals</h3><p class="sub">Draft and submit for independent manager review. This does not accept the client.</p></div>${btn('Prepare proposal','r-proposal-new','soft sm')}</div>${rv().proposals.map(p=>`<div class="config-row"><div><h4>${esc(p.title)}</h4><p>${p.id} · ${cash(p.amount)}</p></div>${demoStatus(p.state)}</div>`).join('')}</div>`;
Object.assign(pages,{overview:roleHome,'role-guide':roleDirectory,privileges:privilegePage,'client-summary':safeClientSummary,intake:intakePage,compliance:compliancePage,acceptance:acceptancePage,team:teamPage,renewal:renewalPage,portal:clientPortalHome,'client-team':clientTeam,'client-requests':clientRequests,'client-approvals':clientApprovals,'client-deliverables':clientDeliverables,quality:qualityPage,billing:billingPage,'commercial-review':commercialReview,'commercial-requests':commercialRequests,records:recordsPage,handover:handoverPage,invitations:invitationPage,administration:adminPage,'access-requests':accessRequestsPage,operations:operationsPage,'my-time':myTimePage});
const oldReleaseBlockers=releaseBlockers;
releaseBlockers=function(e=E()){const blockers=oldReleaseBlockers(e);if(isAudit(e)&&!(rv().representations[e.id]?.generation===e.generation&&rv().representations[e.id]?.revision===e.packageRevision))blockers.push('Current management representation is missing');if(e.eqrRequired&&(!rv().eqr.eligible||rv().eqr.concerns.some(c=>c.status!=='Cleared')))blockers.push('Independent EQR eligibility/concern resolution is incomplete');return blockers;};
const oldSaveAJ=actions['save-aj'];actions['save-aj']=d=>{oldSaveAJ(d);E().journalTechnical=null;E().journalRevision=(E().journalRevision||1)+1;save();};
actions['apply-adjustment']=()=>actions['r-journal-consent']();
actions['record-terms']=()=>actions['r-client-terms']();
actions['portal-approve']=()=>actions['r-management-approve']();
const oldPackageApproval=actions['package-approve'];actions['package-approve']=d=>{if(d.key==='client')return actions['r-management-approve']();if(d.key==='eqr')return actions['r-eqr-complete']();return oldPackageApproval(d);};
actions['new-eng']=()=>engagementModal();
actions['eng-open']=d=>{if(!authorizedEng(d.id))throw Error('The requested record is unavailable.');state.selected=d.id;closeModal();ui.selectedReview=E().reviews[0]?.id||'';save();go(routeAllowed('engagements')?'engagements':routeAllowed('accounting')?'accounting':'overview');};
actions['open-overview']=()=>go('overview');
actions['save-client-query']=()=>{const text=required('client-query-text','Question');rv().queries.push({eng:E().id,by:actor().name,text,status:'Recorded locally — not sent'});addRoleEvent('Client-facing question recorded',E().client);closeModal();render();toast('Local message recorded; no email sent.');};
actions['log-time']=()=>openModal('Record your work time',`<div class="field-grid"><div class="field full"><label for="time-task">Work description *</label><input id="time-task" class="input"></div><div class="field"><label for="time-hours">Hours *</label><input id="time-hours" class="input" type="number" min="0.1" max="24" step="0.1" value="1"></div><div class="field"><label>Authorized engagement</label><span class="input">${E().id}</span></div></div>`,`${btn('Cancel','close-modal')}${btn('Submit time entry','save-time','primary')}`);
const oldSaveTime=actions['save-time'];actions['save-time']=d=>{oldSaveTime(d);state.times.at(-1).status='Submitted';save();};
const originalPbcModal=pbcModal;pbcModal=function(id){if(!isClientRole())return originalPbcModal(id);const p=E().pbc.find(p=>p.id===id);if(!p||(state.role==='client_finance'&&p.contributor!==actor().name))throw Error('The requested record is unavailable.');openModal(p.id+' · '+p.title,`<div class="between">${demoStatus(p.status)}<span class="caption">Due ${shortDate(p.due)}</span></div><p class="sub mt16">Authorized entity: ${esc(C().name)}. The firm separately reviews evidence adequacy.</p><div class="dropzone mt20">${icon('upload')}<p>${esc(p.file||'Provide the requested synthetic file')}</p><small>5 MiB maximum · metadata and local hash only; bytes are not persisted.</small><div class="row wrap mt16">${btn('Choose local file','pick-document','soft sm',`data-id="${p.id}"`)}${btn('Use built-in sample','sample-upload','sm',`data-id="${p.id}"`)}</div></div>${p.message?`<div class="comment"><h4>Client-facing clarification</h4><p>${esc(p.message)}</p></div>`:''}${p.history?.length?`<p class="caption mt16">${p.history.length} previous submission metadata record(s) retained.</p>`:''}`,btn('Close','close-modal'));};
actions['pbc-detail']=d=>pbcModal(d.id);
const sampleAction=actions['sample-upload'];actions['sample-upload']=d=>{sampleAction(d);const p=E().pbc.find(p=>p.id===d.id);if(p)p.lastUploader=actor().name;save();};
const oldPbcAccept=actions['pbc-accept'];actions['pbc-accept']=d=>{const p=E().pbc.find(p=>p.id===d.id);if(p?.lastUploader===actor().name)throw Error('An independent technical reviewer must accept the submission.');return oldPbcAccept(d);};
// Client consent, technical review and release remain separate even in the demo.
const BASE_ACTION_RULES={
 'new-lead':'lead.write','save-lead':'lead.write','lead-advance':'lead.write','new-eng':'team.assign','save-eng':'team.assign',
 'new-pbc':'pbc.clarify','save-pbc':'pbc.clarify','pick-document':'pbc.upload','sample-upload':'pbc.upload','pbc-accept':'pbc.accept','pbc-clarify':'pbc.clarify','save-pbc-clarification':'pbc.clarify',
 'import-tb':'tb.import','pick-tb':'tb.import','source-change':'tb.import','replace-tb':'tb.import','approve-mapping':'mapping.approve','revise-adjustment':'journal.propose','save-aj':'journal.propose','apply-adjustment':'journal.consent',
 'build-package':'workpaper.write','confirm-supplements':'workpaper.write','clear-rec':'workpaper.review','submit-wp':'workpaper.write','clear-wp':'workpaper.review','save-planning':'partner.approve','save-sample':'workpaper.write',
 'respond-note':'review.respond','clear-note':'review.clear','reopen-note':'review.clear','add-note':'review.raise','save-note':'review.raise','portal-approve':'management.approve','record-terms':'management.approve',
 'prepare-release':'release.issue','issue-release':'release.issue','demo-deliver':'release.dispatch','archive':'archive.assemble','invoice-issue':'invoice.issue','invoice-receipt':'receipt.record','log-time':'time.record','save-time':'time.record','continue-client':'continuance.decide','roll-forward':'continuance.decide',
 'r-intake-open':'intake.write','r-compliance-open':'compliance.decide','r-partner-case':'acceptance.decide','r-task-open':'team.assign','r-time-review':'time.review','r-pbc-assign':'pbc.assign','r-client-terms':'management.approve','r-journal-review':'journal.review','r-journal-consent':'journal.consent','r-management-approve':'management.approve','r-representation':'management.approve','r-eqr-eligibility':'eqr.complete','r-eqr-add':'eqr.complete','r-eqr-concern':'eqr.complete','r-eqr-complete':'eqr.complete','r-eqr-response':'manager.approve','r-contact-new':'client.nominate','r-invitation-new':'intake.write','r-new-invoice':'invoice.draft','r-invoice-submit':'invoice.draft','r-review-invoice':'invoice.review','r-commercial-new':'invoice.draft','r-proposal-new':'proposal.submit','r-review-proposal':'proposal.review','r-record-open':'archive.assemble','r-hold-open':'hold.record','r-handover-new':'handover.request','r-user-open':'access.execute','r-access-verify':'access.execute','r-config-new':'system.configure','r-config-detail':'system.configure'};
const GENERIC_ACTIONS=new Set(['menu','close-modal','search','activity','about','reset','confirm-reset','source','service-detail','lifecycle-detail','r-role-directory','r-my-privileges','r-filter-group','r-switch-role','r-role-detail','r-export-catalog','r-export-permissions','tour','tour-requirements']);
function allowedAction(name,d={}){
 if(GENERIC_ACTIONS.has(name))return true;
 if(!roleEnabled())return false;
 if(name==='r-confirm')return !!pendingRoleCommand&&pendingRoleCommand.role===state.role&&permission(pendingRoleCommand.capability);
 if(BASE_ACTION_RULES[name])return permission(BASE_ACTION_RULES[name]);
 if(name==='r-renewal-note')return ['relationship','onboarding'].includes(state.role);
 if(name==='r-contact-detail')return ['client_admin','onboarding','admin'].includes(state.role);
 if(name==='r-ack-delivery'||name==='client-query'||name==='save-client-query')return isClientRole();
 if(name==='invoice')return state.role==='billing';
 if(name==='lead-detail')return state.role==='relationship';
 if(name==='package-approve')return state.role===d.key&&permission({manager:'manager.approve',client:'management.approve',partner:'partner.approve',eqr:'eqr.complete'}[d.key]);
 if(['open-reviews','open-approvals'].includes(name))return routeAllowed('reviews');
 if(name==='open-overview')return true;
 if(name==='eng-open')return !isClientRole()&&state.role!=='admin'&&!!d.id&&authorizedEng(d.id);
 if(name==='client-detail')return routeAllowed('clients')&&visibleClients().some(c=>c.id===d.id);
 if(name==='service-for-eng')return routeAllowed('services');
 if(name==='pbc-detail')return ['preparer','reviewer','manager','client_finance'].includes(state.role)&&(!d.id||E().pbc.some(p=>p.id===d.id&&(state.role!=='client_finance'||p.contributor===actor().name)));
 if(['preview-report','export-report','print-report'].includes(name))return permission('financial.read')||(state.role==='client'&&isCurrent('manager'));
 if(['released-preview','released-download'].includes(name))return ['partner','records'].includes(state.role)||(isClientRole()&&E().releases.some(r=>r.id===d.id&&r.delivered));
 if(name==='release-export')return ['partner','records'].includes(state.role);
 if(['tb-template','export-tb','reconciliation','supplements'].includes(name))return permission('financial.read');
 if(['workpaper','planning','sample-result','summary-memo','announcement','report-memo','select-review'].includes(name))return ['preparer','reviewer','manager','partner'].includes(state.role);
 if(name==='offboarding')return ['records','partner'].includes(state.role);
 if(name==='tab')return !isClientRole()&&state.role!=='admin';
 if(name==='tour-accounting')return routeAllowed('accounting');
 return false;
}
const legacyButton=btn;
btn=function(text,action,cls='',extra='',ic=''){const data={};for(const m of extra.matchAll(/data-([\w-]+)="([^"]*)"/g))data[m[1].replace(/-([a-z])/g,(_,c)=>c.toUpperCase())]=m[2];if(!allowedAction(action,data))extra+=' disabled aria-disabled="true" title="Not permitted for this role in the demonstration"';return legacyButton(text,action,cls,extra,ic);};
linkBtn=function(text,route,cls=''){return `<button class="btn ghost ${cls}" data-route="${route}" ${routeAllowed(route)?'':'disabled title="Outside this role’s navigation"'}>${text}${icon('arrow')}</button>`;};
const originalRoleAllowed=roleAllowed;roleAllowed=function(roles){return originalRoleAllowed(roles);};
context=function(){if(isClientRole())return clientContext();const e=E(),list=visibleEngagements();if(!e||!list.length)return '';return `<div class="contextbar"><div class="context-item"><label>Authorized engagement</label><select id="eng-select" aria-label="Selected authorized engagement">${list.map(x=>`<option value="${esc(x.id)}" ${x.id===e.id?'selected':''}>${esc(state.clients.find(c=>c.id===x.client)?.name)} · ${x.year}</option>`).join('')}</select></div><div class="context-item"><label>Service / period</label>${esc(e.service)} · FY ${e.year}</div><div class="context-item"><label>Reporting basis</label>${esc(e.mode)} · QAR</div><div class="context-item"><label>Package</label>v${e.packageRevision} ${badge(e.builtGeneration===e.generation?'Current':'Stale')}</div></div>`;};
go=function(route){if(!routeAllowed(route)){toast('This workspace is not available to the selected role.','warn');ui.route='overview';location.hash='overview';render();return;}closeModal();ui.route=route;location.hash=route;render();window.scrollTo(0,0);};
function filteredEvents(){return rv().events.filter(ev=>{if(isClientRole())return ev.scope==='CL-001'&&['client_admin','client_finance','client'].includes(ev.role);if(state.role==='admin')return ev.role==='admin';if(state.role==='billing')return ['billing','manager'].includes(ev.role)&&/invoice|receipt|commercial|billing|proposal/i.test(ev.text);if(state.role==='compliance')return ev.role==='compliance'&&ROLES[state.role].demoClients.includes(ev.scope);if(ev.role==='compliance'||ev.role==='admin')return false;return ev.role===state.role||(ev.scope&&ROLES[state.role].demoClients.includes(ev.scope));});}
actions.activity=()=>openModal('Your local activity',`<p class="sub mb16">Editable browser history, not a secure audit log.</p>${filteredEvents().map(ev=>`<div class="activity"><div class="activity-dot">${icon('clock')}</div><div><p>${esc(ev.text)}</p><small>${esc(ev.actor)} · ${esc(ev.time)}</small></div></div>`).join('')||'<div class="empty">No visible role activity yet.</div>'}`,btn('Close','close-modal'));
actions.tour=()=>openModal('Explore the complete role handoffs',`<p class="small">Use the top-right simulator to switch among 14 source roles. It is a presentation control, not a real login.</p>${[['1','Commercial → onboarding → compliance → partner','Prepare a proposal, collect CASE-003 evidence, submit for compliance, then obtain a distinct partner decision.'],['2','Client administrator → finance contributor','Nominate a contact, assign a document request, then upload a built-in synthetic sample as the contributor.'],['3','Preparer → senior → manager','Respond to review points, clear them independently, rebuild the package as preparer, then record manager review.'],['4','Client signatory → partner → quality reviewer','Approve the presented accounts, attest representations, respond to EQR concerns as manager, then record EQR as its independent reviewer.'],['5','Partner → records administrator','Issue the frozen demo package, record authorized dispatch, assemble its manifest and demonstrate hold/disposition blocking.'],['6','Billing officer → manager → billing officer','Prepare/submit an invoice, review it independently, then issue and record a bounded synthetic receipt.']].map(([n,t,p])=>`<div class="tour-step"><div class="stepnum">${n}</div><div><h4>${t}</h4><p>${p}</p></div></div>`).join('')}<p class="policy-footnote">System administration is a separate technical view. No role selection grants actual provider access, signs a document or creates a production approval.</p>`,`${btn('Close','close-modal')}${btn('Explore all roles','r-role-directory','primary')}`,true);
actions.about=()=>openModal('AuditSphere · all-role visualization',`<h3>14 source roles · 44 proposed activity families</h3><p class="sub mt16">Source: STE-PRD-001 revision 1.0, §§2 and 21, supported by the lifecycle and approval requirements. Role duties are source-derived; dashboard layouts and narrower default permissions are proposed design decisions.</p><p class="sub mt16">This extends the earlier interactive prototype with role-specific homepages, navigation, record projections, command checks and handoffs. It preserves the original prototype and source documents.</p><p class="sub mt16">No backend, authentication, enforceable multi-user security, secure storage, live Microsoft integration, legal signature, real financial posting, complete methodology or production retention is implemented. Source files and all demo data remain inspectable by anyone who has this HTML. Use synthetic data only.</p>`,btn('Close','close-modal'),true);
updateSearch=function(query){const q=query.toLowerCase();const items=ROLES[state.role].routes.map(route=>({title:ROLE_ROUTES[route][0],sub:'Permitted workspace',route}));if(['preparer','reviewer','manager','partner'].includes(state.role))items.push(...visibleEngagements().map(e=>({title:state.clients.find(c=>c.id===e.client)?.name,sub:e.id,eng:e.id})));const matches=items.filter(x=>(x.title+' '+x.sub).toLowerCase().includes(q)).slice(0,12),target=document.getElementById('search-results');if(target)target.innerHTML=matches.length?matches.map(x=>`<button class="search-result" ${x.route?`data-route="${x.route}"`:`data-action="eng-open" data-id="${x.eng}"`}>${icon(x.route?'grid':'users')}<div><b class="small">${esc(x.title)}</b><small>${esc(x.sub)}</small></div></button>`).join(''):'<div class="empty">No matching records in this role’s scope.</div>';};
render=function(){initRoleState();ensureContext();if(!routeAllowed(ui.route)){ui.route='overview';if(location.hash!=='#overview')history.replaceState(null,'','#overview');}const r=ROLES[state.role],title=ROLE_ROUTES[ui.route]?.[0]||r.home;const main=roleEnabled()?scopedView(()=>pages[ui.route]()):roleHome();const groups=['Commercial','Professional','Client','Operations'];document.getElementById('app').innerHTML=`<div class="mobile-overlay" data-action="menu" id="mobile-overlay"></div><aside class="sidebar" id="sidebar"><div class="brand"><div class="brandmark">${icon('layers')}</div><div class="brandname">Audit<span>Sphere</span></div></div><div class="workspaceselect"><span class="firmavatar">STE</span><span>STE Audit & Accounting<div style="font-size:9px;color:#8daba7;margin-top:1px">${esc(r.group)} workspace</div></span></div><div class="portal-nav-context">${esc(r.label)}<br><span class="role-mini">${isClientRole()?'CLIENT PORTAL':'ROLE-SCOPED WORKSPACE'}</span></div><nav class="side-scroll" aria-label="Role navigation"><div class="nav-label">YOUR WORKSPACE</div>${r.routes.filter(routeAllowed).map((route,i)=>`${route==='role-guide'?'<div class="nav-label">DESIGN REFERENCE</div>':''}<button class="navitem ${route===ui.route?'active':''}" data-route="${route}" ${route===ui.route?'aria-current="page"':''}>${icon(ROLE_ROUTES[route][1])}<span>${route==='overview'?'My dashboard':esc(ROLE_ROUTES[route][0])}</span></button>`).join('')}</nav><div class="side-footer"><div class="side-demo"><strong>14-role interactive visualization</strong>All data is synthetic.<br>No real login or secure access control.</div><button class="navitem" data-action="reset">${icon('refresh')}Reset local demo</button></div></aside><div class="shell"><header class="topbar"><div class="topbar-left"><button class="icon-btn mobile-menu" data-action="menu" aria-label="Open navigation">${icon('menu')}</button><div class="role-breadcrumb">${esc(r.group)} / <b>${esc(title)}</b></div><button class="search-trigger" data-action="search">${icon('search')}Search your workspace<kbd>/</kbd></button></div><div class="topbar-right"><button class="icon-btn" data-action="tour" aria-label="Explore role handoffs">${icon('play')}</button><button class="icon-btn" data-action="activity" aria-label="Your local activity">${icon('bell')}</button><div class="persona">${avatar(r.initials,r.group==='Client'?'blue':'')}<div><label for="role-select">DEMO PERSONA · NOT A LOGIN</label><select id="role-select" aria-label="Switch simulated user role">${groups.map(g=>`<optgroup label="${g}">${Object.entries(ROLES).filter(([k,v])=>v.group===g).map(([k,v])=>`<option value="${k}" ${k===state.role?'selected':''}>${esc(v.label)}</option>`).join('')}</optgroup>`).join('')}</select></div></div></div></header><main class="main" id="main" tabindex="-1">${main}</main></div>`;document.title=`${r.label} · AuditSphere Role Prototype`;};
// A centralized frontend guard keeps the visualization internally consistent.
// It does not protect HTML/localStorage from a user with developer tools.
for(const [name,fn] of Object.entries(actions)){actions[name]=async function(d={}){if(!allowedAction(name,d))throw Error('This action is not permitted for the selected demo role.');if(d.eng&&!authorizedEng(d.eng)&&state.role!=='admin')throw Error('The requested record is unavailable.');return fn(d);};}
document.addEventListener('click',event=>{const target=event.target.closest('[data-action]');if(target&&!allowedAction(target.dataset.action,target.dataset)){event.preventDefault();event.stopImmediatePropagation();toast('This action is not permitted for this role.','warn');}},true);
document.addEventListener('change',event=>{const t=event.target;if(t.id==='role-select'){event.stopImmediatePropagation();switchRole(t.value);return;}if(t.id==='eng-select'&&!authorizedEng(t.value)){event.stopImmediatePropagation();render();toast('The requested context is unavailable.','warn');return;}if(t.id==='document-picker'&&!permission('pbc.upload')||t.id==='tb-picker'&&!permission('tb.import')){event.stopImmediatePropagation();toast('This upload action is outside the selected role.','warn');return;}if(t.dataset.change==='mapping'&&!permission('mapping.write')||t.dataset.change==='assessment-item'&&!permission('intake.write')||t.dataset.change==='annual'&&state.role!=='partner'){event.stopImmediatePropagation();render();toast('This edit is outside the selected role.','warn');}},true);
const resetAction=actions['confirm-reset'];actions['confirm-reset']=async d=>{await resetAction(d);initRoleState();state.role='manager';render();save();};
// No v1 state is migrated or deleted; v2 uses its own localStorage key.
initRoleState();ensureContext();render();save();
